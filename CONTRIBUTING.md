# Contributing

## Development setup

- Use Python 3.12+
- Install dependencies: `pip install -e .[dev]`
- Run tests: `pytest`

## Pull requests

- Keep PRs focused and well-described.
- Add tests for new behavior.
- Ensure lint/test checks pass before requesting review.
