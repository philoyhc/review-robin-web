# Architecture

This repository currently contains a lightweight FastAPI service:

- `app/main.py` creates the application instance.
- `app/web/routes_health.py` provides health endpoints.
- `tests/` contains pytest-based tests.

As the project grows, additional route modules, services, and data layers can be added under `app/`.
