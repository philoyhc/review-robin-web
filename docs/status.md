# Implementation status

**As of:** 2026-09-23. **Segment 19R is closed and archived** — optimization and bugfixes, off the responsiveness measurements in `guide/app_responsiveness.md`; eight items shipped and a ninth moved to `guide/deferred_consolidated.md` unbuilt (`guide/archive/segment_19R_optimization_and_bugfixes.md`). **Segment 19S is open**, now at **ten items** — **Items 1–6, 8 and 9 closed**, 7 and 10 open — `guide/segment_19S_post_assessment.md`. **Item 1 is the register** of what the 22 September assessment and the independent Codex read surfaced with no home of its own: eight entries, none scheduled, each carrying the trigger that would promote it to an item. **Items 2–4 are what has been promoted out of it**, and all three are now closed: the four index gates (from **E4**) as `tests/unit/test_index_currency.py`; the **investigation** into what `spec/csv_contracts.md` §3.2 actually describes (from **E8**'s first half) as `guide/findings_2026-09-22_csv_contracts.md`, ten rows all dispositioned; and Prepare's per-pair insert (from **E1**, **widened to the recompute pass** after a trace found the insert was one of three full ORM materialisations) as a bulk insert plus a column-projected recompute — **Prepare measured 26.7 s → 13.1 s**, two of the three materialisations gone and the verify pass deferred. **Seven of the eight entries are disposed of** — four closed (**E2**, **E4**, **E6**, **E7**), **E3 retired** on the author's ruling that the ≥1,000 LOC list is not a tripwire but part of an assessment's judgment, **E1** promoted to Item 3, **E8** closed with its first half promoted to Item 4, and **E5** promoted to Item 5 — so every entry is disposed of, and **Item 1 closed on that basis the day it opened**. **Items 6, 7 and 9** were logged separately on the author's instruction: a Tag box on the Create page; typeahead on the two tag boxes; and, once the first shipped, the two halves it scoped out — an Owners card below Tags on Create, and a Tags card below User interface settings on Session Home's details card. **Item 9 is closed**: Create's Owners card stages co-owners that Create session saves, and Session Home's Tags field saves with the details card inside its edit window. **Item 8** was logged out of Item 4's own defect record and is **closed**: a pytest node id cited in live prose resolves, checked over 7 citations in two shapes. **Item 5 is closed**: `Blast radius` sections from segment 19S on must state the commit or date they were measured at, checked by G5, and rung 2 decided against building the re-run — `docs/unenforced_conventions.md` §1.10. **Item 6 is closed**: a Tags box on the Create page, submitting with the Create session button and writing after the settings CSV, so a typed tag beats a bundle's rows by ordering rather than by changing the tag applier — plus a bundled P1, the settings apply that never committed. It is deliberately not a queue, which is what both reads asked for. **Segments 19O and 19Q are both closed and archived** — `guide/archive/segment_19O_rosters_and_instruments.md` (eight items) and `guide/archive/segment_19Q_workflow_and_previews.md` (seven). Item 1 retired the Previews hub onto the Invitations per-reviewer drill-in, Item 2 folded invitation creation into Prepare, Item 3 brought the in-app Guide and the Workflow card's own copy up to both, Item 4 fixed a button sitting outside its own column and stopped the card's right column reproducing the Validate page, Item 6 made an instrument's operator-facing number per-session, and Item 7 closed a rule that had one home only the CSV importer could reach — one mailbox under two names. Item 5 took the author's replacement capture set — 19 pairs to 17 — and found that nothing in the suite can tell a light/dark pair from two different screens. 19O's Item 7 ran as a **register of loose ends** — findings that would otherwise live only in a closed item's judgment calls, which is how three of them were lost to a `Status` compaction before it existed. It closed at sixteen entries, and its last two produced 19O Item 8: the Sessions lobby's table overflowing its card, which measurement turned into seven pages and a universal `.table-scroll` rule.

**Its largest arc is accessibility.** `docs/known_limitations.md` had
recorded a WCAG AA failure since **2026-05-18** naming a token that no
longer existed — stale rather than fictitious, and live the whole time at
**2.31:1**. Repairing it widened twice, each time because the narrower
version was lying by omission, ending at a sweep over **73 pairs / 146
theme-resolved pairings**. `--text-dim` retired into `--text-subtle` on
the author's counter-proposal once measurement showed one tier cheaper
than two, and collapsing a hierarchy became general policy. Item 10
closed the last four shortfalls by inverting three dark mappings: the
accent family was the lone outlier still carrying white onto a bright
dark fill, and the palette had already answered that question for amber.
Darkening the fill was rejected with the measurement — it would have
traded a text failure for a **2.76** control boundary, under the 3:1 WCAG
1.4.11 asks. The palette now clears AA normal in both themes, with three
transient hover dips accepted on machine-checked premises, and the theme
customizer's Contrast panel renders a row per pair from the same function
the test imports, so the audit is inspectable and not only enforced.

**Item 9 moved out whole** rather than closing, to
`guide/post_azure_todo_checklist.md` item 4. The inline stylesheet is
**157.7 KB**, **64.6–80.5%** of a rendered page, and its first rung needs
a response header from a dev slot the container's network policy refuses
(`403 CONNECT tunnel failed`) — *a segment that cannot close until
someone reads a header is held open by something it does not own.*
Nothing about the stylesheet is resolved by the move, and both documents
say so.

**Four findings the segment keeps.** *A clean result and a no-op are
indistinguishable until you check which one you have* — Item 5's trial
opened at 0 verdict flips, which is also what a rule that does nothing
returns; confirming it bit exposed a bug reading every committed
directory as missing. *A fixture that produces no data makes every
assertion about that data true* — Item 3 shipped two vacuous tests, the
second written because a mutation escaped the first. *Reverting a
mutation with the version-control tool reverts the work too, while the
work is uncommitted.* And, four separate times, **a correction reaches
the paragraph it was found in and no further unless someone walks it** —
most instructively at the close, where `spec/validate_page.md` §2.4 was
found describing a grouping superseded in Segment **15E**, caught only
because this segment's own Guide copy was the more accurate of the two
descriptions. `spec-writer` moved before the push at Item 3 and stayed
there.

Before it — **Segment 19J — the assessment's three moves — closed and
archived** at ten items, PRs #2281 → #2311; plan now at
`guide/archive/segment_19J_assessment_moves.md`. Before that — **Segment
19H — additional refinements — closed and archived** at seven items, PRs
#2219 → #2278, alongside **Segment 19I**, which closed the same day:
`guide/archive/segment_19H_additional_refinements.md`.

Before them — **Segment 19I — roster search and row
deletion — closed and archived** at thirteen items (twelve closed,
one withdrawn), PRs #2230 → #2276; plan now at
`guide/archive/segment_19I_roster_search_and_row_delete.md`. Its last
shipped item — **Item 12**: one
place for column selection, and one card fewer. The `Show columns:`
chips now sit in the preview-table card on all **six** surfaces that
have them — four moved there, Invitations and Responses having
arrived at Item 11 — a slot with no data anywhere in the roster
renders
neither chip nor column, and the "Fields with data" card retired —
its pills said what the chips say, and a chip also acts on it.
Before it — **Segment 19I Item 11**: the
roster table facility on Invitations and Responses. Both Operations
tables gained tag columns, show/hide chips, sortable headers and
tag-aware search — and the mechanism that made it cheap was
extracting the column-visibility JS, duplicated byte-for-byte across
four templates, into one primitive in `base.html`. Before it —
**Segment 19I Item 10**: one preview-count sentence across seven
pages, branching four ways so that rows a filter excluded are never
reported as rows the page withheld. Before it —
**Segment 19I Item 9**: the
Assignments strip finishes the job. `Assignment.include` — the
boolean the strip's own Inactivate / Activate buttons set — became
filterable, and the search gained the typeahead the roster pages
have had since Item 1, with the picked-label rule that makes a
suggestion return rows at all rather than none. Before it —
**Segment 19I Item 7**: the
Assignments search reads the tag columns. Name and handle already matched, and the
reviewer/reviewee segregation already existed; the six tag columns
were invisible, so a tag an operator could filter by on the roster
pages returned nothing here. The rule is Item 1's — text by
substring, tags by whole value — expressed a second time in SQL
because the count and the row cap run in the query, and pinned by
one table of cases against both paths. Before it —
**Segment 19I Item 8**: the
Assignments page, told straight. Its template gated the whole
operator-actions card on `not is_ready` while its five mutating
routes gate on `is_editable`, and the two disagreed on three of five
states in both directions — `expired` and `archived` offering live
controls the routes refuse, and `ready` hiding the search along with
them. The selection-driven half now follows the routes' own
predicate and the read-only half renders everywhere. Before it —
**Segment 19I Item 6**: the
instruments a finished session could still lose. The Instruments
page gated its structure mutations on `not is_ready`, which
protected them exactly while the session was collecting and stopped
the moment collection ended — so on `expired` and `archived` the
page offered live Delete buttons, the routes permitted, and the
cascade took submitted responses with them. Both halves now read
`is_editable`, and the lock card covers all three locked states,
each naming the way out that state has: revert for `ready` and
`expired`, the lobby's Unarchive for `archived`, which `/revert`
refuses. Before it — **Segment 19I Item 5**: the
replace an operator could not make. Uploading a replacement roster on
a session that had collected responses returned **400** telling the
operator to tick an acknowledgement that was not on the page — the
third surface with the defect Item 3 fixed on the Danger Zone, and
reached by the *documented* route, since editing a started session
means reverting it to `draft`, which is where the Upload card
reappears. The confirmation's single tick now names the responses it
discards and carries the acknowledgement with it; Observers' import,
which can destroy neither an assignment nor a response, loses the
requirement altogether. Before it — **Segment 19I Item 4**: the
roster pages' two counts moved to where they read. `Showing N of M`
left the operator-actions strip — where it sat flush right, ~950px and
a card boundary away from the rows it counts — for the top-left of the
preview table; and the selected-count pill gained its own denominator,
`N of M selected`, where M is the rendered window. The pair is one
idea: the hint's old adjacency to the delete gate was what warned that
select-all takes the window rather than the whole match, and the pill's
new denominator carries that warning in the strip now the hint has
gone. Before it — **Segment 19I Item 3**: the
delete surface told straight. The roster pages offered row selection
and a live Delete on `expired` and `archived` sessions where every
mutation returned 409, and a permanently dead Delete on `ready`; the
gate is now `is_editable`, which is what the routes already enforce, so
page and route agree by construction rather than by two lists kept in
step. Both delete confirmations now name what they destroy, modelled on
the Instruments page's sentence. And the Danger Zone's Delete-all,
which had required an acknowledgement **no template ever sent** and so
returned 400 on any session carrying a response, works. Before it —
**Segment 19I Item 2**: the
four roster Setup pages can now delete the rows an operator has
selected, which until today they could not — the checkboxes, the bulk
form and eight bulk routes all existed, and the only way to remove one
wrong imported row was to delete the whole roster and re-import it.
`Delete` sits after a shortened `Add` in the Destructive role, behind a
two-stage gate: a selection enables the confirmation checkbox, the
checkbox enables the button, and changing the selection clears the
tick, because "Yes, delete these" names the selection as it stood when
it was ticked. It **landed scaffold-first at the author's
instruction** — the layout and the gate's states as one reviewable
slice, held until the strip had been looked at on the dev slot — and
that pass paid for itself: the browser found a 4px misalignment no
test could have caught, caused by the app's global `label` margin,
which a flex row centres as a *margin box*. Before it — **Segment 19I
Item 1**: the
four roster Setup pages' search boxes now read tag columns as well as
names and emails, and Relationships stopped being the odd page out.
The motivation is a 1,000-row roster: the tags partition it into groups
the operator can zoom in on, and until now the only way to reach a
group was to know a member's name. Matching is stated **per column** —
substring on names and handles, whole value on tags, unioned — after an
input-level rule ("exact if the input equals some tag value") was
killed by a case the author supplied: searching `Ethan` would have
stopped returning every Ethan-by-name the moment any row acquired a tag
of exactly `Ethan`, a defect that depends on the data and would pass
every test. Relationships' **Search by** side-picker is retired for a
Status filter the page had needed since 15D, when it gained the status
the buttons already set. Before it — **Segment 19H Item 2**: locking
an instrument card with unsaved edits now honours its own warning. The
confirm said *"Your changes will be lost"* and then neither reverted nor
saved, copying the edited values into the locked read-only view — a
locked card displaying state the database did not have. It now discards
and reloads without `?editing`, so the card comes back locked **and**
clean. The pre-fix template was run through the same browser scenario
before being replaced, which produced the sharpest statement of the
defect and one the write-up did not have: the confirmed lock left the
card locked *and* dirty, **with Save still enabled on it**. Before it —
**19H Item 1**: the card's `Set up` / `Not set up` pill and the page's
aggregate Instruments pill both went stale after the reload-free Save,
because the predicate behind them reads exactly what the card edits and
nothing re-read it; `/save` now returns that state and the client
repaints both. Its first wiring test **asserted nothing** — the
attribute name it grepped for also appears in the repaint helper's own
selector string — which is `docs/unenforced_conventions.md` §1.6 biting
the segment after the one that wrote it. Before them — **Segment 19G**,
ten items in one day, **26 lines of shipped surface** and everything
else documentation and the tooling that checks it: `close_check`
repaired three times by its own findings, the `§N` heading-validity
check, `docs/unenforced_conventions.md` opened, and the `archived`
companion pill on `/me`. Before it — **Segment 19F PR 6**: the specs
pass, which found **three specs the plan never named**.
`spec/reviewer-surface.md` carried six stale claims about the surfaces
19F rewrote (`close_check` flagged it, since `spec_registry.py` maps
both changed modules there); a grep for the retired
`roles_held_anywhere` and the superseded `require_reviewee_in_session`
then turned up `spec/architecture.md` and `spec/rrw_functional_spec.md`.
A gate rename propagates further than the routes a manifest is written
from. Before it — **19F PR 3**: the Guide
audience gate. `/guide` now redirects a viewer who resolves no audiences
to `/about`, and the chrome stops offering them the link — **reversing**
19E rung 7, which showed such a viewer all eleven cards. A stranger
seeing more of the Guide than any role-holder does is backwards. Two
defects on `/about` were found by rendering it and fixed with the same
change: it had been showing *"Signed in as "* with an empty name (the
identical defect rung 7 fixed on `/guide`, in the sibling nobody
re-checked), and it offered a bounced stranger a Guide link straight
back to the page that bounced them. Before it — **19F PR 5**: the
observer archive short-circuit and the unlinked archived row. The
headline is that **the divergence this rung existed to close had already
been closed** — by PR 2a, as a side effect of requiring `expired` for
the release window, since an archived session is not expired. Verified
against a running app before the spec entry was rewritten. The
short-circuit landed anyway as defence in depth: without it the archive
rule is *emergent*, true only while two lifecycle predicates keep
refusing archived sessions. Before it — **19F PR 4**:
`/me/sessions/{id}/results` now answers a bare **404** unless a
visibility grant currently resolves for the signed-in reviewee — the
same 404 a stranger gets, body included. Until this rung the page
returned 200 and rendered the session name, its description and a
`Reviewee` role chip to anyone on the roster, so typing the URL told
someone they were the subject of a review before anyone had decided
they could see anything about it. The `POST .../acknowledge` companion
shares the gate. **A deliberate feature retires with it**: the
pre-release scaffolding, which showed a reviewee the *names and emails*
of the reviewers lined up to review them, with only the values hidden.
Before it — **19F PR 2a**: the
response-release window now requires the session to have **closed**
(`expired`), not just a reached anchor. The two visibility windows mean
literally *as data is coming in* and *after the review has closed* —
which was already the Workflow card's rule, gating Release and
Stop-release on `is_expired`, while the predicate checked only the
anchors. The gap let every path that writes those columns without the
button open a window the UI would never offer, and one of them mattered:
`revert_session_to_draft` leaves `responses_release_at` stamped, so a
session an operator had **withdrawn** went on showing released responses
to reviewees and observers. Before it — **19F PR 2**: a
reviewee's `/me` row now requires a **currently-resolving visibility
grant**, not merely a roster row. Until this rung the row appeared the
moment an operator uploaded the roster — disclosing that someone is the
subject of a review before anyone had decided they may see anything
about it, and continuing to disclose it on an archived session after
that decision was withdrawn. A reviewee with no current grant is now
indistinguishable from a stranger: same empty `/me`, and from PR 4 the
same 404 at `/results`. The role is gated, not the row — someone who is
also a reviewer or observer on that session keeps it. Before it —
**19F PR 1**: every
session-scoped gate answers a bare **404** on refusal, identical to
an unknown session id. Until this rung the four gates split it — 404 for
"no such session", 403 with a `detail` naming the role for "the session
exists but you are not on it" — so any signed-in account could enumerate
session ids, and count them, from status codes alone. Content never
leaked; existence did. `require_operator` (303) and `require_sys_admin`
(403) are untouched: neither takes a session id. **One exemption**: a
sys-admin who is not an owner of an *existing* session still gets a
legible 403 naming the adopt door — `/operator/sys-admin/sessions` lists
every session to them by name and links to that route, so the 404 was
concealing nothing while breaking a link the app itself renders. It sits
behind an existence check, so an absent id still answers 404.
Before it — **Segment 19E — operator
onboarding — closed and archived** (planned in #2130, rungs through
#2173, closed in #2176 — the span carries eight interleaved PRs from
other work, itemised in `guide/todo_master.md`; plan now at
`guide/archive/segment_19E_operator_onboarding.md`). A first-time operator
arrives at something: the in-app `/guide`, the lobby first-run card, both
CSV template sets, a `What this page is for` disclosure on all six Setup
pages, and — **rung 7** — Guide sections filtered to the viewer's actual
audiences, `visible_audiences()` resolving them from the operator flag and
roster rows instead of returning a constant. The close sequence found
three drift items across two of the ten doc-impact specs, all fixed rather
than waived and all the same shape: prose made untrue, hours after it was
written, by a same-day commit that changed what it described without
touching it. None could have failed a test — each describes markup the
suite already asserts directly — which is the argument for running
`spec-writer` at the close rather than at the rung. On top of the ladder,
the author's Guide rewrite: the walkthrough with twelve screencaps, behind
the app's first static mount. Before it — **19C Item 5 v1.1** (theme-customizer follow-ons,
including a stale-document merge fix that had been silently dropping
tokens). Before those, the **development-practice
audit and its follow-through** (PRs **#2085 → #2119**, 2026-09-04 → 09-05), the first arc to change how the repo is *worked on* rather than what it does: a documentation-drift test gate (`tests/unit/test_doc_conventions.py`, three checks), a fresh-context `diff-reviewer` agent (validated retrospectively — run cold at `9b9cc457` it found Codex's P0.2 25 days early), the merge policy stated in `CONTRIBUTING.md`, the portable `new_project_practices_setup.md`, a benchmark of the practice against loop engineering and the 2026 vibe-coding evidence (`docs/practice-audit-2026-09-04.md` Appendix A), `CLAUDE.md` trimmed 266 → 183 lines, duplication + churn made standard codebase-assessment items (`tools/code_metrics.py`; churn walks the full merge history so the figure is deterministic — 1.0×), the 2026-09-04 codebase assessment with the area classification pinned in `guide/assessment.json`, `rrw_sdd_in_practice.md` (the working practice read as spec-anchored SDD with a phase rule: plan on the way in, spec on the way out), and — closing **Segment 19A Part 1** four months after the sweep flagged them — `spec/permissions.md` and `spec/email_template_editor.md`, whose writing surfaced and fixed five spec-vs-code drifts. Closing **Segment 19A Item 3** the same day, the phase rule's exit — plan on the way in, spec on the way out — stopped being held by convention alone: `tests/unit/test_spec_coverage.py` fails when a routing module has no governing spec, and `tools/close_check.py` verifies at a close that a plan's `Doc impact` commitments were kept (baseline over the archive: 85 of 101 live committed paths honoured, 84%). **Segment 19A closed entirely** with Item 2 the same day: the whole-folder sweep gained a cadence (8 weeks or 500 merges), one fixed shape whose carried-forward section is read before anything new, and `--stale` to order the reading. The sweep's own eight findings were then actioned as **19C Item 7** (PRs #2116 → #2118): seven fixed, one declined with a reasoned waiver, and two re-diagnosed at build after failing re-verification. The arc closes with the 2026-09-05 codebase assessment (`guide/archive/codebase_assessment_05sep.md`, taken at `eff2661f`), whose §6 flags the one open question worth acting on: `tests/integration/test_reviewer_summary.py:146` had been skipped since 2026-05-24 pending an extract update that has since shipped. **Checked the same day (#2120): stale marker, no bug** — the extract carries the response value correctly, the skip is removed, and the assertion is strengthened to guard the `Value` cell rather than the reviewee name that would pass without it. The residue is an open question: a skip marker outlived its stated remedy by 104 days and nothing noticed. Branch protection was measured and deliberately not adopted. Before that, **Segment 19C** closed its last two items — **Item 6** (semantic colour tokens, two-tier reorg; PRs **#2047 → #2062**, 2026-08-23) and **Item 5** (theme customizer v1; PRs **#2065 → #2083**, 2026-09-04). Earlier in 19C — **Segment 19C Item 1** (friendly tag labels via roster CSV headers: reviewer / reviewee / relationships tag friendly labels now round-trip through the roster CSV headers as a `ReviewerTag1.<label>` suffix — the sole carrier; `field_labels.*` retired from the Settings CSV, silently ignored in old bundles; PR **#2010**, 2026-08-20). **Segment 19** split into **19A** (documentation hygiene — the spec/ + docs/ currency sweeps; Part 1, the two missing Tier-1 specs, shipped 2026-09-05; Parts 2–3 open), **19B** (code-consistency remediation — shipped, PRs **#1987 → #2003**), and **19C** (small operator-facing refinements — all six items shipped). Before that — **Segment 18S** (security refinements: the three-tier operator ⊂ admin ⊂ super-admin role model, in-app Accounts Management, tightened owner-management gating; PRs **#1925 → #1935**, 2026-08-17 → 08-18) and **Segment 18R** (operator-UX refinement — instrument-card save/lock harmonization, config-onto-Session-Home consolidation, chrome-strip + archive-card work). **Segment 18Q** was a no-code blob-storage deferral (2026-08). The per-segment detail lives in the segment index and Project timeline below; the recent-history paragraphs that follow still lead with **Segment 18P — patching the round-trip** (PRs **#1864 → #1878**), two groups on top of the coverage sweep (`spec/roundtrip_coverage.md`). **Group 1** harmonized export→import + clone so neither silently drops hand-set config (feature toggles, instrument view policies, reviewer / reviewee / observer roster `status`, observer cohort rules, scheduling / retention anchors, data shapes, session tags, `band1_touched_links`). **Group 2 — Rehydrate** rebuilds a live draft session from a complete set of extract CSV files: the **Rehydrate** button in the lobby search-card row → the dedicated `/operator/sessions/rehydrate` page (Validate → Rehydrate), a pure pre-flight analyzer, an operator-scoped Postgres-backed stash (`rehydrate_stashes`, the segment's one migration), a sectioned-`responses.csv` importer (`responses_import.py`, with group fan-out + its own size bound), and the `session_rehydrate.rehydrate_session` orchestrator that lands a `<name>_REHYD` draft (assignments regenerated, not activated) with all-or-nothing rollback and a `session.rehydrated` audit event. See `spec/rehydrate.md`. <!-- retired-term-ok -->

Before 18P — **Codex P0 identity case-normalization** (PRs **#1836 → #1839**): `get_or_create_user` and the per-row reviewer / reviewee / observer CRUD now use case-insensitive email comparison (`func.lower(<col>) == value.lower()`), aligning with the sys-admin invite path and the operator add-by-email lookup that already used the same pattern. The `get_or_create_user` lookup also carries an `.order_by(User.id).limit(1)` safeguard so historical case-variant duplicate rows resolve deterministically to the oldest match. Addendum archived to `guide/archive/weaknesses_and_bugs_found_by_codex.md` 2026-06-28; Slice D (storage-level lower-expression uniqueness guard) carried forward in `guide/deferred_consolidated.md`.

Post-18N (2026-05-29 → 2026-06-05) the **participant model surfaces shipped end-to-end** — reviewee `/me/sessions/{id}/results` body across all three visibility modes (Raw / Anonymized / Summarized) with an Acknowledge stamp; observer `/me/sessions/{id}/collation` MVP with per-instrument 3-row stats (reviewer headcount + aggregate, reviewee headcount + aggregate, conditional CSV download) and Anonymized download tokens (`participant_tokens.csv` keys served from the Extract data tab); per-observer **Cohort match rule** editor (multi-rule + AND/OR) that partitions the session's assignment pool into the observer's view. **Segment 18O** (2026-06-03) four-track file splits brought every production file ≤1,300 LOC with no behaviour change (`scheduled_events.py`, `assignments.py`, `routes_reviewer/_surface.py`, `session_config_io/_apply.py` all carved into per-concern packages). The **Extract data Operations tab** carved out from the Session Home card (2026-05-29 → 2026-05-30) with a Data shaper UI + per-instrument metadata cards. **Workflow card Row 3** added manual Release-responses / Stop-releasing / Archive buttons (2026-06-02). The **URL remodel** (PRs **#1668 → #1669**, 2026-05-30) renamed `/reviewer/` → `/me/` across all four router prefixes. **Segment 18N** (2026-05-28) closed the last 1,300+ LOC band for production Python and brought the settings CSV round-trip current with 18G + 18J + 18M field additions. See "Project timeline" for per-cluster summaries and `guide/todo_master.md` for the per-PR Done log.

This document is a periodic snapshot of what Review Robin Web actually
does today, vs. what is planned but not yet implemented. It is updated
at the end of each segment. Per-segment plans live in
`guide/segment_NN_*` and `guide/segment_NNA.md`. <!-- path-ref-ok -->

For the full long-term plan see
`guide/archive/low_intensity_workplan_review_robin_web.md`.

---

## Project timeline

<!-- path-ref-ok: section -->

| Date | Milestone |
|---|---|
| 2026-09-23 | **Segment 19S Item 9 closed — owners on Create, tags on Session Home** (#2567, #2569, #2570, #2571). Create gains an Owners card that mirrors Session Home's. Add owner only stages a row, and **Create session** saves everything; the owners are validated before the session exists, so a non-operator address is a 422 with nothing written. Session Home's details card gains a Tags field inside its own edit window, where an emptied box clears the tags. The settings-CSV importer now lowercases tags, as every typed surface already did. One create, and one Session Home save, is **one correlation id**. **The cold read reversed a rung-2 decision**: FastAPI hands an absent field and an empty one to a route identically, so a tab rendered before the deploy would have cleared every tag on its first Save. A `tags_present` marker now keeps an absent field from touching the tags. One `diff-reviewer` read found 3 medium and 5 low issues: seven acted on, one declined. 36 mutations were run, 32 caught first time. The owners stager and `set_owners` are built for Item 10 to reuse. |
| 2026-09-23 | **19S gains Item 10 — Session Home's Owners card saves with the card** (logged on the author's instruction, not built). Item 9 made Create session save everything, owners included, with Add owner and Remove only staging rows; Session Home's Owners card still posts each add and remove on its own, outside the details card's Save, so Cancel cannot undo an owner just added. Item 10 stages them the same way and persists them through `POST /sessions/{id}/config`, reusing the two pieces Item 9 rung 4 is built to share — the owners stager script and `session_owners.set_owners`. **What Session Home adds over Create** is recorded before it is built: Cancel is `form.reset()` and will not undo script-added rows; a button click does not mark the card dirty; an absent `owners` field would read as *remove every owner* (rung 2's FastAPI finding), so a marker field is needed; and the whole set must validate before any write, or a bad email saves the rest of the card without its owners. **One open question blocks it**: an owner error is a banner today that leaves other edits alone, and inside the card's Save it becomes either `/config`'s bare 422 page or a redirect that discards the unsaved edit. The two `/owners` routes stay — a non-owner sys-admin reaches `owners/add` to add themselves. |
| 2026-09-22 | **19S gains Item 9 — Owners on Create and Tags on Session Home, the two halves Item 6 scoped out** (logged on the author's instruction, not built). Item 6 put a Tags card on the Create page and stopped there, and both templates record what that left: Create carries **0** owner mentions, so a session is born with exactly one owner and the operator goes to Session Home to add a second, and Session Home carries **0** tag mentions, on the card that edits every other session-level attribute. **Nothing is missing underneath on either side** — `add_owner` / `remove_owner` are the whole owner write path and commit for themselves exactly as `set_tags` does, and `session.owner_added` is already emitted — so this is UI over paths that work end to end, which is what Item 6 was. **Both blockers the deferred entry preserved come with them.** Owners **cannot reuse `POST /sessions/{id}/owners/add`**: it needs a session id the Create page has not got, so rows stage in the form and apply after `create_session` returns — the ordering Tags already uses, so the page gains no third shape. It also inherits `add_owner`'s two rejections with nowhere yet to put them (`not_in_workspace`, and `already_owner`, which self-add always is since the creator is owner #1), and `workspace_operator_candidates` needs a session to exclude current owners, so Create wants a narrower query. **The Session Home half opens on a live design call, and rung 1 is blocked on it**: the details card's `config_editing` gate is `is_draft or is_validated`, so a box inside it is editable in **2 of 5** lifecycle states where the lobby edits tags in **any**, and the two surfaces then disagree about one field. Escaping the gate costs the box its ride on `form="config-save-…"` and means its own save; the recommendation recorded is to escape it, tags being classification rather than configuration, which is why the lobby never gated them. **One rule already written down is what both halves turn on**: an empty box means *write nothing* on Create — `set_tags` with an empty set is a replace that would drop what a settings CSV had just applied — and must mean *clear* on Session Home, matching the lobby's row expander. `spec/csv_contracts.md` states both meanings as of Item 6's close; Part B is the first code to depend on it. **The layout costs no CSS**, which is Item 6 rung 3 paying off: that rung replaced an ad-hoc card rule with `.bottom-left`, the documented column primitive, so a third card in the Create page's right-hand cell is spaced by the column's own `gap`. `tools/close_check.py 19S.9` **fails by design** on committed paths no rung has created yet and warns on the absent `Status` block — what a close tool run at planning time should say, as 19S.2 recorded. The entry this promotes from, `guide/deferred_consolidated.md`'s *Tags, Owners and a typeahead*, narrows to **one unbuilt change**: moving Cancel / Create session to the foot of the Create page's right column. |
| 2026-09-22 | **Segment 19S Item 6 closed — a session can be tagged when it is created, and the rule that makes it work had never been written down** (three rungs, PRs #2561 → #2562 → #2563 and the close; the segment stays open on Item 7). The Create page carried **0** tag mentions, so a session was born untagged and the operator went back to the lobby to classify it. It now has a **Tags (optional)** card whose box associates with the create form via `form="create-session-form"` — one POST, one button, no second request — and `POST /operator/sessions` calls `set_tags` **after** the staged Quick Setup uploads, of which the settings bundle is the last. **That ordering is the whole mechanism**: a typed tag beats a settings CSV's `session_tags[]` rows because it runs later, not because `_apply_session_tags` was narrowed — that applier is wipe-and-replace with no section-presence flag and is shared with `POST /sessions/{id}/import-config` on an existing session, where the wipe is 18P PR D2's pinned behavior. An empty box writes **nothing at all**, since `set_tags` with an empty set is a replace that would drop what the CSV had just applied. **The precedence rule the item leaned on existed only in `_apply_session_metadata`** — a `grep` for *precedence* or *wins* over `spec/csv_contracts.md` and `spec/settings_inventory.md` returned nothing — so the close writes it into both: fill-blanks for five identity fields, force-apply for twelve config keys, `assignment_mode` and `status` defensively ignored. **A P1 that predates the item rides here, disclosed**: `apply_session_config` flushes and never commits and `get_db` closes without committing, so all three callers of `_run_quick_setup_settings` returned a success redirect over work that was then rolled back. Rung 2 made it *conditional* — a typed tag's own commit saved the import as a side effect, so the loss showed only when the box was blank. **The suite could not have caught it and three attempts to make it say so are in the test's docstring**: reading rows back proves nothing (the fixture shares a transaction, so a flush reads like a commit), counting `after_commit` proves nothing (a savepoint release emits none, and a draft asserting on them passed with the fix removed), and counting commits across a whole create cancels at 2 against 2; the test that works spies on `Session.commit` as a **class** attribute. **Rung 3 was a no-op as scheduled** — both buttons were already canonical, and the line the plan's Blast radius called button markup is the `.btn-pair` wrapper's margin, so the `.btn` ride-along is zero pairs, not one. **Sixteen mutations, all caught but one**, which is a finding on the test rather than a pass. **The item's two cold reads both found real defects, and the one they agreed on was in prose the close had just written** — `spec/sessions_overview.md` claiming all three tag write surfaces go through `set_tags`, where the toolbar's `bulk-tags` uses `add_tag` / `remove_tag`, contradicting its own bullet three lines above. The cold read also caught the dev-slot gap fix adding an app-wide `.bottom-grid .card + .card` rule where `spec/ui_elements.md` §10 already names `.bottom-left` for two cards stacked in one cell — a rule that would have double-spaced the cards *inside* a `.bottom-left` the first time a pair returned to one; reverted, the cell is the primitive, and a test fails on the plain `<div>`. And it caught `_card()` returning the rest of the page rather than the card, so every `in card` assertion was really about the document's tail — the mutation set had degenerated that helper to `""` and never to `body`, which is `docs/unenforced_conventions.md` §1.11's own subject, missed for the second time this segment by the person who wrote it. |
| 2026-09-22 | **`docs/unenforced_conventions.md` gains §1.11 — a mutation set mutates what the assertion stands on: the helpers, and the change itself** (author's instruction). §1.8 asks that *a mutation of the protected property fails*, and 19S produced **two** slices where that was satisfied and the thing under test was still unguarded — **42 minutes apart**, `#2559` and `#2560`, by an author who had just read §1.8 and written a mutation table to satisfy it. **They fail it in different directions, so the entry prescribes two mutations and not one.** *(1) Degenerate each helper the tests stand on*: **Item 8** ran five mutations of the assertion and its recognizer, all behaving, and replacing the scan `_node_refs` with `return []` then left **all 9 tests green with a genuinely broken citation in the repo**, because the floor three of the four tests relied on had re-implemented the pattern walk instead of calling the scan. *(2) Revert the change and run the tests unmodified*: **Item 3**'s rung 2 asserted *“one statement, not one per row — the point of the change”* when the pre-refactor unit of work also emitted one executemany `UPDATE`, so **all 6 tests of both rungs passed against a full revert**. **A review caught the first draft offering the helper diagnostic as finding both**, and the difference is measurable: degenerating Item 3's `_stored_flags` fails its tests *whether or not the optimization is reverted*, so on Item 3 that diagnostic reports **guarded** and is a false reassurance — only the revert exposes it. Recorded as a correction rather than by quietly dropping the weaker half, since a register whose own procedure is wrong is worse than no entry. **Not enforced**, for §1.8's own disqualifier one level in: a check would have to establish that a mutation *was tried*, and coverage tooling does not substitute — it says a line ran, where the failure here is a line that runs and is not depended on. The revert is the one a tool could plausibly run, and also the one needing a human to say what *the change* is on a slice touching five files. What covers it is an item's definition of done, extended to name the helpers **and the revert** as targets — *“worthless if nobody writes it”*, which is §1.8's concession repeated and is said plainly rather than dressed up. |
| 2026-09-22 | **19S Item 8 lands the node-id gate, and a mutation decided the question the plan had not asked.** `tests/unit/test_doc_references.py` already failed on a backticked repo path naming nothing and on a `§N` pointer to a missing section; it did not see `` `tests/…py::test_name` `` **at all** — `PATH_REF` stops at the first character outside its class and cannot reach a node id's closing backtick, so the *file* half went unchecked too. The instance is Item 4's own: a findings register cited a test by its pre-rename name, and `guide/findings_2026-09-22_csv_contracts.md` is not matched by `DATED_DOC`, so this would have caught it. **The case is not the corpus size** — 7 live citations, all resolving — but that all **3** in `guide/archive/` are stale across two segments. Resolution is by definition name read from the file, not `pytest --collect-only`: collection is the authoritative answer and the wrong instrument, costing a subprocess per hit and turning an unrelated collection error anywhere in the suite into this check's failure. Exact only while every test is a module-level function — **0** class-based tests today, and the docstring names that as the thing to revisit. **The escape question was not in the plan and measurement answered it.** Reusing the path check's inline marker looked free and is not: `test_no_inline_path_marker_outlives_the_reference_it_covers` computes coverage from *path* references alone, so a marker over a broken node id reads as covering nothing and turns the suite red — **the remedy the new check's own failure message offered would itself have failed**. Teaching that check about node ids means editing a check the ladder scopes out, and minting a second marker with zero uses is mechanism ahead of need; so the section escape is honoured and proved, there is no inline escape, and the message says so rather than pointing at one that does not work. The same mutation caught a second defect: the floor asserted every live citation *resolves*, which contradicts the section escape — a legal escaped citation would have failed it, and a floor that contradicts its own escape is deleted the first time someone uses the escape. **Five mutations, all run**: a renamed test and a renamed test file each fail the check; a recogniser matching nothing fails the floor, the recogniser test and the archive test; a **resolver broken open** — always returning OK — fails the recogniser and archive tests; and a broken citation inside a section-escaped register stays green by design. The fourth is the keeper: the archive test's `assert stale` doubles as the guard against a resolver that never reports failure, which is how this check would most plausibly rot. **Green from its first commit**, §2's bar, so it arrives as a guard and not a cleanup. Every `Blast radius` figure re-took at the build except one — the module is **5** test functions, not the *“4 checks”* the plan counted. Two residuals stated where they can fail rather than only in prose: a node id inside a section-escaped dated register is unchecked, and so is one in a live segment plan, the latter **left open** since widening `LIVE_PROSE` changes a corpus three checks share. **0 production LOC.** |
| 2026-09-22 | **19S Item 3 closes: Prepare halves, and the item's own figures corrected its plan four times.** Two of Prepare's three full ORM materialisations of the assignment set are gone — the per-pair `db.add(Assignment(...))` becomes a bulk insert, and `recompute_self_review_classification` selects a seven-slot projection and writes changed rows as one bulk `UPDATE`. The read-only `verify_self_review_classification` is the deferred third. **Measured 26.7 s → 13.1 s**, 2.0× (Python 2.4×, queries 134 → 58), three replicates each on identical freshly-seeded 200 × 200 / 80,000-row sessions, with the eight bench sessions ending at an identical 80,000 rows. **The first correction is that it was measurable at all**: the plan said *the bench is not re-takeable here* on the evidence that `pg_isready` gets no response on 5432 — which establishes that no cluster was **running**, not that none could be **started**. `postgresql-16` is installed, so the recipe in the bench tool's own docstring gives one. **The second inverts the reason the item was widened**: the insert was argued to be *a third of the cost at best*, which is why the recompute pass was brought in; the insert delivered **9.9 s of the 13.6 s** and the recompute 3.7 s, and the whole query drop is the insert's. The widening still earned its 3.7 s. **The third is a correction that was itself wrong** — a `Semantics` bullet claiming the two Core insert forms *do not behave identically* was corrected by measurement to *they differ on `created_at`*, and they do not: `created_at` has only a `server_default`, so neither form supplies it. The first probe had been read off a bare `insert(Assignment).compile()`, which renders every column and is not the statement executemany runs. **The fourth is the identity map**: the plan reasoned *with no entity there is nothing to mutate*, true of the write but leaving an already-loaded `Assignment` stale — and the verify pass still reads entities, so it would have reported drift that was not there. The ORM-enabled bulk UPDATE by primary key writes through; a Core one would not, and with `sqlalchemy>=2.0` the only pin, that is now asserted. **The two cold reads found the item's worst defect, and it is §1.8's own precedent recurring.** Rung 2 shipped asserting *one statement, not one per row — the point of the change*, and the pre-19S unit of work **also** emitted one executemany `UPDATE`: **all six tests passed against a full revert of both rungs**. Rung 1's three were non-discriminating by design — written to pass before the insert changed — but nothing then owned the change. Both rungs now count **entity construction** instead: mapper `init` for the insert (**4 → 0** objects), ORM `load` for the recompute (**6 `Assignment` + 2 `Reviewer` → 0**, and 3 `Reviewee` for 6 rows, which tests the dedupe the projection relies on). Each fails on revert naming the expected figure. That is 19R.5's *passed having recognised nothing*, one segment on. **`group_keys` needed no variant, only an honest type**: the five attributes it reads are all it reads, so a `GroupKeyable` Protocol takes both `Assignment` and the projection through one function — documentation-grade, since the repo runs no type checker, and the docstring says so rather than claiming a guarantee. **Two specs named the moved function** and both reads caught it independently, which is why `spec/assignments.md` and `spec/rrw_functional_spec.md` were added to `Doc impact` mid-item: a spec the plan does not name is one the close does not look at. **Two findings logged rather than swept**: 16 docstring references across 11 files cite `guide/self_review_consolidate.md`, archived to `guide/archive/` and invisible to `tests/unit/test_doc_references.py`, which reads `.md` prose and not Python docstrings — **Item 8**'s class one layer down; and `spec/csv_contracts.md` §5 names `is_self_review(reviewer, reviewee)` as the `SelfReview` column's source where the extract reads the canonical column, the pair-level call having been retired for being wrong on group-scoped rows. Both predate this item, so neither rode its PR. |
| 2026-09-22 | **19S Item 5 closes: rung 2 decided *not* to build the re-run, and the anchored corpus is what decided it.** `docs/unenforced_conventions.md` **§1.10** carries the answer with its measurement, which is more than E5 had when it was logged as *"no plan, and I am not sure what one would look like"*. of the 33 in-scope rows **3** are machine-comparable, **21** carry a qualified value with no single number to compare, and **9** carry a bare value behind a *prose* command cell. **Two criteria fail independently and a review caught the first draft conflating them**: only the 21 are uncomparable by nature — *"67, of which 52 are unmentioned"*, *"8, of which 1 is button markup"* — while the 9 would become comparable under a command-cell convention at no cost to any qualifier, so **the ceiling is 12 of 33** and that ceiling, not the qualifier argument, is what the coverage objection amounts to. **The rung found a live error anyway, and the decision is still no.** Of the then-four comparable rows **1 was wrong at its own anchor**: Item 7 published *"datalists in the lobby template: 1"* against a command yielding **2**, and `sessions_list.html` is byte-identical between that anchor and `a62d40c` — a **mis-measurement, not drift** (elements counted, lines commanded), corrected in Item 7's table. A 1-in-4 hit rate, and the answer stays no on the ground it does not touch: a re-run would **execute shell out of a freely-edited markdown cell** in CI, and a plan file is not an execution surface. **That correction is also what moved the comparable count from 4 to 3** — the fixed row now carries a qualified value, so the rung's own fix changed the figure it was first published beside. **What rung 1 bought is the cheap manual check** — that error was found by checking out the anchor and re-running, which was impossible before G5. `close_check 19S.5` exits 0. |
| 2026-09-22 | **19S Item 5 rung 1: a `Blast radius` section now has to say *when* its numbers were taken, and picking the cutoff is what kept the check from being vacuous.** **G5** joins G1–G4 in `tests/unit/test_index_currency.py` (23 → **31** tests, suite 4,663 → **4,671**), with the convention in `.claude/skills/segment-plan/SKILL.md` and `guide/segment_plan_template.md`. **0** production LOC. **The cutoff is a segment comparison, on the author's ruling** — `ANCHOR_REQUIRED_FROM = (19, "S")`, sorted as *(leading number, remainder)* so `19R` < `19S` < `20`. A **date** cutoff was the alternative and is circular: deciding whether a section is in scope would need to know when it landed, and a section carries no date until this convention gives it one — `git log` per section for a question the filename answers. **The plan expected the check to cover 0 sections at the cutoff**; measured, all **7** of 19S's already carry an anchor, so 19S covers real sections from the first commit where one segment later would have been green by seeing nothing — the vacuity §1.6 concedes and §1.8 catches, in the item that cites both. **93** legacy sections excluded, **30** of them unanchored, so the exclusion is load-bearing and asserted. **Three mutations, all caught** against the real corpus: a recogniser that can never fail, a heading pattern matching nothing, and a cutoff past every plan. **The archive is scanned deliberately** — 19S will archive, and scanning only `guide/` would stop covering it the day it moved, which is G5's own failure mode applied to G5; 110 plans scanned. **A published figure is corrected**: the anchored count was *45 of 98 (46%)* and is **69 of 100 (69%)** under the definition now in code, with no reading of *"stating a sha or date"* reproducing 45 (66 / 50 / 75 / 41 for the four obvious ones) — so the gap is **31 sections, not 53**, which strengthens the decision, the convention being mostly observed already. Fourth figure correction of the day and the first surfaced by re-measuring in order to write the code rather than by a reviewer. Rung 2 unstarted. |
| 2026-09-22 | **19S gains Item 8 — a gate that a pytest node id cited in live prose resolves — and Item 5's undefined cutoff surfaces on a readiness question** (author's instruction and question). **Item 8 comes out of Item 4's defect record, not the assessment**: that item produced three citation-staleness incidents and this is the one of the three with a clean derivation, since a wrong `file:line` has none that survives an edit above it. `tests/unit/test_doc_references.py` already fails on a dangling path and a missing `§N`, and would gain one check that every `` `tests/…py::test_name` `` in `LIVE_PROSE` names a test that exists — by definition-name lookup rather than `pytest --collect-only`, which would cost a subprocess per hit and turn an unrelated collection error into this check's failure. **Measured at `9b32a9f`: 2 citations in live prose, both resolving; 3 in `guide/archive/`, all three stale** — one in 19F's plan and two in `unfinished_business.md`, which is the evidence the failure mode recurs across segments rather than being one rename. **Item 4's own instance was in live prose**, since `DATED_DOC` does not exclude `findings_`, so the gate would have caught the pre-rename citation before a reviewer read it. Two citations is a thin corpus and the item publishes the count rather than implying it, per §1.6. Residual named: `segment_*.md` is excluded, and one live citation sits in 19S's own plan. **Separately, Item 5 was found to name its cutoff six times and define it nowhere** — rung 1 cannot be built without it, because *"sections landing on or after the cutoff"* needs a way to tell when a section landed and a section carries no date until the item gives it one. Recorded as an open question with a recommendation: a **segment-number** comparison on the plan file, the shape `LEGACY_BEFORE_SEGMENT` already set, rather than a date that would need `git log` per section. Plan only; no code. |
| 2026-09-22 | **19S Item 4 closes: §3.2 rewritten against the code, all ten register rows dispositioned, nine of them spec edits.** Rung 2b gave §3.2 the real parse and save signatures, named `reviewees.email_or_identifier` and the non-email fold (`ANON-007` ≡ `anon-007`), dropped the false *(lowercase)* on `Status`, and added three paragraphs the section had never carried — *a row yields at most one issue*, *per-row detection, all-or-nothing save* (naming all three callers), and *roster status is not checked*. §5's primitives row lost its false claim that `_parse_email` runs on this importer. **No second code change fell out**: row 9 was the only one and rung 2a took it. **Row 7 is documented, not decided** — §3.2 says an `inactive` member imports *and* that whether it should is an open product question, so an unexamined default is not promoted to stated intent. **Intended two rungs; done in three, with five review passes** — Codex on rung 1 (one reclassification), `spec-writer` on the register (**two new findings, one a live bug**, plus three corrected asides), `diff-reviewer` on the cumulative diff (**nine**, including that rung 2a's fix falsified a §3.2 sentence its PR body said it did not, and that the fix's comment alleged data loss that cannot occur), a second Codex pass (**two**, both introduced by the commit before it), and `spec-writer` again on 2b's own edits, which **caught a false claim 2b had just written** (that observers skip `_parse_email` for a non-email cell — they pass `strict=True` and reject it) and two fair flags, one of them a pre-existing signature gap in the very §5 row 2b was editing, fixed here rather than left for a later sweep. **The item's recurring defect was citation staleness, three times** — four wrong line numbers in rung 1, three re-broken by rung 2a when it moved those lines, and a test node ID left at a pre-rename name. Only the third is mechanically checkable, and it is recorded as a candidate rather than built: a gate reading pytest node IDs out of live prose and asserting each collects needs no allowlist and would be green from its first commit, which is `docs/unenforced_conventions.md` §2's bar. `close_check 19S.4` exits 0. |
| 2026-09-22 | **19S Item 4 rung 2a fixes the bug the rung-1 register's checker found — a rejected row no longer consumes its pair.** `seen_pairs[(reviewer_id, reviewee_id)]` was written at the duplicate test, *above* the `Status` check, so a first row with a bad status was dropped **and kept its key**: a later row naming the same pair with a valid status was rejected as *"Duplicate pair … also on row 1"* — a misleading error on the row that was not the problem — and the pair reached `ParseResult` from neither. The write moved to just before `parsed.append`, so only a row that passes every check reserves its pair. **The test was written to fail first**: `rows=0` before, `rows=1` with a single `Status` issue on row 1 after, and `test_parse_duplicate_pair` is unchanged and passing so a genuine duplicate still errors. Suite 4,662 → **4,663**. The dead `if status_raw == ""` branch went with it — provably unreachable, since `_cell` returns `""` for a blank and the line above is `or "active"` — removed because this rung was rewriting that exact block, not as a sweep. **The rung split is itself recorded**: the plan had one rung 2, but rung 1 could not know a bug would fall out and `CLAUDE.md` forbids bundling an unrelated bug fix with other work, so the one code disposition landed alone and the nine prose dispositions follow as 2b. **The cold read then found that claim false.** `diff-reviewer`, run on the item's cumulative diff at its only build rung, established that §3.2's *"second occurrence rejected"* is now **conditionally** false — when the first occurrence fails a later check the **second** is the one kept — so the §3.2 duplicate rule is edited **in the rung that changed the behavior**, not deferred. It found eight more: the fix's comment claimed **data loss that cannot happen** (every issue is blocking and all three callers refuse the whole file, so the defect was the misleading message, not data — the same parser-versus-import conflation a reviewer had corrected once already); **three** call sites, not two, since `session_rehydrate.py:601` also parses and gates; a test assertion that indexed `issues[0]` where it meant the whole list, and so **would have passed a revert** (now stated over the list, the test renamed to the file's `test_parse_*` convention, and revert-resistance re-proved by reverting the fix in place); **three register citations gone stale in the rung that moved the lines**, the same class rung 1's commit message boasted of catching; the register still calling its rows *left standing* while this rung actioned two; and **"73 lines" / "eight divergences" stale in two live documents**, true at `8a2adea` and false after `dda459f` grew the register to **112** lines and ten rows while editing both. All nine acted on here. |
| 2026-09-22 | **19S Item 4 rung 1: `spec/csv_contracts.md` §3.2 investigated, and it is wrong in more places than it is right.** The section documents the Relationships importer; 19R had twice found its signature line stale, and the item existed because **nobody had checked the rest of it at all** — fixing the one visible line would have made the unexamined claims look verified. Output is `guide/findings_2026-09-22_csv_contracts.md` — **112 lines at the item's close, 73 when first written**: the eight questions answered with the code location that settles each, then **ten divergences** (four spec-vs-code, five spec-silent, one code-internal) and three code observations, eight of them left standing because `rrw_sdd_in_practice.md` §4 makes adjudication the author's call. **Every answer was run, not read** — a throwaway probe built a roster in memory and called `parse_relationship_csv` on twelve CSVs, one per question, which is the item's own subject applied to itself. **The finding that matters most is not the signature**: the save policy is **all-or-nothing and undocumented** — `Severity.error` is blocking and all three callers gate on `result.is_blocked` — `_setup_relationships.py`, `_quick_setup.py` and `session_rehydrate.py`, a count an earlier draft of this row put at two — so one bad row rejects the whole file; measured, a two-row CSV with one unknown reviewer returns `rows=1, blocked=True` and saves neither. **Rung 1 first filed that as a contradiction and it is not**, on a reviewer's correction the same day: §3.2's table is headed *Per-row validation* over a *Detection* column, so *"per-row error"* describes where an error is attached and the parser does exactly that — the section is silent on what the import does with the result. The register is **five spec-silent against four spec-vs-code and one code-internal**, and the corrected row forces no behavior question, since partial import would be a new product decision rather than the resolution of a divergence. Also undocumented and measured: an `inactive` roster member resolves and imports silently; `Status` is case-insensitive where the table says *lowercase*; duplicate detection runs **after** case-folded resolution and the **first** occurrence survives; a row yields **at most one** issue because every check `continue`s; and `RevieweeEmail` resolves against `email_or_identifier`, `normalize_email` lower-casing non-email identifiers too. **Three claims are right** and the register says so. **The register closed at ten rows, not eight, and the last two came from a checker**: a `spec-writer` verification pass over the finished register — a reader who did not write it, `constitution.md` III — confirmed all eight and still found a **live bug** (row 9: `seen_pairs[…]` is written *before* the `Status` check, so a first row with a bad status is dropped yet keeps its pair key, and a later valid row for the same pair is rejected as *"Duplicate pair … also on row 1"* with the pair reaching `ParseResult` from neither — measured after the report at `rows=0`) and a **false claim outside §3.2 but inside its subject** (row 10: §5's primitives table says `_parse_email` runs on the Relationships columns; `relationships.py` has **0** occurrences, so that importer does no email-format validation and a typo surfaces as *"Unknown reviewer"*). **The probe missed row 9 for a reason worth keeping**: no case combined two failure modes in one file — a coverage shape, not an oversight in any one case. The pass also corrected three asides inside rows 1–8. Earlier, **four of the register's own line citations were wrong** and were fixed before the first commit by printing every cited line. So this slice took **one correction from a bot review, two findings and three corrections from a spec pass, and four self-caught citation errors** — the checker earned its keep, and the register says which rows it owes to whom. Rung 2 follows in its own rows. Rung 1 itself: no spec, no code. |
| 2026-09-22 | **A readiness check on 19S Item 3 found the item would have shipped correct and measured almost nothing, so the author widened it.** Asked whether Item 3 was ready to build, the re-read turned up what the plan had not: the per-pair insert it targets is **one of three** full ORM materialisations of the assignment set, and the other two run immediately after it inside the same function — `recompute_self_review_classification`'s whole-session `select(Assignment, Reviewer, Reviewee)` once per instrument, then the identical select in `verify_self_review_classification`. At the 200 × 200 bench that is 80,000 objects built three times. **The plan had seen those passes only as a visibility risk** — *"must keep seeing the rows through the flush"* — and never noticed that the mechanism providing that visibility is the very cost Finding 4 attributes the 74.8 s to; worse, its `Judgment calls` pre-excused a small figure on the wrong grounds (Validate and Invite untouched). **Author's ruling: widen to the recompute pass, defer the verify pass** (read-only, so cheaper to move and safer to leave). The trace the ruling depended on ran first and found the coupling is **one attribute**: `_group_key_by_assignment` reads `assignment.reviewee`, a many-to-one lazy load that resolves from the identity map only because the same query loaded every `Reviewee` — so column tuples mean passing the reviewee explicitly, which `classify_self_review` already holds. Two more gaps closed: the recompute **writes**, so column tuples need a Core bulk `update` keyed by id rather than an entity to mutate, and `is_self_review` is omitted at the insert site on a Python-side `default=False`, so rung 1 now names the Core form that still applies it. **The existing verify pass is rung 2's oracle** — it already raises `AssertionError` on drift in a test env, so the test files that actually call `replace_assignments` gate the rewrite — **11** of the **16** that name it, the other five only importing or inspecting it. **Two of this slice's own figures were wrong and a reviewer caught both.** The test-file count was **32** and is **16 naming / 11 calling**: `grep -rln` over `tests/` had matched `__pycache__/*.pyc`, so every source file was counted twice — a figure the plan carried unchallenged from the day it was logged, re-stated as re-measured. And the projection inventory listed **4** attributes where the passes read **6**: it omitted `reviewee_id`, and more seriously `is_self_review` itself, which the recompute **compares** to decide `changed` — so a projection built to the old list could not have reproduced the count the definition of done promises. Ladder is now three rungs; `pg_isready` re-checked and still answers no response, so rung 3's disclose path stands. Plan only; no code. Item 3 is **198 lines against the ~120 budget**, compacted by 17 while the trace was recorded, and still over. |
| 2026-09-22 | **19S Item 6's last open question closed on the author's ruling — *follow the analogy of other fields where the form overrides the CSV update* — and the tidier reading of that analogy is the one a test forbids.** The precedence was already settled by audit (the form wins, on `_apply_session_metadata`'s identity-versus-config split: 5 of the 13 Create form fields fill-blanks so the form wins, 8 force-apply so the CSV does). What was open was **mechanism**, because the tag applier is wipe-and-replace. The literal per-field analogy — make `_apply_session_tags` fill-blanks, skipping a session that already has tags — reads as the tidier home for the rule and is **ruled out by evidence, not by taste**: `test_round_trip_carries_session_tags` pins the opposite behaviour in terms (*“the destination pre-seeds a stale tag that must be dropped”*, 18P PR D2), and `_run_quick_setup_settings` is shared by **three** flows, one of them `POST /sessions/{id}/import-config` against an **existing** session — so the change would have weakened a pinned contract on a shipped path to serve a create-page feature. **Ordering delivers the same ruling and touches neither**: the create handler dispatches the staged Quick Setup uploads with the settings CSV **last**, so the box's `set_tags` call goes after that block and the CSV's `session_tags[]` rows are discarded when the box is non-empty — whole-field, which is what form-wins already means for `name`. Two smaller questions fell out with it: bad input is **silently skipped** to match the lobby (`set_tags` catches `normalize_tag`'s `ValueError` per tag), and partial failure follows the handler's existing per-block error redirect. **One thing the ordering does not fix is now named out of scope**: with no section-presence flag, a settings CSV carrying no `session_tags[]` rows still clears an existing session's tags on re-import — a candidate entry of its own. Plan only; no code. Item 6 is **199 lines against the ~120 budget** — compacted by 6 while the decision was recorded, still over, and said so rather than claimed as fixed. **§1.9's own figures went stale proving its point**: the entry quoted *“~129 to ~160”* and was written two items before these two, so the range is removed rather than extended — the entry already says a tally is the drift class it concedes, and now it says why it quotes none. |
| 2026-09-22 | **19S Item 2 lands the four index-currency checks, and three of them saw less than they claimed until a mutation said so.** `tests/unit/test_index_currency.py` gates what a reader kept catching by hand: every archived plan from segment **16** on has a `### Segment <id>` entry under `guide/todo_master.md`'s `## Done` (**39 of 39**), the entries that declare a PR number run ascending by it (**0 violations of 49**), `docs/status.md`'s `As of` equals its newest timeline row, and no queued-work plan pointer names an archived plan (**3 pointers, all live**). Chosen against `docs/unenforced_conventions.md` §2's own bar — *no allowlist, green from its first commit* — so the pre-16 exclusion is one integer comparison rather than a list of exceptions, and a test asserts that filter is load-bearing. **The item adopted §1.8's evidence bar, and the bar caught the item**: the G1 mutation was inert because `-` is a non-word character, so `19R-removed` still matched `^### Segment 19R\b` — and the same hole had a *live* instance, `### Segment 12C-1` answering for a `12C` that has no heading of its own; the G3 mutation was inert twice, first inserting its row where `max` and `rows[0]` agree and then splicing at a `---` rule that occurs 18 times; and the `**Plan:**` recogniser matched this repo's prose *about* its own pointers, a false positive that was what satisfied G4's floor. **Seven mutations now, all caught.** **Two published figures were wrong and are corrected here**: the pre-16 plans unmentioned under `## Done` were reported as *34* and are **52 of 67** by the check's own criterion, and the headings declaring no PR number were *33, all predating the convention* and are **35, not all of which do**. Rung 2 is prose only and adds no checks: the twins' *"A green `ruff` is not evidence"* list names the module, `docs/unenforced_conventions.md` §1.5 records the index half as enforced beside the file-level path half it already carried, and `guide/todo_master.md`'s `## Done` maintenance note now says both invariants are checked rather than merely asked for. No `app/` change, no migration. |
| 2026-09-22 | **The settings CSV's precedence rules audited, and 19S Item 6's open question answered by precedent rather than by a decision.** The author asked which settings-CSV items overlap the Create page's edit boxes and what the current rule is. **All 13 form fields on `POST /sessions` overlap the CSV**, and the order is: parse the form → `sessions.create_session` → **then** apply the CSV — so the file always runs last and `_apply_session_metadata` decides per field by one of **two rules**. *Fill-blanks* for `name`, `code`, `description`, `deadline`, `help_contact`, where the **form wins** and the docstring names this very flow (*“on Create New Session, operator-typed fields are non-empty so the snapshot fills in only the blanks”*); *force-apply* for `display_timezone`, `scheduled_activate_at`, `responses_release_at` / `_until`, `invite_offsets`, `reminder_offsets`, `relationships_enabled`, `observers_enabled`, where the **CSV wins** because they are *“session config, not operator-typed identity”*. **5 form-wins, 8 CSV-wins**, plus a third case: `assignment_mode` and `status` are *defensively ignored on import* as machine-derived. **None of it is documented** — `grep -i "precedence\|wins"` over `spec/csv_contracts.md` and `spec/settings_inventory.md` returns nothing, so the rule lives only in `_apply_session.py`; Item 6's `Doc impact` now commits to writing it down. **The audit also found a hazard the item would have walked into**: `_apply_session_tags` is **wipe-and-replace, unconditional, with no section-presence flag**, so a bundle carrying no `session_tags[]` rows is indistinguishable from one asking for none and **deletes the session's tags** — deliberate for the round-trip, fatal for a form box applied before it. So Item 6's precedence question closes as **the form wins, by the rule already implemented** rather than a second philosophy, and what is left to rung 2 is *mechanism*: write the box's tags after the apply, or merge them into the plan before it. Its definition of done now asserts the case that would have been lost silently. **One gap traced and deliberately not built**: the route validates cross-field datetime ordering through `scheduled_events.validate_schedule_ordering`, the CSV force-applies those fields afterwards with no re-check, and no Validate rule covers ordering — so a hand-edited bundle can write a set the interactive form would reject. **The trace says every consumer already guards itself**, and the prediction that it would matter was wrong: `is_response_release_window_open` returns `False` unless the session `is_expired` **whatever the anchors say** — added at **19F PR 2a** for exactly this class, its docstring naming a *backdated anchor on Quick Setup* as one of two motivating cases; a `responses_release_until` before its anchor leaves the window permanently shut rather than early-open; scheduled activation fires only from `validated` and otherwise takes a one-shot audited skip; reminders past the deadline are skipped with an audit event. So the ordering check is an **interactive-path courtesy**, not a correctness boundary — no Item 8, and the reason rides into the `spec/csv_contracts.md` edit Item 6 already owes. |
| 2026-09-22 | **19S Item 1 closes at eight entries and no mechanism built, and Items 6 and 7 are logged — the register produced most of the segment** (author's instructions). **Item 1's own account of itself is the finding**: it was written to avoid becoming a queue, and then **four of its eight entries were promoted** into Items 2–5, so the file that refused to schedule work produced the segment's work anyway — the difference being that each promotion was the author's call against a named trigger. **No entry closed by being declared finished**: four were closed by linking or rehoming (three into `docs/unenforced_conventions.md` §1, which is what `constitution.md` VI's list is for), one retired as not-a-tripwire, three promoted. **Two entries' stated reasons turned out wrong and measurement found both** — E7's length check is a one-liner, not prose judgment, and the objection is that it would be red from its first commit; E5's `Blast radius` commands are re-runnable, **162 of 171 exactly as written**, and the missing piece is the anchor, with only **45 of 98** sections saying when they were measured. **`spec-writer` had nothing to verify at this close and that is recorded rather than performed**: Item 1's manifest names no `spec/` path, because a register of findings changes no surface contract. **Item 6** puts a half-width Tag card below User interface settings on `session_new.html`, which carries **0** tag mentions today, and **supersedes** the unbuilt *Tags, Owners and a typeahead on the Create page* — one of its three changes, **in the slot that plan gave Owners**, with Owners still deferred as the staged mini-editor it is. Re-measuring that entry's own figures corrected one: its *“8 inline-styled buttons”* is **8 lines carrying an inline style, of which 1 is button markup**, so the `.btn` ride-along is one pair. **Item 7** is the typeahead, and the cheap-looking version is wrong: the lobby already computes the vocabulary and already feeds a `datalist` for its **filter** box, so the row expander's input is one `list=` away — **but a native `datalist` completes the whole field value and that box is comma-separated**, so it would offer to replace `alpha, beta` with `gamma`. The fork — one-tag-per-input, or a per-token progressive-enhancement script — is priced in rung 1 before either is built. `docs/unenforced_conventions.md` §1.9 stops carrying a **tally** of length overruns, which was a counter needing maintenance and §1.5's own drift class; it now records the durable point, that knowing the budget while writing about it did not produce items inside it. |
| 2026-09-22 | **E5 becomes Item 5, and the measurement inverted the item it was going to be** — the register's last entry promoted on the author's ruling, which leaves **every one of Item 1's eight entries disposed of**. E5 was the entry the 22 September read logged with *“no plan, and I am not sure what one would look like”*, so the first work was **classification, not design**: against 19G Item 1's taxonomy — which sorts prose drift by *what the prose disagrees with* — four of the seven instances already have live homes (`docs/unenforced_conventions.md` §1.4's class **D**, §1.5's class **B**, two at §1.6), and the residue splits into claims that **cite their own command**, which have something to compare against, and claims about the **process** (*did the read run* — §1.7's already — *is this feasible*, *what is left*), which have nothing. **The item is the first half, and the measurement chose its shape rather than confirming it.** Over 111 live and archived plans: **98** `Blast radius` sections, **175** rows citing a command, **171** in re-runnable form and **162** runnable exactly as written — which killed the expectation that runnability was the obstacle. Only **45 of 98 sections (46%) state a sha or date to measure against**, so on the other 53 a differing count is indistinguishable from the tree legitimately moving, and re-running them would report the passage of time as drift. So Item 5 builds the **prerequisite**: a `Blast radius` section records the commit or date it was taken at, checked on sections landing from a cutoff onward — **green from its first commit because it covers nothing yet**, which is §2's bar, and scoped by one comparison rather than a list of 53 exceptions, which is the shape the author accepted for Item 2's pre-16 filter. Whether the re-run is ever worth building is **rung 2's question, asked against an anchored corpus** rather than against this item's guess, and a negative answer becomes a §1 entry carrying this measurement — more than E5 had. **The item states its own limit**: an anchor makes a later comparison possible, and on its own would have caught **none** of E5's seven instances. |
| 2026-09-22 | **19S gains Items 3 and 4, E3 retires, and E8's second half becomes a comment in the code** — four author rulings, and the register is down to **one open entry**. **Item 3 is E1 promoted**: Prepare measures **20.0 s** at the 200 × 200 bench and 5.7 s at half it, and at the old 1,000 × 1,000 bench **74.8 s of which only 17.4 s was SQL** — a minute of Python building one `Assignment()` per pair. A Core bulk insert replaces it, the precedent being the **same function's delete half**, already bulk Core since PR #1065 while the insert half stayed ORM. `bulk_save_objects` is rejected because it still constructs an object per pair, which is the measured cost. **The flush is the risk**, and rung 1 establishes it before changing anything: `recompute_self_review_classification` and `verify_self_review_classification` both **re-query** the rows, and a Core insert leaves no identity-map entries. **The bench may not be re-takeable here** — `pg_isready` answers *no response* on 5432 in the build container — so rung 2 either records the figure or discloses that it is owed from the dev slot, rather than quoting an unmeasured improvement. **Item 4 is E8's first half, and the author ruled it an investigation rather than the rename it looks like.** Reading §3.2 for the item showed why: the signature is stale (`reviewer_emails, reviewee_identifiers` against the code's `reviewers: list[Reviewer], reviewees: list[Reviewee]`) but **the prose beside it is correct** — it says the function resolves against already-loaded rosters, which is what the code does — and the four per-row rules in its table, its Save paragraph and the `ParseResult` shape (defined in `app/services/csv_imports.py`, not in `relationships.py`) have never been checked at all. Fixing the visible line would have made the section *look* freshly verified, which is worse than visibly stale. **E3 retires** on the ruling that the ≥1,000 LOC list *is not really a tripwire but just part of the judgment of codebase assessments* — which dissolves the entry's question rather than answering it, and what it gives up is recorded: §9's figures leave live prose when the snapshot is superseded, acceptable because the next assessment re-derives them from the tree. **E8's second half is a comment, not a wider gate**: `rehydrate_commit` writes roster data from a stashed set and takes a `token`, not an `UploadFile`, so 19R.4's upload gate cannot see it. The docstring now says rehydrate ships **off** and is unexercised on real data, **and why the endpoint is correct anyway** — `session_rehydrate` passes `field_labels_captured` for all three rosters — so turning the flag on would leave a live **ungated** save path rather than introduce a defect. **The cold read caught three false claims in that docstring's first draft, and the worst is E5's own failure mode**: *“dropped with a warning nobody surfaces”* was copied from the neighbouring `_require_rehydrate_enabled` docstring rather than read off the code, which counts every dropped response in the audit event and hands it back as a CSV — as `spec/rehydrate.md` §9 says twice, calling the flag *“a judgement, not an open defect”*. **That neighbour had been stale since 19N's own slices 3a/3b closed `SC-40`** and is corrected here too, because leaving the source of a defect while fixing the copy is not a fix. The other two: the docstring credited the **flag** with the endpoint's correctness, and wrote *“twelve-endpoint matrix”* — a figure the gate test **deliberately refuses to assert**, since *“a figure self-stales, and the next legitimate upload route would fail a test pinned to twelve”*. All three are recorded under **E5**, the entry they are evidence for. |
| 2026-09-22 | **Three more 19S entries close, none of them by building anything** — the author asked for the shortlist that could close cheaply, and the answer was the entries whose **conclusion already existed somewhere**. **E2** was the smallest of all and smaller than first proposed: `guide/app_responsiveness.md` opens on *“this is not a database problem”* while its re-set bench section, 45 lines down, already said *“The composition has inverted, and the document's headline with it”* with the figures — **84% of Invitations** (540 ms of 646 ms) and 83% of Responses, because 19R.3 moved the counting into SQL. The entry was **the gap between a claim and its own correction**, not a missing measurement, so the fix is a forward pointer rather than a new caveat, and the re-take against real data stays owed under E1's trigger. **E6 and E7 rehomed to `docs/unenforced_conventions.md` §1**, the register `constitution.md` VI promises, as §1.8 (a new guard names the three things that make it evidence — fixture reaches the case, mutation fails, recogniser tested outside its production examples) and §1.9 (a plan stays inside its length budget). **E7's stated reason was wrong and is corrected there**: this register had said measuring it means judging prose, but `wc -l` over the plans is a one-line check — what disqualifies it is that it would be **red from its first commit**, failing §2's own bar, and that the budget is *“a signal, not a limit to game”*, where mechanising a signal makes a limit whose cheapest satisfaction is moving the reasoning out of sight. **E6 is not a standing rule** for 19R.7's reason: a check could only enforce the *mention* of a mutation, so it lives per-item and Item 2 is the first to carry it. **The edit found one more instance in the file it was editing**: that register's own *“§2 is two entries”* had missed §2.3's arrival — the growth its own sentence calls a signal, unrecorded — fixed with the date it was wrong until. **Item 1 now stands at four closed, four open** (E1, E3, E5, E8), none of the four closes having needed a build. |
| 2026-09-22 | **19S gains its item structure: the register is Item 1, E4 closes by splitting, and its gateable half becomes Item 2** (planned, not built; author's framing and ruling the same day). **The ruling that made E4 closable** is that the **pre-16 era is legacy and out of scope**: 52 of the 67 pre-16 archived plans are not mentioned in `guide/todo_master.md`'s `## Done` at all, that era used grouped headings (`### Segment 11`, `### Segment 13`), and without the ruling “fully current” had no test — which is E4's own subject one level up. **E4 closes without archiving its finding**, the objection E3 and E8 both name: the gateable half is Item 2, and the ungateable half — whether *“eight items”* or *“the segment stays open”* is current — already sits in `docs/unenforced_conventions.md` §1.5, which records that registry mechanism as **proposed and rejected** at 19G.1 and notes the file-level half as since enforced. **Item 2 is four checks, chosen against §2's own bar** — *no allowlist, and green from its first commit* — all four measured passing at `92f7aff`: every archived plan from segment 16 on has a `## Done` heading (**39 of 39**, and the check asks for at least one because id `18R` matches two headings), those headings run ascending by declared PR (**0 violations of 49**), `docs/status.md`'s `As of` equals its newest row, and no `**Plan:**` pointer under `## Upcoming` resolves into `guide/archive/` (**3 pointers, all live**; the one archived path cited there is a stub's source in body prose and stays legal). Three of the four have an instance from this week behind them, which §2's existing entries do not. **The item adopts E6's evidence bar on itself** — a mutation per check, not a passing run — because E4's own fix produced a false verification summary. `close_check 19S.1` passes; **`19S.2` fails by design** on a committed path rung 1 has not created yet, which is what a close tool run at planning time should say. **Item 2 came in at ~160 lines against the ~120 item budget** after two trim passes, recorded in **E7**, whose subject it is. |
| 2026-09-22 | **`guide/todo_master.md`'s `## Done` gains the two segments it never recorded, and is sorted** — 19S entry **E4** actioned on the author's instruction, in its own slice rather than folded into 19R's close. **19P and 19Q had no entry at all**, both closed and archived 2026-09-18 / 19, written here from their archived plans and `docs/status.md` close rows rather than from memory: 19P's seven items (five built, two retired unbuilt — Item 4 because `spec/operations_pages.md` had already ruled out the multi-select column an expander needs, Item 7 because its question was answered *retire it* and became 19Q Item 1) and 19Q's seven, Item 5 included, whose finding is that `tests/integration/test_guide_screencaps.py` compares a light/dark pair's **files** and never its **pictures**. **The sort is the other half.** The section's own rule is *by first PR number ascending*, and the tail had been maintained newest-first for six segments: 19O, 19N, 19M, 19L, 19K, 19J ran strictly **descending**, then 19H and 19I ascending below them. Nine blocks were relocated, and because one of them — the `#2190` Guide entry, the second inversion — moved *down* past six others, **fifteen** blocks sit at a different index than before. Of those fifteen, **fourteen are byte-identical** (sha256 per block, base against head); the fifteenth is 19O, relocated **and** deliberately edited, below. No block changed content without moving, the whole `## Upcoming` section is untouched, and the `## Done` preamble **gained four lines** — the blind-spot note, also deliberate. The invariant is measured rather than eyeballed: of the 49 headings that declare a PR, **none** is out of order. **The first draft of this row got all three figures wrong** — "nine blocks moved", "every moved block byte-identical", "the preamble untouched" — because the check was run mid-edit and then summarised past its own output, which had named 19O, while the preamble note landed after it. Codex caught two of the three on review and the re-measurement caught the third. That is **E5 inside the slice fixing E4**: the prose about the work wrong where the work was right. **The rule's blind spot is stated where the rule lives** — a heading declaring no PR cannot be placed by it, many predate the convention, and the checkable claim is about the ones that do. **A third drift instance turned up a line from the second**: 19O's entry closed with *“the segment stays open”* under a heading reading `✅ closed` — fixed, and E4 now carries three instances in two indexes with its trigger moved to a fourth. Nothing was mechanised: a gate here would have to judge whether prose is *current*, which `constitution.md` VI retires rather than mechanises badly. |
| 2026-09-22 | **Segment 19R closed and archived — on its intake channel, not on its theme running out** (eight items shipped 2026-09-21, a ninth deferred unbuilt; PRs #2515 → #2540). The plan was written as an open container — *further items land the same way, as measurement or a report turns them up* — and that is what both end-of-window reads asked it to stop being: `guide/codebase_assessment_22sep.md` §8 ranks closing it **first of three moves**, on the ground that every other candidate is a refinement whose value is a guess until an institution touches the system, and `guide/codex_assessment_21sep.md` §8 reaches the same conclusion from a fresh context, adding *do not add another item merely because the file is already open*. Nothing was built to close it: `close_check.py 19R.1` … `19R.8` each exit 0, the segment has item-level manifests and so no segment-level one, and the eight `Status` blocks were compacted at their own closes and are left as written. The two notes the check prints on five of the eight — *`_operations` / `_quick_setup` touched; not in manifest* — are adjudicated as expected: they name specs a **sibling** item committed to and edited inside the shared window, which is what a per-item window cannot distinguish. **The measured candidates do not move.** Prepare's per-pair insert, the page furniture, compression and the pair sort key stay in `guide/app_responsiveness.md`, whose “*Later candidates*” is their home and whose headline — *not a database problem* — is now a 2026-09-20 fact rather than a standing one, since 19R.3 moved the counting into SQL and made Invitations 84% SQL. What the two reads surfaced with **no** home is logged instead in `guide/segment_19S_post_assessment.md`: seven entries, **none scheduled**, each with the trigger that would promote it to an item and a pointer to its evidence rather than a copy of it — a register, which is the shape that answers “*a measurement is not automatically a backlog item*” without losing the measurement. **The close's own `spec-writer` pass then added an eighth**, and it is the file's argument in one move: 19R Item 4 had recorded two divergences it deliberately did not fix — `spec/csv_contracts.md` §3.2 documenting a `parse_relationship_csv` signature the code does not have, and `_rehydrate.rehydrate_commit` sitting outside 19R.4's own upload gate — and their only record was the `Status` block archiving with this plan. The pass found both from a fresh context; the register now holds them where a reader looks, and the spec-vs-code choice stays the author's under `rrw_sdd_in_practice.md` §4, not a close's. **The close found its own first entry.** `guide/todo_master.md` still carried 19R under `## Upcoming` as an open segment with Item 5 in progress, four items behind; retiring that row showed `## Done` has **no entry for 19P or 19Q** either, both closed and archived days ago, and that the section's own sort rule — *by first PR number ascending* — has been read loosely enough that 19I (#2230) sits below 19O (#2382). Which is the 22 September assessment's §5 bullet about `docs/status.md`’s summary line drifting two items behind, found a second time in a second hand-maintained index at the very close that acts on the first: entry **E4** carries both, and this row adds 19R's `## Done` entry without fixing the two gaps beside it, because an unrelated fix does not belong in a close. |
| 2026-09-21 | **Segment 19R Item 8 closed — `compute_staleness` retired, exported and tested with no caller** (one rung, #2540; the segment stays open). A one-line predicate — pinned **and** count-versus-count — that was once the shared definition behind `InstrumentStatusBlock.is_stale` and `instruments.stale_generated`. Both moved to `staleness_by_instrument` at 19N, which **diffs the pair sets rather than comparing counts, does not gate on pinning, and does not flag a never-generated instrument** — the opposite of the helper on all three points. It stayed exported and unit-tested, and **Item 6 established it as the source the wrong descriptions kept reproducing**: of the nine wrong passages that item fixed, every count-versus-count and pinned-only framing traces to this function still sitting in the package, reading like the live definition. By then its docstring ran 18 lines explaining why nothing called it — longer than the function, its export and its tests combined. Item 6 deferred it as "a public-surface removal"; **that framing was wrong** and is corrected here: an internal package export in a monolith with no published API and no consumer. Measured before the cut, and re-derived independently by the cold read afterwards: **0 callers in `app/`** (the only hits being the definition and the import / `__all__` pair), **0 specs naming it**, no dynamic reach. ~45 lines across 3 files; the suite went 4,639 → **4,636**, the three deleted test functions exactly — which also corrected the plan's own row, `Blast radius` having said four. The descriptions Item 6 fixed stay fixed because there is no second definition left to drift back toward. |
| 2026-09-21 | **Segment 19R Item 7 closed — the `Instruction-Received` stamp was a sampling campaign written up as a standing rule, and more than half its samples never reached the reader** (one rung, #2539; the segment stays open). `CLAUDE.md` said *"The first command of a slice is `date -u +%FT%TZ`"* and `rrw_sdd_in_practice.md` §6.4 said a first commit *"now carries"* the trailer. Measured over **464** slices merged since 2026-09-04: **37 wrote the line and 16 parse**. Git reads only a commit message's *last* block as trailers, so the 21 written in a paragraph of their own above `Co-Authored-By` are **silently discarded** — present in the text, absent to every reader — and nothing checks that, nor a slice that omits the line entirely. **The item's own opening slices demonstrated it**: #2537 and #2538 both stamped and neither parsed, so the rule's author tripped its failure mode twice in one afternoon while writing about it. That corrected the plan's premise — `Opportunity` read the 16 as a campaign that ran and stopped at `#2516`; the practice did not stop, its *recording* broke. §6.4 also **instrumented the split and never wrote down what it measured**, calling it "the next thing to instrument — done the same day" and stopping there: the answer, `wait` med **3.5** / `build` med **3.0** (n=14, `--cut 2460`), existed only in tool output. It is recorded now, and it **qualifies §6.4's own claim** that the 10–14 minute turn floor is "the instruction loop and the context a slice loads, *not the build*" — build is a minority of an 8.5 minute turn median, but about a third of it, not none. The standing instruction is retired and the instrument kept: `constitution.md` VI retires what is mechanised badly, and what was mechanised badly is the rule, not `tools/pace_audit.py`, which is untouched. All four documents that tell a reader to stamp now also say the trailer goes in the **final block**. **One opportunity named and deliberately not taken**: a reader matching the line anywhere in the message would recover all 21 lost stamps at zero ongoing cost, but `Out of scope` forbids touching the tool, so §6.4 names it and leaves it to the author rather than widening a one-rung item on the strength of its own finding. |
| 2026-09-21 | **Segment 19R Item 6 closed — two documents describing the validation rule registry had stopped matching it** (four rungs, PRs #2535 → #2536; the segment stays open). Both were surfaced by the `spec-writer` pass at Item 5's close and left standing there, because `rrw_sdd_in_practice.md` §4 makes `spec/` the contract and each was a fix-the-code-or-change-it-deliberately choice rather than a close's call; the author adjudicated both before this item opened. `spec/validate_page.md` §3.2 listed `reviewees.unreachable_for_results` **22nd of 22 where `REGISTERED_RULES` has it 7th** — an append where the code inserted, at W8. The other 21 rows were in registry order exactly, which is what ruled out the alternative of declaring the table un-ordered: that would have documented a falsehood about the 21. **Registration order is a contract** — §2.4 derives within-gate source order from it and `test_validation_issue_parity.py`'s golden is keyed on it — so a reader using the table to predict issue order got it wrong. `spec/instruments.md` said `instruments.stale_generated` "raises no findings; it is inert by design", which describes `instruments.no_rule_pinned`, correctly described in the same list's last bullet; the staleness check has been **live since 19N**, and its own docstring narrates the Wave 5 PR 5.1 → 19N window when it was not — exactly the state that prose still called current. The replacement **keeps 19R Item 2's qualification**: a first draft restored the flat "cannot disagree with Generate" claim that Item 2 had deliberately made conditional on the cache stamp, caught on re-reading before commit. **The gate was deferred, then asked for**: rung 1 left out the option of deriving §3.2's key column from `REGISTERED_RULES`, because the author had asked for the two fixes and a test is not a fix; they asked for it once rung 1 had landed, so it is rung 2 of the same PR and `Out of scope` is struck rather than rewritten. Two assertions in `test_doc_conventions.py` — the table still parses, and its key column equals `[r.key for r in REGISTERED_RULES]` — with **parseability asserted separately and first**, following the visibility-grid gate in the same file, so a moved table says so rather than reporting every rule as missing, and the order assertion cannot pass by matching nothing against nothing. Four mutants, all caught, including the original W8 drift replayed and a 23rd rule registered but never documented. The findings file is retired to `guide/archive/` with both rows actioned. **Rung 3 took a third description the cold read raised as out of scope**: `_check_instruments_stale_generated`'s own docstring, wrong in both of the item's ways — it said the rule fires when an instrument has never generated, which the code's own branch and both specs deny, and it carried the flat "cannot disagree with Generate" claim 19R Item 2 made conditional — and in a third nobody had caught: it credited the rule's `why` with **three** situations where it names two, and the one the paragraph counts as already covered is not among them. Bundling an unrelated fix is forbidden; an item about descriptions of this registry that stopped matching it, leaving the one attached to the function, is not that. **Rung 4 took the two the rung-3 read left standing, and turned up a fifth**: `tests/unit/test_validation_15E_rules.py`'s module docstring on the retired count-versus-count, pinned-only basis (plus a test crediting its own silence to unpinning where the cause is nothing materialized, and a bullet still calling `instruments.no_rule_pinned` live), the docstring's "two situations" now led by the general criterion and flagged non-exhaustive — and then **`compute_staleness` itself**, whose docstring claimed the view field and the rule "share this one definition" when both take their verdict from `staleness_by_instrument`, which diffs rather than counts, ignores pinning and does not flag a never-generated instrument. That helper is the source the stale descriptions kept reproducing, and **nothing in `app/` calls it** — exported and unit-tested with no production caller. Retiring it is deferred, not decided. **Rung 4's own read then caught rung 4 writing a sixth**: the non-exhaustiveness it had just added named the session's `self_reviews_active`, which cannot move the verdict — it sets a pair's `include` flag where the diff is a difference of pair *keys*; the lever is the instrument's `group_kind`, and only while the pinned rule set excludes self-reviews. Three more of the family followed, none of them findable by grepping the rule's key, because they describe the rule without naming it. **The row that said one other site is the segment's lesson in one line**: it counted files mentioning a key, the question was passages making a claim, and it never said which — so it could be wrong without looking wrong. Measured properly it is 12 files, 19 with `app/` and `tests/`, and **nine wrong passages in six files** once the sweep went after the claim instead of the key. |
| 2026-09-21 | **Segment 19R Item 5 closed — the readiness report loaded the session once per check, and the guard that was meant to stop that coming back passed while seeing nothing** (four rungs, PRs #2531 → #2532 → #2533 and the close; the segment stays open). `validate_session_setup` is a clean orchestrator over 22 registered rules, and **each `check` loaded for itself whatever it needed**: eight of them re-SELECTed the session's instruments, the three rosters came back 5x / 4x / 4x, and two each ran the roster non-empty probe and `included_count_per_instrument` — 43 queries for 21 distinct reads. Validate then ran the whole thing **twice**, once for its issue table and once inside the Workflow card, with nothing passing the first result to the second. Rung 1 answered the plan's open question before changing anything — the second build is not a deliberate recompute, since nothing between the two mutates the session and the card's one write path is gated on a flag the Validate route never passes — and rung 2 moved every input more than one check loads into a per-run `ValidationInputs`, threaded through all 22 signatures. **The reason was consistency, not speed**: twenty-two checks each deciding independently what "the session's instruments" means is how two of them come to disagree after someone edits one. Measured on `FM100`: the report run 43 queries / 21 distinct → **17 / 14**, Validate **112 → 43**, Session Home **79 → 53**, Assignments **92 → 66**. **Parity was measured rather than asserted** — the golden in `test_validation_issue_parity.py` was captured by checking out the pre-refactor module from `origin/main` and running it against the same six fixtures, which reproduce all 29 issues byte for byte across 19 of the 22 rules. **The item's own record is the segment's lesson again, twice, and both times about a test rather than the code.** The parity golden first pinned absolute row ids, which is a SQLite fact and not a fact: Postgres sequences do not rewind on rollback, so `ci-postgres` failed four of seven cases and the golden now pins each row's **position** in its session. Then rung 3's no-duplicate guard attributed queries by splitting a filename on the repo name, which the runner's `/home/runner/work/<repo>/<repo>/` defeats — the visible half was six failures, the half that mattered was that **the main guard passed having recognised nothing**, demonstrated by the cold read running the pre-refactor module and watching it go green. It resolves the root from `app.__file__` now and asserts it saw the report at all. **Rung 3 also narrowed the definition of done rather than widening the engine**: three repeats survive, all `staleness_by_instrument` re-reading inside its own `_load_reconcile_inputs`, and handing that engine rosters loaded elsewhere is the snapshot the item spent two rungs refusing to build — it caches each verdict and flushes, and its value is that it cannot drift from what Generate does. Codex then found the exception stated too widely to enforce its own rule, since a check calling an assignments helper twice would be skipped by one test and accepted by the other; it is pinned to the `staleness_by_instrument` **call** now, which the scenario confirms 20/20 → 6/6. `spec/validate_page.md` was carried unwaived against the expectation of no change and **the build found otherwise**: §7's "Adding a new rule" recipe still documented the two-argument `check`, so a rule written to the spec would have raised `TypeError`. |
| 2026-09-21 | **Segment 19R Item 4 closed — five Quick Setup upload routes dropped the roster header's friendly labels, and the gate that would have caught it could not see itself** (three rungs, PRs #2526 → #2527 and the close; the segment stays open). A roster CSV header may carry a friendly label as a suffix — `ReviewerTag1.Tutor` means *call `tag_1` "Tutor"* — and the save functions reconcile it when handed `field_labels_captured`. The three roster cards always passed it; `_run_quick_setup_import` and `_run_quick_setup_relationships`, the only save sites behind **five** upload routes, did not. The label was parsed, carried as far as the save call and dropped there, **silently, with a 303 and a correctly populated roster**. Against the pre-fix tree the suite was 6 failed, 4,602 passed — every failure in the new test file, none anywhere else: nothing in the suite noticed. Nor could the loss be recovered elsewhere, since 19C Item 1 retired `field_labels.*` from the settings bundle *because* roster headers carry them, so the operator retyped every label and the extract → edit → re-upload round trip stopped closing. **The behavior change worth review is not "labels now register"**: it is that *upsert present, clear absent* now applies here too, so a second Quick Setup upload with a bare `ReviewerTag1` header removes a label the first one set — which the card has always done and `spec/csv_contracts.md` §5a already predicted for the Quick Setup path it was not true of. Rung 2 added a **gate asked of the router**: every POST endpoint taking an `UploadFile` (twelve) must be exercised by the matrix or carry a written reason, because the matrix enumerates the paths known when it was written and that is exactly the blind spot that let five routes break at once. **The gate's own ability to see an upload was then wrong three times, each caught by a reader**: it matched the annotation's source text (an aliased import or subclass invisible), keyed endpoints on a module basename that exists under two packages, and expanded one level of `get_args` so `list[UploadFile] | None` was missed — and each time the twelve real endpoints happened not to use the missed shape, so it stayed green while seeing less than it claimed. Demonstrated rather than argued at the third: with the one-level check and a real optional-batch route injected, the gate **passes** and the route is invisible. The recogniser is pinned directly now, seven upload shapes and five non-uploads. **The item's own record is the segment's lesson a fourth time** — two of three blast-radius rows were wrong, one of them feeding a definition-of-done line, and a first test run that looked like a second bug was my own wrong lookup key (`_VALID_SOURCE_FIELDS` keys pair-context slots `"1"`, not `"tag_1"`, and the wrong key returns a fallback that reads exactly like a dropped label). `spec/csv_contracts.md` needed no edit, which is what carrying it unwaived was for: the contract was right and the code was wrong. |
| 2026-09-21 | **Segment 19R Item 3 closed — both operations rollups count in SQL, the 1 s target is missed, and three defects shared one shape** (four rungs, PRs #2522 → #2523 → #2524 and the close; the segment stays open). `per_reviewee_coverage` and `per_reviewer_progress` loaded every `Assignment` and every `Response` in the session as ORM objects and counted them in Python — **408,027 instances for one render** at a 1,000 × 1,000 roster, with the queries never the cost: removing the 2,000-query N+1 alone took the count to 78 and left the page as slow as it was. Both are aggregate queries now. Coverage is one two-level aggregate, per assignment then per reviewee, and `uq_response_assignment_field` is what lets the inner level ask *does a satisfying row exist* rather than *is the last row satisfying*. Progress is a **hybrid split by instrument kind**: per-reviewee instruments in SQL, group-scoped ones on the existing Python dedupe, the halves added through a new `RollupParts` — a pill cannot be summed, because `not started` does not say whether the required fields were met. SQL's `TRIM` does not reproduce Python's `strip()` in the group key, so that half stays Python and the bound is stated in the code rather than discovered: an all-group session at roster scale gains nothing. Measured: Invitations **8.4 s → 1.36 s**, Responses **8.6 s → 1.52 s**, ORM instances 408,027 → ~9,000. **The definition of done asked for under 1 s and that is not met** — the rollup is 0.69 s and the shared page chrome 0.65 s, which is the 1.36 s, so neither half alone gets there and the furniture is a later item's. A parity oracle holds the old implementation in the tree — still, past the close, because the parity file parametrizes both and deleting it would delete half the cases; **eighteen mutations, all caught**, four only after the fixture grew to carry the case. **Three defects reached a reader, all with the same shape — a guard whose fixture could not reach the case it was trusted to cover**: the `visible` filter dropped from the reviewer side, which would have kept both reminder loops emailing a reviewer who had answered everything shown (cold read); a grouped fallback still prefetching the whole session, putting back the rows the aggregate half had just stopped loading (Codex P1); and a dropped `joinedload` lazy-loading one reviewee at a time (Codex P2). The ORM-row guard's own docstring said its fixture had no group-scoped instrument — that sentence was the defect, written down and not read as one. The close also **re-took the query budget in `spec/operations_pages.md` rather than editing it**: all three pages are flat in the roster now (49 / 35 / 30, unchanged from 25 × 25 to 200 × 200, against a table that ran to 434), and measuring the pre-19R commit showed two of its old rows had already drifted — so the spec now says flatness is the contract and the figures are the reading. |
| 2026-09-21 | **Segment 19R Item 2 closed — the staleness verdict is cached, and five things the item said about itself were readings** (four rungs, PRs #2518 → #2519 → #2520 and the close; the segment stays open). `staleness_by_instrument` answers *would regenerating change which pairs exist?* by running the rules engine once per instrument — on Assignments and Validate always, and on four more pages once a session is `validated`. At a 1,000 × 1,000 roster the engine's floor is **2.29 s per instrument**, and a *narrower* rule costs more, because the million-pair list is built and sorted before any rule is consulted. The verdict now persists on four `instruments.cached_reconcile_*` columns against a **content stamp of everything it is derived from** — both rosters through `rules/fields.py`'s `FIELD_MAP` rather than a copied column list, the relationships rows, the pinned rule, `group_kind`, the session's self-review setting, and a summary of the instrument's own `Assignment` rows, which is no engine input at all but is what makes a Generate invalidate rather than a cached `stale` outlive the regeneration that cleared it. Measured cold vs warm on the 200,000-row fixture: **Session Home 11.9 s → 0.67 s, Assignments 13.1 s → 0.67 s, Validate 14.0 s → 0.99 s**, the helper itself 10.95 s → 0.056 s with the same verdict. Invitations and Responses keep their 2,000-query N+1, now the dominant cost on both — that is Item 3. **The read path flushes and never commits**: a guard that would make committing safe cannot be written, since `audit.write_event` ends in a flush, so a handler that has emitted an event has work `db.new` can no longer see — which means no plain GET persists a warm and the durable warming is Generate's write-through. **The item's own record is five assertions that were readings of the code rather than facts about it**: a rule-set `seed` reaching the engine inside an `options=` block below the named arguments it was enumerated from (Codex); a reason for not committing that was backwards, since the workflow card validates *before* it promotes; an "autoflush emptied it" true only of the test session, where the app sets `autoflush=False`; a column-coverage gate claimed wider than the one model it covers; and a `count` + `max(id)` invalidator that holds on Postgres but not on SQLite, which reuses ids freed from the top. Four were caught by the per-item cold read, which found **no second gap in the stamp itself**. With Item 1's `grep -v` defeated by its own twin, that is the segment's lesson twice over: **a claim is worth what the command proving it is worth.** The close also retired `deferred_consolidated.md`'s 18J Rec E, every name in which — helper, columns, module — went in Wave 5, and re-aimed Rec C at this cache. |
| 2026-09-21 | **Segment 19R opened, and its Item 1 closed — the five places that answered *how many rows* by fetching them** (three rungs, PRs #2515 → #2516 and the close; the segment stays open, so the plan stays in `guide/`). The segment is the build half of `guide/app_responsiveness.md`, which measured the operator surfaces on a 1,000 × 1,000 roster and found **it is not a database problem**: SQL is never more than 9% of any slow page, and five of the six are slow because the rules engine runs on page load. Item 1 is the cheapest of its three recommendations and touches none of that. Five call sites asked for a count with `len(db.execute(...).all())`; at 200,000 assignment rows `csv_imports._count_assignments` alone was **1.17 s of the Setup reviewers page's 2.4 s** — a roster page that had gone 110 ms → 2.4 s while nothing about the roster changed. It is **277 ms** now, against a projected ceiling of 0.27 s. A sixth site was worse in kind: `responses.session_response_count` loads every `Assignment` as an ORM object to decide whether any instrument is group-scoped, and the quick-setup lock gate only ever asked whether the answer was `> 0` — it asks `session_lifecycle.session_has_responses` instead now, and Session Home on a 10,000-assignment draft went 237 ms → 85 ms, of which Python 177 ms → 49 ms. **The reviewers page is SQL-bound now** — 236 ms of its 277 ms — and that is the counting itself, not a missing index. **The item's own lesson repeats 19O's**: the plan's blast radius said six callers of `session_response_count` where there were three, because its `grep -v` was written against the module name `session_lifecycle` while the call sites read `lifecycle.session_response_count(`, the import alias — the name collision the plan had knowingly left alone defeated the measurement of its own twin. Three mutants survived the first test pass for a sibling reason: dropping `session_id` from a roster counter changes nothing when the fixture holds one session. **The segment stays open** — Items 2 and 3 are the other two recommendations, Item 4 is a defect in the quick-setup upload cards, which is why it is named for both halves. |
| 2026-09-20 | **Segment 19O closed — eight items, and a table rule that had only ever been measured once** (Item 8 in three rungs, PRs #2509 → #2510; plan archived to `guide/archive/segment_19O_rosters_and_instruments.md`). The author found the Sessions lobby table overflowing its card on the dev slot. Measuring all eleven operator tables in Chromium found six more: **seven push the whole document sideways** — Relationships from 1100px, the lobby, Archive, Reviewers and Reviewees from 900, Observers and Assignments at 700. **`spec/ui_elements.md` said that could not happen.** Its rule reserved `.table-scroll` for a table "wider than its card by construction" and ended "a roster that measures inside its card goes without" — measured at one viewport, and false at every width beneath it. The wrapper had been applied four times between 19H and 19I, each after someone noticed a page scrolling. **Every table carries it now**, held by `tests/unit/test_table_scroll_wrappers.py` rather than by noticing, because **nothing in a template knows a rendered width** — so the judgment that produced four reactive fixes is precisely what cannot be written down. Two carve-outs it must state out loud: a table built in JavaScript needs the wrapper on its *host*, and `.shaper-preview-table` goes without, anchored to the `overflow-x: visible` rule that earns it. Uniform was not free by inspection and was measured: table geometry across all eleven pages is identical at a viewport where none overflowed, and the twelve page/width combinations that scrolled all stopped with none newly broken. **The item's own record is four positional-anchor errors in two days** — a parser whose stripped comments shifted every line number, a test anchored on "the first `.table-scroll` in the body", a locator that took a CSS selector a thousand lines above its element, and a spec insertion that split `.guide-figure`'s contract in half. Three were caught by a reader, not a gate. With entries 15 and 16's counts-taken-from-shortcuts, that is the segment's lesson: **a claim about a location or a count is worth what the command proving it is worth.** Item 7's register closed at sixteen entries, entry 8's dev-slot list cleared by the author across eleven surfaces. |
| 2026-09-20 | **The Guide's Assignments capture re-shot — one of the four two-state pairs closed** (one PR). 19Q Item 5 landed the author's capture set with four pairs whose light and dark halves are different app states, accepted as supplied under its open question 4. Dev-slot review of the Guide caught the one that shows: `assignments-page` named the instrument *Team Peer Review* in light and *Group Peer Review* in dark, so the theme toggle appeared to rename it. The author re-shot it (`Guide_v3b.docx`, which is v3a with exactly that pair replaced — verified by hashing all 34 files, 32 identical). Both halves read `Group Peer Review` now, 1682/1683px, so the pair check holds, and the alt text names the instrument again rather than working around it. **Three two-state pairs remain** — `invitations-page` (`0/6` against `0/1`), `extract-data-page` (`SELF-REVIEW: INCLUDE` against `BOTH`) and `roster-card-locked` (an empty roster against a populated one) — none of which renames anything, and all of which `spec/ui_elements.md`'s `.guide-figure` contract now warns no test can see. |
| 2026-09-20 | **Segment 19O Item 7 entry 16 — the rename that swept the templates and missed the specs** (one PR; register at `guide/archive/segment_19O_rosters_and_instruments.md`). Entry 15 renamed the Sessions lobby's *Search card* to **Filter**, because the control hides rows already rendered and never was a search. It swept the two lobby templates and `spec/sessions_overview.md` and stopped there. 19Q.5's cold read found the leftovers by noticing the Guide's new alt text disagreeing with them. **The entry's own count was wrong twice**: three files and four lines, taken from the report that found it rather than measured — the re-grep says **five** live lines across four files, including a `sessions_list.html` comment nobody had listed, and a sixth in `docs/status_history.md` that my own search hid behind a `grep -v guide/archive/` filter. All fixed; three historical references stay, and `docs/status_history.md` takes the file-level escape, its premise being rows kept verbatim. **The durable half is a gate — covering less than a first draft claimed.** `Search card` joins the retired vocabulary in `tests/unit/test_doc_conventions.py`, whose `LIVE_DOCS` is `spec/` + `docs/`: that catches spec and status prose, not the template and test comments that were most of the fix, and widening it is a bigger change than this entry. **And the first draft matched the literal string**, so it passed over `spec/rehydrate.md`'s "the search card's" and `docs/status.md`'s "search-card" — the gate reproducing, inside an hour, the miss it was written to prevent (caught by Codex on #2507). It matches `search[-\s]card` case-insensitively now, which found three more live lines, two in `tests/`, and a module docstring. The pattern is knowingly ambiguous: the roster and operations pages put their real search in a card, so the day a spec names one correctly this fires on a true line and the answer is the line escape, not a narrower regex. Mutation-checked either way. | <!-- retired-term-ok -->
| 2026-09-19 | **Segment 19Q Item 5 closed — the author's capture set, and the pairs that were two screens** (two rungs, PRs #2491 → #2492; Item 5 closes segment 19Q, so the plan moves to `guide/archive/`). The author delivered a replacement screencap set as a captionless `.docx`, grown twice mid-item from 12 pairs to 17. **19 committed pairs → 17**: four re-supplied byte-identical, eleven replaced like-for-like, two renamed because the states changed — `roster-upload-card` and `reviewer-tag-labels` become `roster-card-locked` and `roster-card-unlocked` around the Unlock panel — and two retired, `create-session-optional-tabs` absorbed into a quick-setup capture that carries both cards, `workflow-activated` dropped because its committed shot still showed the **Create invites** button 19Q Item 2 removed. Prose followed the pictures, not the reverse (the author's rule): the Reviewers section rewritten around Unlock, **Email addresses** promoted to its own `<h3>`. **The item's real lesson is what no gate can see.** `test_guide_screencaps.py` checks that a pair's two files exist, match in scale, sit in one `<figure>` and carry one `alt` — and can say nothing about whether the two *pictures* are the same screen. Four v3a pairs are not: `assignments-page` renames its instrument between themes, `invitations-page` reads `0/6` against `0/1`, `extract-data-page` `SELF-REVIEW: INCLUDE` against `BOTH`, `roster-card-locked` an empty roster against a populated one. Found by two cold reads (Codex on rung 1, `diff-reviewer` on the item), never by a test; the author accepted the set as supplied, so the alt texts name what both halves share. **Seven alt texts were false after a swap** and every one of them passed every gate: the length check and the two-halves-agree check are both satisfied by an alt that describes the wrong picture. `spec/ui_elements.md` now says so where the `.guide-figure` contract lives. Two claims in the item's own commit bodies were wrong the same way — a narrow-family count and a rung's totals stated as the item's — both caught by measuring rather than reading, and corrected in `Status`. |
| 2026-09-19 | **Segment 19O Item 7 entry 15 — the lobby's "search box" is a filter, and there were two of it** (three rungs, PRs #2487 → #2488 → #2489 and the close; register at `guide/archive/segment_19O_rosters_and_instruments.md`). The author asked why the search box had no search button. It had none because it is **not a search box**: the Session Lobby's control hides rows already rendered, live on every keystroke, and never queries or navigates. The Archived page carried a copy. **Author's ruling: it is a filter, name it one, fold the Archive in**, plus typeahead over name, code and tag. The ruling dissolves the question rather than answering it — a live filter has nothing to submit, so the missing button was correct and the `Cancel` beside it was the odd control; it is `Clear` now, in a card headed `Filter`, on both pages. **The app already had the vocabulary backwards the other way**: the seven roster and operations boxes are server-side filters labelled *Search*, and `session_reviewers.html:771` manages both readings in one element — a `<label class="filter-search">` wrapping the copy `Search:` and a `Search` submit button. **Matching is per column now**, the rule `spec/setup_pages.md` states for those seven: substring on name and code, **whole value** on tags. **The two copies became one** — `rrwSessionFilterMatches` in `base.html`, beside the sort primitive the same two pages already share. A first pass made the copies *identical* instead, which the cold read named as how the fourth drift starts, the entry's own argument for folding the Archive in being that two copies had already drifted three ways. **Two claims measured false, both the agent's, both recorded rather than dropped.** The *cross-column false positive does not reproduce*: driving the filter in Chromium and then mutating the matching back showed the check written to prove it passing against the old code too — `textContent` carries the markup's indentation, so the haystack read `"\n                Spring Review\n               2026-A"` and nothing typable bridged that gap. Per-column matching shipped anyway, on the reasons that survive: it is the documented rule, and it stops the behaviour depending on template whitespace. And the *session cap halved a session*: names and codes were merged and sliced, so 150 sessions offered 150 names and 50 codes — the rest findable by one spelling of their identity and not the other, while the docstring claimed the opposite. The **tag** defect was real and is fixed: `team a` no longer drags in `team a2`. **The gap under all of it was that page JS had no test but `node --check`** — `tests/integration/test_session_filter_rule.py` now *executes* the rule under the node CI already runs, and writing it immediately found a hidden precondition (the function assumed a pre-lowercased term). The cold read returned **ten findings, nine acted on**; of the rest, the cap test could not tell its own versions apart (`len == 200` holds under any slicing), a tag equal to a name was offered twice, the register still called a settled design question open, and **entry 15 had no `Doc impact` bullets at all**, so the spec sweep existed only in a conversation — the failure mode the register's own preamble describes. Ten Python mutants and an eight-check browser gate, all caught, both proven non-vacuous in both directions; one mutant survived its first run because the cap test fed rows already in name order. Suite 4,457 → **4,486**. **Also audited:** `spec/operator_button_audit.md` gains a §2a for the Archived page, which shipped at 18A Part 3 and had **no section at all** — surfacing that its row expander ships a `Download` button `disabled` unconditionally, a placeholder for an export that does not exist. **Owed a dev-slot look:** the typeahead dropdown while typing, and the lobby's two adjacent `Clear` controls. |
| 2026-09-19 | **Segment 19Q Item 7 closed — one mailbox, two names: a rule with one home that only some callers could reach** (three rungs, PRs #2484 → #2485 and the close; plan stays in `guide/`, 19Q open for Item 5). The author's screenshot showed a reviewer and a reviewee sharing a mailbox under two spellings. `csv_imports.check_cross_table_identity` had the rule right and **only the CSV path could reach it** — measured on the running app, same session, same input: CSV **400** and zero rows, the Add form **303** and one row. All six create/edit services consult it now via a single-row form, the two observer CSV sites call it, and it is three-way per the author's ruling, so each roster compares against the other two. An unknown `kind` raises instead of returning `[]` — a guard that passed by not running would have reported success for an observer import it never checked. **Four Validate rules report the rows the guards cannot reach**, the ones a session already held when they started refusing: `observers.duplicate_email` (the one roster the page had nothing to report with, the DB constraint being the guarantee) and three `*.cross_roster_identity`, one per roster rather than one session-wide, because a `ValidationRule` carries a single `fix_url` and one rule would link two of every three findings to the wrong page. Both sides of a conflict are reported; a within-roster pair stays `*.duplicate_email`'s alone. Registry 18 → **22**, and `observers` became an issue source, so the Setup-coverage grid gained a row gated on `observers_enabled` — a source with no row badges nothing. **The item is a case study in one error made three times: reaching for the database to settle a question the values answer.** Rung 1's `ci-postgres` failure was a *pre-existing* assertion on an order nothing guaranteed — `clone_session` iterates an unordered relationship, green on SQLite's fresh per-test database and red on a shared one. Rung 2 then added `ORDER BY id` to `_identity_holders`, and **the mutation gate rejected it**: deleting the clause changed nothing, because SQLite hands an unordered `SELECT` back in insertion order. A clause the suite cannot see the absence of is not a fix. The second attempt picked in Python against a deliberately descending query; **Codex then found that picking at all was the bug** — a mailbox already holding two names accepted a row matching the chosen one, the rule failing open on exactly the legacy sessions the item exists for, and the pick compared a `Reviewer.id` against an `Observer.id`, which are table-local. Every holder is kept now and any disagreement is a conflict. **Three more claims were shorter than the code.** Rung 1's commit said it rewrote *"the docstring"* recording the observers exclusion; there were two, and `_quick_setup.py` kept saying observers are excluded five lines above the call that includes them. The plan's `Doc impact` named two specs and the diff governed **five** — `spec/csv_contracts.md` had the helper's parameter as `side` where it is `kind` and glossed a two-way rule; `spec/rehydrate.md` claimed rehydrate applies the check, which it has never done, since it calls `save_*` directly and reaches no route; and `close_check` itself found the fifth, `spec/quick_setup_card_spec.md`'s *"does **not** perform cross-entity validation"*. The rehydrate gap is **deferred by the author**, not closed, and registered. **A plan claim was wrong and its own test found out:** open question 2 offered "trimmed" against "exact" when every path already trims, so the live rule was exact on *trimmed* values all along — case-sensitive, never whitespace-sensitive. **Reads:** one cumulative `diff-reviewer` pass at rung 2 under the per-item cadence (ten findings, eight real, six fixed in that PR), one Codex review (one P2, real, fixed), and the close's `spec-writer`. **Mutation gate 18, all caught** — ten at rung 1, of which four survived the first run in one shape, *a guard tested on one of its two entry points*. Suite 4,417 → **4,448**. **Owed a dev-slot look:** the `Observers` issue heading and its coverage row are surfaces the page has not carried before, and the 400 copy on three Add/Edit forms and three importers wants a reading in place. |
| 2026-09-19 | **Segment 19Q Item 4 closed — a button that sat outside its own column, and a column that said too much** (three rungs, PRs #2478 → #2479 → #2480 and the close; plan stays in `guide/`, 19Q open for Item 5). Two defects in one screenshot, deliberately separated. **The button.** Three slots measured one track width and the fourth overflowed by 34px — exactly its 32px of padding plus 2px of border. The cause is an asymmetry no stylesheet declares: `<button>` inherits `box-sizing: border-box` from the UA stylesheet and `<a>` does not, this sheet has no global reset, and **Activate session** renders as an anchor on the warnings-acknowledgement detour alone. So the defect showed only in `5W`, which correlates with the right column being full of warnings — one cause, two symptoms, and an obvious explanation that is wrong. Author's contract: *"buttons should not be going beyond their card in general"* — general intent, general rule, on the base `.btn`. **The blast-radius question in the plan was asked wrong.** It asked which of the 79 `a.btn` anchors are stretched; only an *explicit* width can overflow a content box, since flex and grid account for padding and border themselves (measured identical under both models in Chromium). The sheet has two width-setting rules that reach a `.btn`, one of them a `<button>` already border-box — so the rule changes **exactly one selector's matches**, and 0 of the 79 anchors carry an inline style. **The column.** It rendered every issue across all three severities, uncapped, each with its own *Fix* deep link, so a session with several assignment warnings repeated "Assignments" down the page. The partial's own header comment already said it should be *"a compact summary, not the authoritative diagnostic surface"* — **the comment and the code disagreeing, not a design never settled**. It now carries severity counts and one link to Validate, which holds the same issues in a table with the "Why this check?" disclosure the card deliberately omits. **The item kept catching its own defect class.** A `min-width: 0` satisfied a "has a width" regex, so a guard's premise check passed with the row's `width: 100%` deleted; the obvious repair, a negative lookahead, failed too, because the optional whitespace around the colon backtracks to empty and the lookahead then reads a space instead of the zero. A page-level "clean session renders no link" control passed because that state does not *include* the partial — not because the guard inside it works, and deleting the guard outright survived. Both were caught by mutants, not by reading. **And the cold read caught the sharpest instance**: rung 2 rewrote the partial's misleading comment and created **three fresh ones one file up**, one flatly false (*"Under the W overlay this lists the warning details too"*), in a file the rung's own test opens and reads. **Counts were short three times.** Doc impact named 4 specs and the read found a 5th (`spec/rrw_functional_spec.md`, the file `spec/README.md` sends a new reader to first); the `spec/workflow_card.md` bullet said "all four" passages and the read made it five; the close found **eight**. A count nothing derives is a count that rots — the same lesson `base.html:1645`'s retired "five other rules" comment had just taught. **Also found:** `var(--font-size-sm)`, the sheet's only use of a token it has never defined, so the retired fix links rendered at inherited size all along. **Fifteen mutants across two guards, fifteen caught**, including the one the cold read predicted would survive (`content-box` reached via a non-`.btn` subject) before it was widened. Suite 4,405 → **4,417**. **UI-visible and owed a dev-slot look**, with two author decisions waiting on it: under the `W` overlay the card now names Validate in both columns, and the pointer carries no fragment, so it lands above a second copy of the card the operator just left. |
| 2026-09-19 | **Segment 19Q Item 6 closed — an instrument's identity is per-session now, and it took three surfaces to say so** (four rungs, PRs #2474 → #2475 → #2476 and the close; plan stays in `guide/`, 19Q open for Items 4 and 5). The author's screenshot showed one session holding *Instrument_1* and *Instrument_7*: the operator-facing number was `Instrument.id`, a workspace-wide autoincrement, rendered on a per-session page. So were the card tints — ids 47, 48, 51 painted tints 5, 6, 3. **`Instrument.session_seq`** is assigned once at creation and never updated; the label, the delete confirmation and the tint all read it. Not `order`: instrument drag-and-drop ships, so a position-keyed number would move under the operator's drag, which is the defect inverted rather than fixed. **The plan's own method lasted about a minute.** Wiring the value at the four creation paths left **417 tests failing**, because 75 construct `Instrument(...)` directly — editing 75 fixtures to satisfy a requirement reading *as little code change as possible* is backwards. A context-sensitive column default took over: zero creation paths edited, zero fixtures edited, one file changed, and no path *can* forget it. The clone needed no code at all (`session_clone` copies every mapped column), so that rung asserts the behaviour instead of implementing it — a behaviour nothing implements is a behaviour nothing protects. **Three defects were found by something other than the author of them.** `ci-postgres` caught three tests keyed on `instrument.id`, green on SQLite's fresh per-test id space and red on a shared one — *the item's own defect wearing a test's clothes*, and a standing lesson that any assertion keyed on a primary key is a false green in the sandbox. The cumulative cold read caught **a fifth implementation of the label, in SQL**: `_coverage.py::_instrument_label_sql` is the Assignments page's sort key and stayed on `id` when rung 2 moved the Python form, so the server sorted by a string the page no longer displayed — and with ids 9 and 47 the string collation inverts the visible order. Its docstring said a test pinned the two together; that file has never contained the word *instrument*. The measurement that missed it — *44 call sites, all unchanged* — was true and beside the point, because a SQL expression is not a call site. The same read caught a **false claim**: `max + 1` hands the number back after a *trailing* delete, while the sibling test pinned only the interior case; the docstring said the sequence never reuses. Recorded, pinned as it behaves, and **accepted** by the author the same day: a trailing slot has no successor, so recycling there disturbs no ordering the operator can see, and monotonic would cost a column on `sessions`, a migration, and undoing rung 1's cheap allocation. **Guards where there were none:** the palette had no test at all before this item (`git grep -ln "surface-tint" a3d9045b -- tests/` → nothing) and `needs_acknowledge`-style keying had no parity gate; thirteen mutants CAUGHT across the four rungs, one of them a re-aim after the first M1 turned out to be a semantic no-op that a parse gate cannot catch. **Also swept:** seven stale `Instrument_{id}` comments across five files, a dangling docstring pointer, a guard that enforced only the double-quoted half of its rule, a context dict duplicating a Jinja global the codebase already registered, and — pre-existing — `spec/instruments.md`'s claim that a new instrument is named `Instrument #N` zero-padded, where the code writes `instrument_3`. Suite 4,395 → **4,405**. **UI-visible and owed a dev-slot look:** the card title, the delete confirmation, and every instrument card's colour. |
| 2026-09-19 | **Segment 19O Item 7 entry 14 — `4W` was never a state, and nothing said so** (register at `guide/archive/segment_19O_rosters_and_instruments.md`). `spec/workflow_card.md`'s cascade tested `needs_acknowledge` **before** invitation state, so `4W` read as mutually exclusive with States 5 and 6 and its button column showed three. The template has never worked that way: it branches on invitation state to choose the body and then appends the warning help-line, and `send_invites_visible` reads invitation state alone — so `5W` and `6W` are reachable and `5W` renders **four** buttons. Author's ruling: **fix the spec; `W` is an overlay on States 4, 5 and 6.** Swept through the cascade, both tables (the `4W` column is gone and no total moves, because the overlay changes no button's visibility), the warnings detour, `spec/session_home.md`, `spec/rrw_functional_spec.md` and the template's own three comments, which taught the wrong model inside the file that disproves it. Two dated timeline rows keep the old wording: they were accurate when written, and repointing a dated log falsifies it — 19M Item 10's call, made four times over. **Nothing under `tests/` had touched any of it**: `needs_acknowledge` and `4W` appeared nowhere, which is how a documented model and a shipped one drifted for four months. `test_workflow_card_w_overlay.py` pins it by effect, each case against a no-warning control, with a premise assert so a Prepare that stopped creating the invitation cannot leave the `not in` asserts passing vacuously. Four mutants CAUGHT through a parse gate — the old cascade, the overlay hiding Send invites, the detour gated on invitations, and a fifth button added to the row, that last one after the cold read pointed out that four labels present says nothing about a fifth. **The item's own lesson is the gate, not the model.** `ci-postgres` and `test` both failed at exit code 2 on `ModuleNotFoundError: No module named 'tests'` — an absolute `from tests.integration.…` import that resolves under `python -m pytest`, which puts the cwd on `sys.path`, and not under the `pytest` console script, which CI runs and `CLAUDE.md` names. The convention against it was already written down in `tests/integration/_assignment_states.py`. Fixed, and the two helpers moved into `_invitation_states.py` so the dependency is a declared interface rather than another test module's privates. **Reads:** one `spec-writer` pass (which found the divergence, at 19Q.3's close) and one `diff-reviewer` pass on this slice — six findings, of which the sharpest was that the per-state body table still required State 4 to have **no warnings**, three rows above the new paragraph saying it could. **19O.7 is down to two open entries**: 8, the dev-slot verification, and 12b, this file's own compaction. |
| 2026-09-18 | **Segment 19Q Item 3 closed — the Guide caught up to Items 1 and 2, and so did the card it was copying from** (three rungs, PRs #2464 → #2465 → #2466; plan stays in `guide/`, 19Q open for Items 4 and 5). The Previews paragraph and its screencap pair are gone — `app/web/static/guide/` 40 files → **38** — the reviewer drill-in's two jobs are documented, the Prepare narrative rewritten, and `workflow-prepare-session` and `workflow-after-validation` recaptured, the second against a **seeded warning**: the demo data validates clean but for one info issue, so the shot would not have shown the strip the prose beside it describes. **The item's finding is an instrument, not a sentence.** Rung 1a was inserted by the author because the Workflow card's State 2 copy — the only card copy read *before* pressing Prepare — still omitted the invitations 19Q Item 2 had moved into `workflow_prepare`. Item 2's definition of done required exactly that sentence to name invitation creation; States 4 and 7 met it and this one did not, and nothing went red because the string was quoted verbatim in `spec/workflow_card.md` and asserted in **no test**. Grepped against `tests/`, States 7/3/1 are pinned by 3/2/1 files; States 2 and 5 by none. The new test pins the copy **by effect**: it names the three claims and then runs Prepare and proves the third, so a rewrite that keeps them passes and a Prepare that stops creating invitations fails — grepping the sentence would have pinned the wrong thing, since the sentence was never the problem, its silence was. **The cumulative cold read found four false claims, all in rung 2's prose, all with one cause**: the card's copy read instead of the code, while the commit message claimed the opposite. Prepare's three steps were in the wrong order and unconditional, where invitations come last and only on a clean validation; the eligibility rule omitted *included*; Activate was credited with sending; and **"Send invites notifies reviewers"**, which nothing does — no transport is wired (`app/services/email_send.py`) and `guide.html` itself says so two cards down. Rung 1 had already needed a corrective push for two **parity** claims of the same kind: an unpinned field's saved answer reaches the reviewer surface only as a label in the dropped-fields notice, and the operator's view is not what the reviewer gets — it carries `PREVIEW_INVITE_URL_PLACEHOLDER` and `(not yet submitted)`. **Reads:** one `diff-reviewer` pass under the new per-item cumulative cadence (the four above), review-bot findings on all three PRs, and the close's `spec-writer`. `tests/integration/test_guide_screencaps.py` passes at **161** cases. **The close's `spec-writer` pass found the spec truthful about everything Item 3 shipped and one thing it did not:** `spec/workflow_card.md`'s cascade tests `needs_acknowledge` before invitation state, so State 4W is modelled as exclusive with 5 and 6 and its button column shows three — while the template tests invitations first and appends the warning line independently. Confirmed at runtime: validated with generated invitations and one fresh W8 warning renders State 5's body, the warning line and **four** button slots, a combination nothing under `tests/` pins and which is also 19Q Item 4's reproduction case. It predates 19Q, so it is filed as register entry 14 rather than fixed here. **The close itself waited on the author's dev-slot inspection of the recaptured screencaps** — the one gate the sandbox cannot supply — and 19Q Item 5 now supersedes both of them with the author's own capture set. |
| 2026-09-18 | **Segment 19O Item 7 — six register entries worked in one pass** (register at `guide/archive/segment_19O_rosters_and_instruments.md`). **Five were statements that were not true.** The Workflow card's State 6 told the operator *"Reviewers have been notified that the review will open"* when the send path writes an `EmailOutbox` row and flips it `queued` → `sent` with no transport call — the sentence 19Q.3's cold read found copied into the Guide, where it was fixed while the source it came from stood. Its new test pins **the fact that makes the copy true**, asserting no transport is constructed during a real send, so wiring one fails the test rather than leaving the sentence quietly wrong in the other direction. `spec/operations_pages.md` had the invitation gate wrong in **both** directions in one paragraph — all six routes call `_require_validated_or_ready`, reminders included, and the strictness is a button convention; `regenerate-all` turns out to have no UI caller at all. `docs/status.md` annotated five of those routes `ready-only`. `spec/lifecycle.md` listed `precondition` among `session.workflow_run_failed`'s `context.step` values, which no audit row can carry — it is a `super_step` redirect value, so the spec now says which vocabulary owns it rather than deleting it and breaking the other. And `spec/workflow_card.md` still pointed at `invitations_generate`, retired with the Create invites button. **The sixth was an instrument that could not fail:** `tools/code_metrics.py --churn-only` reported a churn ratio pinned at `1.0x` by arithmetic on a shallow clone, because `git blame` cannot see past the graft and both halves of the ratio are forced to the same figure — `100.0% / 100.0%` in a six-day sandbox against `72.7% / 69.6%` over the real 2,452 merges, while the header read *"all 130 merges"*. It now exits 1 with the remedy; duplication reads the worktree and still runs. **The guard is tested against real git state**, cloning a throwaway repository at `--depth 1` — with its own case asserting the clone is actually shallow, since `git clone --depth 1 /local/path` is a hardlink copy that ignores `--depth` and would have left every other case passing while exercising nothing. **UI-visible: the State 6 copy needs a dev-slot look.** |
| 2026-09-18 | **Segment 19Q Item 2 closed — Prepare creates the invitations, and one eligibility test now governs every send** (four rungs, PRs #2457 → #2458 → #2460; plan stays in `guide/`, 19Q open for Item 3). **Create invites committed to nothing**: it minted a row, discarded the raw token and sent no email, while four code sites and three spec rows existed solely to catch an operator who prepared but did not click. It is gone; `generate_invitations` runs inside `workflow_prepare` after `mark_validated`, on the validation-clean path only, under the run's `correlation_id`. **Rung 1 was a live bug and sharper than the plan recorded**: `invitations_send_all` iterated every pending row while the Manage Invitations table above it lists assigned-and-active reviewers — so **the operator saw one row and the button emailed two people**, and the one they could not see is the one who got the mail. Reproduced through routes only before any fix. The ladder named one send path; the build found the **scheduled** dispatcher running its own copy of the same query — the unattended half — and a review found the per-row Send with a third, looser predicate. All three share one helper now, reusing the predicate `generate_invitations` already enrols on rather than adding a fourth spelling. **The root cause is recorded, not fixed**: `monitoring._assigned_active_reviewers` duplicates `invitations._assigned_active_reviewer_ids`, which is *how* the page and the button came to disagree; unifying it reaches into the monitoring layer, so it is filed in `guide/deferred_consolidated.md` with the two other carry-outs. **Open question 1's answer outlived its own measurement.** It established that a session can validate with zero eligible reviewers — which is why the `has_invitations` skip reasons and both amber captions survive the retirement — but measured it via a roster with every assignment excluded, and rung 1 then proved `replace_assignments` re-materialises those rows, so *that* state cannot survive the Prepare that creates invitations. The reachable case is every reviewer inactive, and it is now pinned by a test; nothing pinned it before. **Rung 3's copy was the item's own defect, caught by the first per-item cumulative read.** In `ready` with no invitations it told the operator to run Prepare and edit the roster; measured there, the Prepare button is not rendered, `POST /workflow/prepare` 303s to a precondition failure, and every roster mutator 409s. Both captions and both card strings are state-aware now — `validated` names Prepare, `ready` names Revert to draft and says Revert stops responses — with a test pinning the premise so the copy fails rather than rots. **Every instrument this item used proved less than claimed at least once**: an identity assertion reading a key `invitation.sent` has never carried; a mutant surviving because the test's session had nobody eligible; five tests that would have gone vacuous once `_ready_session` began creating invitations; an anti-vacuity control firing because two id sequences started advancing in lockstep; and a test that **had been skipping itself green for years**, whose docstring claimed creating an invitation "populates email_outbox" when only a *send* ever has — revived, and the suite's skip count fell 17 → 16. None was caught by the suite; all were caught by a reader. **Four reads**: one per rung under the old per-slice cadence (ten findings, then eight) plus a `spec-writer` pass, then the first per-item cumulative read at eleven — which found the only defect that spanned rungs. Suite 4,376 → **4,384**. Browser half unverified: the card with one fewer button and six rewritten copy strings want the dev slot. |
| 2026-09-18 | **Segment 19O Item 6 — the sort migration, its last standing line** (plan `guide/archive/segment_19O_rosters_and_instruments.md`). `session_reviewees`, `session_relationships` and `session_assignments` come off 19P.1's per-page capture-phase handler onto the shared `rrw:sorted` listener, so **the repo carries one mechanism for this bug rather than two**. The port was three lines per page and identical on each — every `render()` already opened with the same panel cleanup, and `panel = null` replaces `panel.remove()` because `_rrwApplySort` has already taken the node out. **The guard that permitted *either* mechanism during the migration now requires the listener and forbids the old handler beside it**: a page keeping its old handler beside the listener removes the panel twice and re-renders twice per sort. *That inverted assertion took three attempts, and the first two are the same mistake.* It matched `setTimeout(render, 0)` — which every one of these pages also uses in its select-all handler — so it failed on `session_reviewers`, migrated days earlier and carrying no workaround at all. **An assertion aimed at a string that plays two roles proves neither.** It then matched the historical handler's *exact byte-shape*, which the cold read ran against nine plausible re-introductions: **one caught, eight through** — single quotes, a braced `return`, both statements on one line, a comment between them, a renamed variable, `panel && panel.remove()`, `panel !== null`, and a guard that only calls `render()`, which is precisely the double-render the test names in its own failure message. **Both drafts were guessing how a future handler would be spelled.** The guard asserts a *property* now — these pages touch `rrw-sort-btn` in markup only, there being no legitimate script-side reason to look at a sort button on a page that listens for `rrw:sorted` — and the page census is *derived* from the templates rather than listed, pinned against a set a human last read so a seventh page announces itself instead of slipping in. **The pin earned itself on its first run**, catching the derivation pulling in `session_observers`, which names `data-rrw-sortable` only in a comment saying it has none. `sessions_list` is the one page with a legitimate script-side reference (Item 4's unsaved-edit confirm) and gets a whitelist rather than an exemption: the first thing called after its guard returns must be `expanderIsDirty`. *That test's own blacklist draft — forbidding `remove(`, `render(` and `= null` — was defeated in the mutation run by `refreshExpander()`, this page's actual panel rebuild under a third name.* **Three drafts, one lesson, and the third one is the only one that does not have to predict a spelling.** Thirteen mutants: twelve caught, one a negative control that correctly survived (a bare `expander = null` with no call removes nothing and re-renders nothing). **Two pre-existing tests re-aimed rather than deleted**, including the second `test_the_panel_survives_a_sort` — for a while this repo had two tests of that name asserting opposite architectures, which a cold read caught and this slice resolves. Suite 4,366 → **4,373**. Browser half unverified as ever: no JS runtime here, so the mechanism is asserted on the shipped source. |
| 2026-09-18 | **Segment 19O Item 6 closed — the register worked through in one pass** (plan `guide/archive/segment_19O_rosters_and_instruments.md`). Seven things found during 19P's close and 19Q Item 1, filed rather than fixed so none of them lived only in a conversation; settled together on the author's instruction. **Two touched behavior.** `app/web/spec_registry.py` pointed `_preview_surface` at `spec/preview_hub.md` alone — a file that since 19Q Item 1 explicitly hands the surface contract to `spec/reviewer-surface.md`, so the registry named a document *disclaiming* the module it governed; both modules list `reviewer-surface` first now. And **the lost hint is restored**: `/preview-surface` with an unmatched `?reviewer_email=` used to land an operator on Manage Invitations with nothing said — the Previews hub had answered that path with a "No reviewer matched" notice, and 19Q Item 1 took it as a **consequence rather than a decision**. The address now rides the redirect as `?no_match=` and the page names it. Not when it is blank: a blank email resolves to `None` only on a session with no reviewers, and reporting *"no reviewer matched ''"* would name a mistake nobody made — the existing empty-roster test pins the hint's absence there. **The dead `previews` return-to slug** is out of `_REVERT_RETURN_TO`, its docstring example and `spec/lifecycle.md`'s copy of the allowlist; it was harmless, being an allowlist rather than a router, but an allowlist that lists the unreachable teaches the wrong set. **The register named one instance of a two-instance class**: the unordered `select` indexed at `[210]` sits in `test_observers_row_landing.py` as well as the `test_reviewers_page_mutate.py` the entry named, copied along with the defect — a `ci-postgres`-only flake, the worst kind, because SQLite returns insertion order for that shape and the suite is green where it is written. `test_preview_pager.py` looked like a third and already ordered. *A register entry names the instance somebody noticed, not the class.* Plus the documentary corrections — one `Doc impact` bullet each rather than a count, since two records of this item disagreed on its size (§**9.6**'s session status card, rewritten against the shipped template — half-width in a `.card-columns` pair, an accepting-count pill row, Expand/Collapse buttons, and **no** bulk accepting or visibility control; the two that went at 18R Item 3 went differently, the accepting one never wired and the **Show all when closed** toggle removed from a page it was on; the 11F attribution restored to the `GET .../preview` row; `guide/archive/new_ux_ideas.md` no longer proposing to fold in a retired page) and two cosmetic ones. **And the register was incomplete about its own scope**: Item 4's `Status` says the remaining sort-workaround migration is Item 6's and nobody wrote the line, so it is a line now and stays open by the author's ruling — `session_reviewees`, `session_relationships` and `session_assignments` keep 19P.1's per-page handler until their own slice. Suite 4,362 → **4,366** — four tests: the hint, the whitespace address that used to produce an empty one, the hand-typed `no_match` the page now refuses to believe, and the picker label reported as the address inside it. |
| 2026-09-18 | **Segment 19O Item 4 closed — a client-side sort no longer strands the injected panel** (one slice; plan `guide/archive/segment_19O_rosters_and_instruments.md`). `_rrwApplySort` re-sorted **every child** of `tbody.rrw-rows`, and an injected `.session-expander` panel is one of them: it carries a single `colspan` cell and no `data-sort-value`, so `_rrwCellValue` returned `null` for every column and the null-last rule parked it at the bottom, detached from the row it brackets. **The quieter half was the index.** The same pass stamps `rrwOriginalIndex` once per row, which is what clearing a sort restores — so a panel present at stamping time occupied an index and shifted every row after it by one, and "unsorted" stopped restoring the server's order. The fix is a removal plus a signal: the panel is dropped **before** the rows are collected or stamped, and `rrw:sorted` fires once they land, so each of the three migrated pages rebuilds from its existing funnel. **19P.1's local workaround went in the same commit** — it existed on Reviewers alone, the other two migrated pages never having had one. **The plan's blast radius had gone stale**: measured at `820d5d6` as three injecting pages, it was **seven** by the time the item was built, six of them sortable, because 19P.2–3 and 19P.5 took the expander to four more. Reviewees, Relationships and Assignments keep their per-page handler — correct today, since it runs before the inline sort handler — and migrating them is filed as 19O Item 6. The guard now pins that an unmigrated page keeps **exactly one** mechanism and never zero, because the mistake worth failing a test is deleting a workaround without adding a listener. **The item sat open for three days looking closed**: `close_check 19O.4` passed throughout, because its manifest names `spec/ui_elements.md` and `docs/status.md` and both were edited inside the window by *other* items — C3 satisfied by other people's commits while nothing here had shipped. *A manifest of files other work also touches cannot tell a closed item from an open one.* **Two plan claims were wrong, both about where things live**: the new rule was to go "beside the `[data-rrw-sortable]` entry" in `spec/ui_elements.md`, which has no such entry, and the `sessions_overview` waiver reasoned from the panel entry while that file's **Sortable columns** bullet was the one needing the line — waiver replaced with a bullet. **The guard failed twice before it proved anything.** First on its own comment — the ordering assertion matched `rrwOriginalIndex` inside the prose explaining the fix, 400 characters ahead of the code — so it now blanks `//` comments before indexing. Then, under a cold read, on a mutant the author's own six had missed: indexing `.session-expander` pins where the panel is *selected*, not where it leaves the DOM, so splitting the two statements passed every assertion while leaving the index corruption live and re-appending the panel at the bottom. It indexes `removeChild` now. *A mutation table proves what its author thought to mutate*, for the third time this segment pair. **And the lobby's panel is the only editable expander in the app** — five `[data-expander-field]` inputs — four single-selection, one bulk — against read-only panels everywhere else — so the rebuild the plan decided on silently reverted a typed session name. The gate now runs **before** the sort, on the capture phase — which does stop the header's inline `onclick`, since `stopPropagation` halts dispatch before the target phase — so declining cancels the sort and leaves panel, edit and row order alone. Wording is Instruments' and Observers' verbatim, and dirty is asked of the DOM against a `data-seeded-value` baseline rather than mirrored in a flag that can go stale. **The first version guarded four fields and missed the fifth**: the bulk panel's tag box was seeded without a baseline, so a typed bulk tag list was still discarded silently — the defect the gate exists to prevent, one function over. One pre-existing test re-aimed rather than deleted. Suite 4,352 → **4,362**. Browser half unverified: no JS runtime in the suite, so the mechanism is asserted on the shipped source and the visible behavior needs the dev slot. |
| 2026-09-18 | **Segment 19O Item 5 closed — the two spec bullets a roster delete had owed since it shipped** (plan `guide/archive/segment_19O_rosters_and_instruments.md`). The code landed days ago: a roster replace or delete-all cascades through `relationships.reviewer_id` / `reviewee_id`, both `ondelete="CASCADE"`, and `csv_imports.py` counts the destroyed pairs into the audit event's `cascaded_relationships` slot. **Two manifest bullets never followed it**, which is why `close_check 19O.5` was failing C3 on `spec/csv_contracts.md` and `spec/architecture.md` — both last modified 2026-09-13, outside the window. `csv_contracts` § *Implementation principles* now states the cascade under **Wipe-and-replace**, including that Relationships' own replace *is* the leaf and has no further cascade; `architecture` § *Audit-event detail schema* documents the `counts` envelope's cascade slots and the rule that `cascaded_relationships` is **omitted rather than zero** when not applicable, so its absence and a `0` mean different things. **The architecture paragraph was wrong twice before it was right**, which is the item's other lesson. The first draft listed `cascaded_responses` beside the other two as though all three ride the same events; the correction then said `cascaded_responses` is the bulk delete's *alone* in a contrast that implied the bulk delete carries **only** that — where it in fact emits all four slots. Each draft sent a reader hunting a key that is not there, in opposite directions. It is a three-row table now, one row per event, because the prose form kept failing. The prose went into the existing numbered principle rather than a new one: inserting an item renumbered every principle after it. |
| 2026-09-18 | **Segment 19Q Item 1 closed — email previews moved to the reviewer drill-in and the Previews hub retired.** The Invitations per-reviewer page now carries all three rendered email tabs beside its existing **Open reviewer surface** door, so the richer reviewer table — search, tags, sorting, pagination and status — is the only picker. The old hub's typeahead, Previous / Next / Random controls, templates and Operations tab are gone; `GET /operator/sessions/{id}/previews` remains as a permanent 308 to Invitations, while its Random POST has no bookmark contract and returns 404. Operations now has five tabs. The accepted tradeoff is reach: previews no longer have a door before Prepare or for inactive / all-excluded reviewers, and Random has no replacement. Rung 2 necessarily absorbed the planned chrome rung when it removed the tab; the strip had no fixed six-column layout to repair. The segment stays open for Items 2–3, so its plan remains in `guide/`. |
| 2026-09-18 | **Segment 19P — the expander revamp — closed and archived** (seven items: five built, two retired unbuilt; PRs **#2393 → #2446**; plan now at `guide/archive/segment_19P_expander_revamp.md`). The premise was one measured defect repeated across seven pages: **a control sitting a grid away from the rows it acts on**, and the Session Lobby's row expander as the answer. Items 1-3 carried it to the four rosters — Reviewers, Observers, Reviewees and Relationships — and Item 5 to the three Operations tables, where only Assignments had selection to re-house; Invitations and Responses took the toolbar alone, their rows staying links. **Item 6** re-keyed the Invitations drill-in on the reviewer rather than the invitation, so the row opens a page whether or not an invitation exists. **Items 4 and 7 retired unbuilt**, each for a reason the segment found rather than assumed: Item 4 because `spec/operations_pages.md` had already ruled out the multi-select column its expander would need, and Item 7 because its question — *should the Previews hub become the reviewer page* — was answered **retire it**, and the work is now **19Q Item 1**, which caught what the stub had missed: the hub also hosts the **email previews**, so retiring it needs a home for them. **What the segment keeps is a lesson about verification, not about layout.** Three times a guard proved less than it claimed — an assertion matching a CSS *comment*, a stylesheet test collecting lines ending in `{` that a one-line rule does not, and a vacuity trap that ran in **both** directions at Item 6, where an assertion scoped to one block proved nothing by denying a string that block cannot contain while the unscoped version would have matched a label instead of a pill. **Two of one rung's seven `CAUGHT` mutation verdicts were false**: both mutants were Jinja syntax errors, so every render failed and the red said nothing about any assertion, while a genuine two-branch collapse left the suite green and the region that rung headlined **entirely unpinned**. The runner gained a parse gate, which immediately exposed a second false verdict and a third on re-aim, and — after a run piped through `head -3` closed stdout and **stranded a mutant on disk** — a snapshot of every target restored in `finally`. *A mutation table proves what its author thought to mutate*, so the cold read is the gate, not the table: across the segment, Item 3's three rungs alone added 9, 12 and 12 findings on top of mutation rounds that had caught everything chosen. **And prose claimed more than the code did at least six times**, the close itself included — a comment arguing from a suppression the same commit removed, a "rendered, never enumerated" claim true of one template and false of the page beside it, *"three-state"* written above a four-row table in two files, and a citation to a section and row that **do not exist**, chasing which surfaced that the Previews hub's own `Open full preview` had never been in `spec/operator_button_audit.md` at all, since 11F. Three things stay open for the author, carried to `guide/todo_master.md` (Upcoming → Stubs) rather than left in a plan this commit archives: Assignments' `Clear` renders on `{% if filter_q %}`, so a status-only filter leaves the page filtered with no way to clear it; the three Operations pages gate their whole left pane on a has-rows conditional where the four rosters let the partials self-guard; and `regenerate_token` leaves `last_reminder_at` standing, which moves the Invitations Reminder column and the reminder scheduler and so ships in its own slice. Suite **4,367** passing at the close, from 3,955 when Item 1 opened — but 19O.5's rungs land inside that window too, so the honest deltas are the per-item rows below. (19Q is in the window as well and contributed none of it: all three of its commits so far are plan documents.) |
| 2026-09-18 | **Segment 19P Item 6 closed — the Invitations row opens a reviewer page that does not wait for an invitation** (five rungs across five PRs, #2441 → #2442 → #2444 → #2446; #2443 carried an unfinished draft of rung 2a that #2444 replaced. Plan stays in `guide/`; 19P remains open for Item 7, whose question moved to 19Q Item 1). **The table's rows never depended on invitations; its links did.** `build_invitations_rows` iterates `monitoring.per_reviewer_progress`, whose set is `_assigned_active_reviewers`, and consults no `Invitation` — but the reviewer name was wrapped in `{% if row.invitation %}`, so before **Create invites** the page was a list of names going nowhere. The route is now keyed on the **reviewer** (`/invitations/reviewers/{rid}`, with the invitation-keyed URL 308ing), because the reviewer is the page's subject: the invitation supplied one optional field while the reviewer supplied the heading, the email, the breadcrumb and the row match. **The Review Progress card gained `Open reviewer surface`**, a second door to `/preview-surface/1` — the reviewer's own surface, inert, with their saved responses, which no test had covered until this item wrote one. **The Invitation card was the item's long tail.** Reported from the dev slot: a reviewer with *no invitation at all* showed `Email Status: not sent · Email Sent: — · Last reminder: —` under a chrome pill reading `Invitations: NOT CREATED`, because `email_status` derives from the **outbox** and falls back to "not sent" when there is nothing to send — a state rung 1 had just made reachable. It now reports three independent facts (created / email sent / last reminder), both dates read from the `Invitation` row rather than the outbox so they survive a reviewer leaving the table, and a **four-state** URL region: the URL, "no URL issued yet", and two different no-invitation cases — because `generate_invitations` selects active reviewers with an included assignment, and telling an ineligible reviewer's operator to press **Create invites** sends them to a button that cannot reach them (Codex, on rung 2a's PR). **Two of one rung's seven "CAUGHT" mutation verdicts were false**, and that is the item's lesson. Both mutants were Jinja syntax errors — an `{% elif %}` rewritten as a second `{% else %}`, and an unclosed `{% if %}` — so every render failed and the red said nothing about any assertion; a genuine two-branch collapse left the suite green, meaning the URL region the commit headlined was **entirely unpinned**. The runner now parses each mutant first and reports `INVALID` rather than `CAUGHT`, which immediately exposed the second one and a third when the set was re-aimed. It later **stranded a mutant** when piped through `head -3` — stdout closed, the loop died mid-iteration, and the survivor failed the full suite as though the change were broken — so it now snapshots every target up front and restores in `finally`. Twice this segment a tree-mutating runner has cost a diagnosis. **The vacuity trap ran in both directions**: an assertion scoped to `#invitation-facts` was near-useless for denying a string that block cannot contain, while `"sent" not in facts` would have failed on the label `Email sent:`. **Five open questions answered by the author**, two of which created work in rung 2b: the dropped-fields notice is no longer suppressed in `preview_mode` (that surface now inspects real reviewers, where a dropped field is the point), and the card carries delivery state with `failed` among its values, **rendered rather than enumerated** so widening `EMAIL_OUTBOX_STATUSES` needs no second edit. `regenerate_token` clearing `last_reminder_at` was answered too but ships elsewhere — it moves the Invitations Reminder column and the reminder scheduler, neither of which this item owns. Suite 4,350 → **4,364**. |
| 2026-09-17 | **Segment 19P Item 5 closed — the three Operations tables take the roster toolbar** (four rungs across five PRs, #2434 → #2437 plus this close, #2434 being the plan itself; plan stays in `guide/`, 19P is open for Item 4 and the two held Invitations row links). **All seven table pages now open their card with the same two-pane toolbar**: column chips, pager and preview-count line left, the filter strip right. Assignments took both halves of the idiom — the toolbar at rung 1 and an **injected row expander** at rung 2 — because it is the one Operations page with selection; Invitations and Responses took the toolbar alone at rung 3, their rows staying links. `Apply` became **`Search`**: five surfaces already said Search and these two said Apply, so the minority renamed. **Three CSS scopes retired in three rungs** — `.grid-right` and `.operator-actions-card` with Assignments' corner card, `.filter-card` with the two half-width filter cards — and each retirement was a card the segment had been arguing against since 19P.1: a control a grid away from the rows it acts on. **The plan's blast radius was wrong about the last of them, and the error survived two restatements.** It recorded `filter-card` as *not retired* because `session_validate.html` uses it; that page's token is `severity-filter-card`, and a substring `grep -rln` counted it as a fourth caller. Scanning `class="…"` by token gives two, both in scope. **The item's lesson is the guard that failed its own mutation.** Nothing pinned the newly-dead rule *staying* dead, so a test was written for exactly that — and it collected lines ending in `{`, which a one-line rule does not, and survived `.filter-card form { margin: 0; }` coming back. That is the same defect the rung-2 cold read had already flagged in `test_roster_expander.py`'s twin, **copied across without noticing it had a second half**; both now read the stylesheet brace to brace through one parser. Item 3's lesson — *a mutation table proves what its author thought to mutate* — recurred one level up: a guard written in response to a cold read still needs its own. **Three real defects and one phantom control.** Rung 1 found the toolbar's `<div>` opening above `{% if not pair_sample %}` and closing inside its `else`, so a search matching nothing emitted an **unclosed `<div>`**. Rung 2 shipped without the capture-phase sort guard the idiom needs — `_rrwApplySort` slices `tbody.children` with the injected panel among them, measured at row 30 of 31 — and its port assumed a selectable row is a visible one, which Assignments breaks with the only *client-side* filter in the app: the panel anchored after a hidden row and its `colSpan` went stale on a chip toggle. And §9.7 of the functional spec listed a **`Show-pair-row` control that exists nowhere** in the templates or `spec/assignments.md`; dropped rather than carried. **The table card had to leave `{% if rows %}`** on Invitations and Responses: fine while the filter was a card away, a trap the moment it moved in, since a search matching nothing would take away the only way to clear it. Assignments already rendered its card in every state, and what rung 1 had to fix there was the toolbar `<div>` above. Two things stay **open for the author**: Assignments' `Clear` renders on `{% if filter_q %}`, so a status-only filter leaves the page filtered with no way to clear it; and the three Operations pages gate the whole left pane on a has-rows conditional where the four rosters let the partials self-guard. Suite 4,323 → **4,350**. |
| 2026-09-17 | **Segment 19P Item 3 closed — Reviewees and Relationships, and one shape at last** (five PRs, #2424 → #2428 plus the close; plan stays in `guide/`, 19P is open for Item 4). Both pages now render full-width guidance, a **roster card** whose `Unlock` panel holds the tag-label editor, the `Danger Zone` and the CSV upload card, and a preview table carrying the filter strip in its own two-pane toolbar and the selection actions in an **injected row expander**. With that, **all four roster Setup pages carry one shape**, and the spec says it once: `spec/setup_pages.md` gained § *The roster card and the Unlock panel* with a three-row table of the only axes any page differs on — all three Observers'. **That hoist diverges from what Doc impact committed to, deliberately.** The bullet asked for each page stated *outright* rather than by reference to Reviewers, which was right when a pointer led to a section describing a **different** shape — the failure mode 19P.1 catalogued eight times. With all four identical it inverts, and four ~90-line copies is the drift the item existed to remove. **Two real defects, both found by a cold read rather than by a mutation table.** `relationship_column_state` counted two **non-nullable integer foreign keys** through `slot_row_count`, whose predicate is TEXT (`column != ''`): Postgres refuses `integer <> character varying` and **SQLite compares across types without a word**, so the whole suite passed locally and `ci-postgres` went red — the dialect gap `CLAUDE.md` warns about, met in a query rather than a migration. Reproduced on Postgres 16 in the sandbox: 36 instances of the error before, 0 after. And `_handle_import` still hardcoded `col_readouts = []` for Reviewees from when the page had no index row, so a **failed import** — the moment an operator looks hardest at which columns arrived — answered *"Populated columns: none yet"* over a roster it was displaying; mutating either branch to `[]` is caught now, where before **neither** was. **The item's lesson is one finding that recurred at every rung: a guard that proves less than it says.** Mutation survivors ran 3/10, 12/25 and 15/24 after first rounds that caught everything chosen, and the cold reads found 9, 12 and 12 more on top. Rung 4 shipped with **no guard file at all**, leaning on two pre-existing tests that happened to fail, and almost every survivor was plain rendered markup a server-side test could read. **A mutation table proves what its author thought to mutate**, so the cold read is the gate, not the table. **The close swept the specs and fixed what the sweep exposed.** `grep -rnE "19P\.4|19P\.[0-9]–\.[0-9]" spec/` → 0, and the definition-of-done grep itself was **re-aimed rather than quietly passed**: it could not tell *"defers to 19P.3"* from *"happened at 19P.3"*. `spec/ui_elements.md` §6 claimed every destructive confirm is also `required` — **false of all four pages** and forced: the expander's confirm shares `<noun>-bulk-form` with `Inactivate` and `Activate`, so `required` would refuse those two submits until an operator ticked a *delete* confirmation. Two specs the plan never named (`visual_style_rrw.md`, `color_tokens.md`) were falsified and added to Doc impact rather than fixed silently. `reviewer_column_state` gained the `Profile` readout it had lacked since 19P.1 — which exposed that `session_reviewers.html` decided its Profile column from the **rendered window** where Reviewees reads a whole-roster flag, the one surface 19I Item 12 rung 2 missed because that sweep followed the chips and this column has none. Suite 4,151 → **4,272**. |
| 2026-09-16 | **Segment 19P Item 2 closed — Observers, the divergence proof** (seven PRs, #2415 → #2421; plan stays in `guide/`, 19P is open). Item 1's idiom was agreed on the page that fit it; this item took it to the one roster page that does **not** — an extra row action, a per-observer **cohort match rule** builder with no equivalent anywhere, no `_field_labels_editor`, no column chips, and a page that 404s unless `observers_enabled`. It survived: same toolbar, same row expander, same Unlock panel, with the cohort editor in the expander's **left pane** (two columns, `is-split`) beside the row actions, because it is selection-driven and a card a grid away from the rows it acts on was the defect. **The real finding is a behavior change, not a layout one.** Observers only view — they never appear in assignments, produce no responses and no readiness rule references them — so freezing their roster at Activate protects nothing. Author's ruling: the whole surface takes `not is_archived`, and **eight** routes now read that gate (seven relaxed here; `cohort-rule` already did). That makes Observers **the first roster page whose mutating surface outlives `is_editable`**, now a stated exception in `spec/lifecycle.md` §5 with a rule governing any second one — §3.1's *nothing may use a narrower predicate* gains its mirror. **The ladder was wrong about scope three times, each measured rather than guessed:** six POSTs not five; **seven routes not five** (`grep -c _require_editable` → 7 — `bulk-delete` an enumeration slip, `import` *structurally forced*, since one `{% if %}` gates it with `delete-all` and a rendered control the server 409s is the silent failure the rung existed to remove); and the shared **lock card**, which unchanged would have read *"cannot be modified while the session is ongoing"* above a live roster, solved with a `lock_when` parameter leaving the other three pages byte-identical. A **rung 5a** was inserted on the author's ruling after rung 5 made an unsaved cohort edit reachable and discarded it silently — verified against the column, which stayed `null` on every loss path — taking Instruments' wording verbatim so an operator meets one sentence on two pages. **One control bit twice for one reason**: `Save` rides *inside* the last rule cell, so `X` on that cell destroys and recreates it — killing the `sync` rebind at rung 5 and the nav flag at 5a, where pressing `Save` then raised the browser's *Leave site?* and its Cancel silently ate the save. Anything bound to `Save` is bound where `Save` is built; the spec says so now. **Three structural gaps in the repo's own verification, each found by shipping into it.** A stray `}` killed this page's entire script and **all 4,106 tests passed** — Python never parsed the app's JS; closed by `test_inline_scripts_parse.py` (`node --check` over 13 pages), proven by breaking `base.html`'s. An assertion matched the *comment* explaining a class rather than the class. And **nothing pinned a card's position**: all 4,131 existing tests passed with rung 6's two cards relocated, their container deleted and both redirects rewritten. **Author's corrections improved the design twice** — `full_width=true` on the guidance card, and the roster **index** the first draft had reasoned its way out of, which then settled the gating (card always, panel conditionally) and forced `views.observer_column_state`, the second caller `reviewer_column_state`'s docstring had been explicitly waiting for. The rule it settles: **the index mirrors the columns the preview table renders** — identity plus populated tags on Reviewers, where unpopulated tag columns hide; all three on Observers, where none do. `Tag1` displays as `Tag` in all four display sites; `ObserverTag1` and the rule key `observer.tag1` are identifiers and did not move. **Six cold reads, 63 findings, all upheld**, four of them live defects the rungs shipped — including the `.confirm-label` swap that reintroduced the exact pill-gap defect `spec/ui_elements.md` §6 exists to prevent, measured at **12px** against 4px, which that spec calls *"invisible in the markup"* and was. The close added `operator_button_audit.md` **§8.5**: the page had no section at all, a gap rather than a scoping choice in a file that audits every button on the surface. Two counts of the author's own were wrong and are corrected: seven routes where eight read the gate, and *"three cases"* reaching the fragment-fallback script where two do. Suite 4,049 → **4,151**. |
| 2026-09-16 | **Segment 19P Item 1 closed — the Reviewers Setup page moves to the lobby's expander idiom** (21 PRs, #2393 → #2414; plan stays in `guide/`, 19P is open). The page now renders full-width guidance, a **roster card** whose `Unlock` panel holds the tag-label editor, the `Danger Zone` and the CSV upload card, and a preview table carrying the filter strip in its own two-pane toolbar and the selection actions in an **injected row expander**; nothing renders below the table, and `.bottom-grid` is gone from the page. **Rung 1 shipped, was rejected on the dev slot and was reverted whole** (#2395) — the Decision survived (Variant B, the `.session-expander*` reuse, the three retired cards, the `edit_mode` gate), only the surface it had been sketched around did not, so it was re-agreed by **mockup** before further code. Rung 2 then split three ways: the toolbar move, a **filter-strip base rule** promoted out of the close (four scopes restating one shape with no unscoped base, which is why moving the strip dropped a rule in two separate slices — three of the four are now one base narrowing **6 declarations, down from 49**), and the row actions plus the card's retirement. Rung 3 was sliced on each card's **route response shape, measured before cutting**: `delete-all` and `field-labels` both 303, but the import **re-renders in place** on a failure with its issue list inside the card the panel absorbs — so it went last and carries the panel's **start-open contract**, one rule with two mechanisms (redirects carry `?unlocked=1`; the in-place 400 sets the flag server-side). Each slice wires **and** deletes its own card in one commit, because `sync` resolves a confirm's button by first-match `querySelector` and two live keys gate the wrong one. **A defect the segment had been walking past**: a row action landed at the top of the document, measured at **821px** of jump — and `offset` was carried by **none** of the 17 `_redirect_keeping_selection` call sites, so an action on page 2 answered with page 1 and no anchor could resolve; fixed for Reviewers, and the other three pages still carry it. **No-JS reachability**, raised by the cold read and independently by Codex as a P2: rung 3 put all three cards behind a panel opened only by an inline handler, fixed with a `<noscript>` link to the `?unlocked=1` state the redirects already use — restoring the empty-roster import, not the replace path, which the confirm-pairing script has gated since before this segment. **What the item kept getting wrong is its real lesson.** Six assertions matched something the page renders anyway (`base.html` inlines the whole app's CSS and JS, so one passed against a CSS *comment*, and `assert "disabled" in card` is satisfied by `aria-disabled`); one was created while fixing another; the sixth aimed the trap at the **test suite**, claiming a guard was unique because a grep for a literal missed a sibling reaching the same element through a helper. **Eight spec sentences defined one roster page as "the same shape as" another** — a page's spec falsified by a change to a page it merely cites, which a grep for the changed page never finds. And four confident sentences were simply false, two shipping in three places each and outliving their own correction; the spec cold read found **fifteen**, ten of them plain errors about one page rather than artifacts of specifying it mid-migration. **Rung 4 aligned nine spec files** — and did not follow its own manifest twice, both recorded: § *Operator actions card* could not "retire" because three pages still render it, and one line the bullet sent for retirement turned out to be **Session Home's** Danger Zone. `operator_button_audit.md` row 128 gave the Edit/Add `Save` the **Primary** role while all four roster pages render `btn secondary`, so the audit was wrong about what ships rather than the code drifting; corrected as a record, not a change — **and the close pass caught that the same correction was owed on rows 135 and 142**, which the "measured on all four" claim had the evidence to fix and did not. Four more Reviewers rows (123/124/126/127) still cited the retired `Operator actions` card while three siblings in the same table section said it was retired. **19P.1 specifies Reviewers while the other three pages keep the old shape**, so nine spec files now state the layout per page; un-hedging is a committed sweep at **19P.4's** close. Suite 3,955 → **4,049**. |
| 2026-09-14 | **Segment 19O Item 3 closed — a sent invitation no longer makes a roster un-replaceable** (three PRs, #2390; plan stays in `guide/`, 19O is open). Re-uploading a reviewer roster over one whose invitations had been **sent** raised `IntegrityError` on `DELETE FROM invitations`, reaching the operator as an unhandled **500**. `email_outbox` carries FKs onto both `reviewers` and `invitations` with no `ON DELETE` and no cascade from either parent, and deleting a `Reviewer` cascades `Reviewer.invitations` (`delete-orphan`), so the ORM emitted the invitation delete while outbox rows still referenced them. **Four paths, every one that deletes a reviewer** — `/reviewers/import`, `/quick-setup/reviewers`, `/reviewers/delete-all`, `/reviewers/bulk-delete`; reviewees unaffected, `email_outbox` having no `reviewee_id`. No data was lost: the transaction rolled back whole. *Why it read as "only when assignments exist"* — `generate_invitations` targets only reviewers that have assignments, and outbox rows are written when an invitation is **sent**. **Not a dialect gap**, which is the part worth keeping: `app/db/session.py` enables `PRAGMA foreign_keys` and the test engine imports the same helper, so SQLite and Postgres failed alike, and the suite was green at 3,948 purely because **no test built an outbox row and then deleted a reviewer**. Fixed by `invitations.detach_outbox`, one helper in the only module that writes outbox rows, called from all four paths **and** from `session_purge` — which had this exact unlink open-coded with a comment naming the hazard. *That inline copy is why the bug existed*: the knowledge lived in one path and did not travel, so switching it to the helper is the root-cause half of the fix. **Unlink, never delete**: the row is self-contained (`to_email`, `subject`, `body`, `sent_at`, `backend_message_id`, `delivered_at`), so `reviewer_id IS NULL` with `sent_at` set is the defunct marker. `status` is deliberately not overloaded — and **the plan's reason for that was false**, caught by `diff-reviewer`: a `"defunct"` member would *not* have moved the Setup page's invite summary, because `views/_setup.py` filters `reviewer_id.is_not(None)` alongside `status == "sent"`. The plan contradicted itself, its own Semantics bullet being the refutation; the decision survives on the closed `EMAIL_OUTBOX_STATUSES` alone. **The belief that caused the bug sat in three places, only one a spec** — `roster_bulk.bulk_delete`'s docstring carried the same *"inherited rather than reimplemented"* words as `spec/setup_pages.md`, so 19O.1/.2's Doc-impact finding generalises past `spec/`. **`session_purge` had no outbox coverage**, so the root-cause call site shipped untested; two tests added. Seven tests, seven mutations, each caught by exactly the right ones. Declined and deferred: `spec/sessions_overview.md` specifies no purge modes at all, so the unlink is unstated on that surface — recorded in `guide/deferred_consolidated.md` with its trigger. **First item under the 19O.3 reader cadence**: `diff-reviewer` 5 findings / 4 upheld, `spec-writer` pre-push 5 / 3, **close pass 0 new findings and 0 edits** — everything reached the close already caught. Suite 3,948 → **3,955**. |
| 2026-09-14 | **19O Item 2 follow-up — the self-review control hides, and clears, when the rule it excepts is not settled.** Two behavior improvements on the author's ruling. *(a)* **Hidden while any of the three Links is `Not set`**, with the stored flag cleared in that state: an instrument with an unset Link has no settled rule to except self-reviews *from*, and a flag left ticked behind a hidden control is invisible on the page while being live at the next Generate. Hidden as one unit — rule, heading, checkbox — since a lone divider under nothing reads as a rendering fault. *(b)* **Cleared when Link 3 moves individual → group**, because the two modes except different things: a tick agreed against *the individual reviewed is the reviewer* must not carry into *the reviewer is in the group being reviewed*, which drops every member row of that group. Re-ticking is one click; discovering a whole group went missing is not. Both enforced **server-side on save** (`resolve_exclude_self_reviews`) with the client clearing the box at the same moment — the visible half and the durable half, neither alone. Visibility follows the pills live from **one** function both pill handlers call, rather than a copy in each. **The change broke five of Item 1's tests and they were right to break**: their payload never sent the `*_touched` flags, so those instruments read as `Not set` on all three Links — *All* and *Not set* differ only by that bit, so the fixture had been describing an unconfigured instrument while claiming to test a configured one. **Raised, not decided:** the reverse transition (group → individual) is not cleared, because it narrows what the flag drops rather than widening it, and the author asked for one direction. All three rules mutation-checked, each failing exactly the test that should catch it. **The author dissolved the one open question rather than deciding it**: the reverse move needs no rule because the Link 3 pill cycles `Not set → Individual → Group → Not set`, so it cannot reach individual *from* group without passing through `Not set`, which hides the control and clears the box on the way — a question the UI had already answered. That makes the cycle's shape load-bearing, so it is now pinned by a test rather than left to memory. **And the guarantee was partial until the close pass said so**: session-config import writes the column straight from the CSV while the instrument rows carrying `band1_touched_links` arrive from a different part of the same bundle, so an import could pair a ticked flag with an unset Link — invisible *and* in force, the one state the hide rule exists to prevent. The first rule now runs at the end of the import apply too; a clone needs no guard, since it copies an existing pair atomically rather than combining independent values. **One more spec row Item 1 never reached**: `spec/settings_inventory.md` still called the column *vestigial* with *"every row is `False`"*, untrue since rung 1 and contradicting `spec/roundtrip_coverage.md`, which that item did correct — the same miss, one file further on. Suite 3,940 → **3,948**; `ruff` clean. **Verified on the dev slot** (author, 2026-09-14), closing the one gap every PR body in this item declared: there is no browser harness in this suite, so the live copy swap and the hide-on-unset were argued from structure plus server-render assertions rather than seen. 19O Item 2 is closed; **19O stays open**, so the plan remains in `guide/`. |
| 2026-09-14 | **Segment 19O Item 2 closed — the self-review control's heading and live copy** (one PR; plan `guide/archive/segment_19O_rosters_and_instruments.md`). Item 1's checkbox shipped and the author found two faults on the rendered page. **The copy did not follow the pill**: the label read *"Exclude if the **individual** reviewed is the reviewer"* directly beneath a pill reading **GROUP USING TAGS**, because the mode word was rendered server-side from the *persisted* `group_kind` while the pill cycles client-side — so between cycling and saving, the control described the opposite of what the operator had just selected. Every other thing the pill governs updates live; this one did not. **And the control had no heading**, so what the `.col-divider` separated was left to inference — Item 1 chose that divider precisely to say *this is a second thing*, and a heading is the other half of that sentence. Now `Self reviews`, unbold like the three Link labels. The mode word is swapped by `newModelToggleUnitMode` itself — the handler that already owns every live consequence of the pill, rather than a second listener on the same event — from `data-copy-*` attributes so the two spellings live in one place. **Two whole sentences, not one with a swapped noun**, on the author's wording: *"Exclude if the reviewer is in the group being reviewed"*, because *the reviewer is in the group* is the actual test on a grouped instrument and Item 1's phrasing (*"the group reviewed is the reviewer"*) described something that cannot happen. `Not set` takes the individual sentence, matching the `link3_mode` it submits. **A test of mine passed vacuously, and de-vacuuming it exposed a second bug in the test**: the first assertion read the element's raw markup, which carries *both* sentences as `data-copy-*` attributes, so it held in either mode; narrowed to the span's text it then failed for an unrelated reason — it was reading the **first** instrument card on the page rather than this one, the same trap that produced a wrong-but-passing assertion in Item 1 rung 2. Every assertion now goes through one `_instrument_card` helper with the reason in its docstring. *Scope first, assert second.* Both fixes mutation-checked, each failing exactly the test that should catch it. No behavior change: copy and a heading; the flag, its storage and its effect are Item 1's. Suite 3,937 → **3,940**; `ruff` clean. |
| 2026-09-14 | **Segment 19O Item 1 closed — the self-review exclusion shortcut, and a design that had been argued away on a premise that no longer held** (four rungs; plan `guide/archive/segment_19O_rosters_and_instruments.md`). `spec/assignments.md` named two ways to suppress self-reviews and **one of them could not be reached**: the Link-rule workaround needs `reviewer.email` / `reviewee.email`, the Band 1 field picker offers only `tag1`/`tag2`/`tag3`, and the general Rule Builder retired with the library tier. The affordance had been designed away deliberately — the 13A picker omitted email because *"the engine's `excludeSelfReviews` desugar handles that case implicitly"* — and then **both halves went a day apart in opposite directions**: the picker retired 2026-05-25 (`5aa0c9ed`), `excludeSelfReviews` was pinned `False` 2026-05-26 (`8f8d336d`), the same commit that wrote the policy naming the now-unreachable rule. What survived was post-generation only: materialize every self-review row, then deactivate it. **The mechanism is a per-instrument flag honored after the engine's pair fan-out, never inside it** — which is the whole design, because by that point `_diff_one_instrument` already knows whole-group membership, so a group-scoped instrument drops the **entire group** rather than one `(R, R)` pair. The desugar stage cannot: it filters before group composition, which is exactly the recorded hazard that pinned `excludeSelfReviews` — and worse than one member short, since `classify_self_review` identifies a self-review group *by finding that row*, so a pair-level filter leaves the remainder classified as an ordinary review. The three enforcement layers are untouched. **Rung 1 found the storage already there and being actively overwritten**: `SessionRuleSet.exclude_self_reviews` existed, and `_band1.py` force-normalized it to `False` on *every* Band 1 save to heal rows predating PR #1452 — a job migration `d2e4f6a8c1b3` had already finished, so all the re-write still did was discard operator intent. **The ladder's second deletion would have been a bug**: it also said to drop the `exclude_self_reviews=False` seed, but the mapped column is `default=True`, so removing the explicit `False` would have inverted the default for every new instrument — silently, until rung 3 made the column live. **Rung 2 hit a hole the plan had not seen**: Band 1 only creates a rule set once a Link rule exists, so an untouched instrument had nowhere to store the flag — and that is the instrument most likely to want it, since Full Matrix generates every self-pair. Turning it on now materializes an **empty** rule set, verified output-identical to the synthetic Full Matrix schema (the seed they differ on is read only inside the quota loop an empty rule set never enters); turning it off with no row stays a no-op. **Rung 4 was load-bearing, not decoration** — a row never written cannot be inspected or toggled, which was the live half of the policy's original objection, so the *Self review* cell reads **"Excluded by rule"** where a bare `0` would be indistinguishable from a roster with no self-pairs; *none exist* and *none kept* are different facts. The plan assumed that count is always `0` when the flag is set — **between ticking the box and the next Generate it is not**, so the cell keeps its real count and its toggle until the rows actually go. **Ticking the box on a generated instrument deletes saved responses**, routed through the existing `responses_deleted` count and the Prepare confirm card; asserted rather than assumed. **Every error this item produced was in prose about the code, never in reading what the code does** — a `server_default` cited on the wrong table (read off a grep hit's neighborhood instead of its enclosing function), a blast-radius test count doubled by `__pycache__` files, a "lands inert" claim contradicted by this plan's own judgment call two sections above it, a spec drift reported in the previous session that **was not real**, and a rule set that could be created with no `.created` audit event. Four `spec-writer` passes caught them; each was verified at `file:line` before being fixed. `spec/assignments.md` § *Self-review policy* now distinguishes the desugar stage from exclusion as such and records the unreachable Link rule as removed with its reason; `spec/instruments.md` gains § *Self-review exclusion*; `spec/ui_elements.md` §10 gains `.col-divider`, the horizontal sibling of the Band 1 column separators, which is what says the checkbox shares Link 3 for space rather than belonging to it. **The close pass found the Band 2 preview diverging** — `find_sample_in_scope_reviewee` built its schema from the live Link 1 / Link 2 fields and never read the column, so an instrument with the checkbox set could still preview a sample in which the reviewer reviews themselves. Raised rather than fixed (rung 3's scope rule forbids touching that function, and closing it means deciding what the preview is *for*); the author ruled *"the preview should follow the rule too"*, so it now does — by filtering the engine's **output**, whole group at a time, never by flipping `excludeSelfReviews`, which would drop pairs before group composition is known and reintroduce the very hazard the three layers prevent. A grouped instrument whose only group is the reviewer's own previews empty rather than showing a row Generate would not produce. Its control test earned its keep immediately: the first version of the exclusion assertion passed **vacuously**, because reviewees sort by name and the fixture's other reviewee already sorted first — the sample was never the self-pair to begin with. **Then the review bot and the close pass found three real defects in the mechanism itself, which is where the item's error pattern finally moved.** *(a)* Self-review **groups were detected over the surviving pairs**, so a Link rule that filtered the `(R, R)` row out of the fan-out left the group with no self-review marker at all — and the reviewer went on reviewing their own group with the checkbox set. Membership is a fact about the **roster**; the rules decide only which rows survive. Fixed at the root, which closes the same blind spot in the pre-existing `self_reviews_active` path. *(b)* **"Excluded by rule" was driven by configuration alone**, so a roster with no self-review candidate read as an exclusion that never happened — the very ambiguity the cell exists to remove, one step over; it now requires evidence (`InstrumentReconcileState.self_reviews_excluded`). *(c)* The dry-run's *eligible* figure and the audit pair count were still taken from the **pre**-exclusion fan-out, advertising rows Generate would never create. **And the preview fix was itself wrong on its first attempt**: it reused the picker's reviewee-only boundary list — correct for the member-id partition, wrong for grouping — so a pair-context-only boundary dropped the test to pair level while the generator still grouped, reproducing the same class of divergence the ruling was meant to end. Both now key through `group_key_for_pair` over the full boundary. *Two keyings of one concept is one too many.* Its control test had already earned its keep: the first exclusion assertion passed **vacuously**, because reviewees sort by name and the fixture's other reviewee already sorted first, so the sample was never the self-pair. Suite 3,915 → **3,937**; `ruff` clean. |
| 2026-09-14 | **The two cold readers get separate cadences — and the precondition for saying so failed the measurement.** 19K.3 moved the `spec-writer` pass **before the push** (~8 min/rung) after three items merged with corrections outstanding, one a real code bug; it stayed there, against 19E's close, which had argued the opposite from drift that only a close can see (*prose made untrue, hours after it was written, by a same-day commit*). Resolving that tension meant checking the assumption underneath it — that `diff-reviewer` was holding the maker ≠ checker line (`constitution.md` III) on every rung. **It is not, and never has been.** Measured this date: since 2026-09-06, **70** PRs name `spec-writer` and **0** name `diff-reviewer`; over the last 180 commits, **33** non-merge commit messages name the first and **none** the second; every PR that has ever named `diff-reviewer` belongs to the 2026-09-04/05 arc that created it. `docs/practice-audit-2026-09-04.md` had said on its own date that the reader *"has not yet been run against a real diff"* — the honest qualifier in `rrw_sdd_in_practice.md` §6.4 (*"the reader works when run"*) resolved to **not at all**, and `spec-writer`, chartered **at a close**, became the de facto per-rung reader by default. *That is what 19K.3 was really responding to: not that the close pass was late, but that it was the only reader there was.* So the fix is a swap, not a removal: **`diff-reviewer` on every rung** before the push, unconditional; **`spec-writer` at the close**, unconditional, and before a push only where it can still be falsified by nothing downstream — the rung's diff touches a path the plan's `Doc impact` names, touches `spec/`, or is the closing rung. Outside a close `spec-writer` is in Mode B and can only report, so the deferred rungs lose a report, not an alignment. Stated once in `CLAUDE.md` "Where work runs" (twins copied); pointed at from the `segment-plan` skill's close step 3, which is unchanged, as are the five Definition-of-done lines. §6.4 carries a dated annotation rather than a rewrite. **No tool, no CI step, no hook** (`constitution.md` VI): the decision is `git diff --name-only main...HEAD` read against the manifest. **What is not yet known** is whether the swap holds — naming a cadence is not evidence it is run, which is the same failure this row records; the next audit re-measures it the same way, by the commands now recorded beside the figures in §6.4 (the PR counts come from the GitHub API, since PR bodies are not in git and a session clone is shallow). The corrections side of 19K.3's trade also remains untimed, so the ~8 min/rung is still quantified in one direction only. **The new cadence's first run paid for itself.** `diff-reviewer`, run on this change under its own new rule, returned ten findings, **seven upheld**: `constitution.md` III is *derived* from §6.4 and still read *"making the pass routine is the open item"* after §6.4 was annotated — the two-places drift V exists to warn about, committed in the act of citing V; `docs/unenforced_conventions.md` had no entry, though the commit invoked VI by name and VI's trade-off names that file as the list (now §1.7 — the one entry whose subject is a practice rather than a property of the tree); the §6 appendix row still asserted the *6 + 5 commit messages* parity the annotation fourteen lines above falsifies; two of the three `spec-writer` triggers **presuppose a plan** and were undefined for a plan-less change — *which this change is*, and it had adjudicated itself under the rule anyway; *"can only report"* overstated Mode B, which forbids re-aligning a spec to the code rather than all writing; `rung` was `segment-plan` vocabulary imported into a file that says `slice`; and one British form in new prose. **Three declined**: a superseded cadence left standing in `guide/codebase_assessment_11sep.md` and an archived 19K definition-of-done line, both dated records — *repointing either falsifies a log*, the call `19M.10` made four times over `spec/`; and this row's own restatement of the triggers, timeline rows being self-contained by local convention with `CLAUDE.md` named as the normative home. **And the boundary moved on its own evidence**: the rule said *before the push*, but a stop-hook caught that an unpushed commit in an ephemeral container is a loss risk and a draft PR is not a merge, so the gate is now **before ready for review** — the repo's own merge gate (`CONTRIBUTING.md`), not `git push`. No behavior changed; docs only. |
| 2026-09-13 | **Segment 19N Item 2 closed — the identity fold is `str.lower`, and the "Unicode-correct" answer was the dangerous one.** `email_identity.normalize_email` case-*folded* while a dozen comparisons composed in SQL used `func.lower(column)`; nine compared a `func.lower` column against an already-casefolded Python value, so the two sides of one expression used different rules (`SC-45`). The item opened expecting casefold to win. It must not: casefold maps `ß`→`ss`, so `straße@example.com` and `strasse@example.com` — **two different mailboxes** — collapse to one key, and that key decides access at the three `web/deps.py` participant gates, at `auth.roles.is_super_admin`, at the reviewer dashboard's roster match and at invite acceptance. **The first write-up called the pre-fix state fail-closed and was wrong** — it checked only whether a `ß` holder could match their *own* lower-cased row (they could not) and stopped. The other pairing was never checked: a `ß` holder's casefolded key matched an unrelated **`ss`-spelled** row exactly, at every gate, because casefold was applied on both sides there. A real fail-open existed; low likelihood in an ASCII tenancy, no evidence it occurred, now closed, and recorded as a fail-open rather than left reassuring. `normalize_email` now strips and `str.lower`s, agreeing with every SQL-side site on every ASCII identity — which is every identity this deployment has. **The fold is deliberately never composed in SQL**, and measuring that turned out worse than the argument predicting it. Against Postgres 16 stood up in the sandbox, SQLite and CPython: SQLite's `lower()` leaves non-ASCII untouched (`ÄÖÜ`→`ÄÖÜ`) where Postgres folds it (`ÄÖÜ`→`äöü`), so `func.lower` means one thing in the suite and another in production — **and Python and Postgres disagree with each other** on `İ`, Python yielding `i̇stanbul` (9 chars, `i` + U+0307) where Postgres yields `istanbul` (8). That second hazard was **not** predicted: the two sides of one comparison can disagree *in production*, where no test can show it. It was found by standing up a real Postgres rather than asserting the behaviour, after the first draft shipped the Postgres half as a claim. Non-ASCII identity is therefore out of scope **by construction**; the stored-normalized-column design that would bring it back is recorded in the plan and deferred (the tenancy is MS365 and not expected to produce such addresses). Two things the finding had not named: `normalize_email` had **zero** direct test coverage, and its module docstring claimed write- and read-time comparisons "can never disagree" — false when written. **And changing one function did not close the gates it was written for** — it took three verification passes to establish that it had. The first found **four** identity comparisons folding **inline**, never calling `normalize_email` — `auth/roles.py` (super-admin), `routes_reviewer/_dashboard.py`, `routes_reviewer/_invite.py`, and two `assignments/_coverage.py` handle filters. The second found **three more**, the worst inside a module the new test already listed: `deps.py:134`, the sign-in resolution deciding which `User` row an authenticated principal becomes, folding with a bare `.lower()` that the test's `.casefold()`-only regex could not see; plus `users.py`'s uniqueness gate and the session-owner lookup, absent from the list entirely. The root cause each time was the same — the blast radius counted *callers* of `normalize_email`, which cannot falsify the claim that everything folds through it. **The third pass found no eighth gate, and that is its finding**: what it found instead was that the protection was still a list someone has to remember to extend, whose regex required a trailing paren and so would have missed `key=str.casefold`, already in use in `views/_filters.py`. So the test was **inverted onto the columns** — `.email` / `.email_or_identifier` are a closed set of two names where the modules are not. `test_identity_columns_are_compared_through_the_fold` scans all of `app/` with no allowlist: a comparison against either attribute must reach `normalize_email` on the line or in its enclosing function, or carry a `# not-identity:` comment inside that function. Cost measured before building it: **10** marker sites against the **67** that marking every `.lower()` in `app/` would have needed. `test_no_identity_gate_folds_inline` stays as the stricter rule for the 15 modules that decide access, now matching three spellings against a `tokenize`-blanked view of the source — widening it to the bound-method form had made it trip on two docstrings *describing* the convention, `deps.py`'s among them. Twelve tests now cover the fold, including eszett distinctness (with the casefold premise asserted alongside), the super-admin merge, and the SQLite/Postgres divergence pinned in a form true on both dialects. Convention recorded in `docs/security_posture.md` §5.5a with the RFC 5321 local-part concession stated. Suite 3,903 → 3,915, all additions; every mutation across the three passes caught by exactly one test. |
| 2026-09-13 | **Segment 19N Item 3 closed — the workflow failure banner named the wrong action.** `next_action_card.html` computed its headline from a two-branch ternary — "Prepare session" when `super_failure.button` was `prepare`, "Activate session" otherwise — while `routes_operator/_workflow.py` and `_session_home.py` pass **five** values. So a failed **Close session** was headlined *"Activate session failed"*, naming an action the operator had not taken on a session that was already activated, with a correct error detail underneath contradicting it. Found by the second-pass spec audit as `NF-01`, the only live user-facing defect among its 22 rows. Fixed with a lookup carrying all five labels **read off the shipped buttons** (`prepare` → Prepare session, `activate` → Activate session, `close` → Close session, `release_responses` → Release responses, `stop_release` → **Stop releasing responses** — the plan had guessed "Stop release" and was wrong); an unrecognized value headlines the generic **"Action failed"** rather than any named button, so a sixth reads vague instead of wrong. `_step_label_map` gains `close`, and the step phrase is suppressed when it repeats the button label, retiring the shipped *"Activate session failed at the Activate session."* too. The same two-value vocabulary was recorded in **three files and six passages** besides the template, and updated in none — `_shared.py`'s `_redirect_url`, `views/_workflow_card.py`'s `parse_super_failure` (which the plan missed), and `spec/workflow_card.md` ×4 — all now enumerate the five. Seven tests; four mutations, each failing exactly the right ones, with the original ternary failing five. Author's ruling: *"copy follow button labels."* |
| 2026-09-13 | **Segment 19N Item 1 — assignments are only ever generated — closed** (seven PRs; plan `guide/archive/segment_19N_generated_assignments.md`). Opened from the author's ruling after asking whether manual assignment still existed in the code: *"manual shouldn't be reachable by operator. Assignments should not be hand created, uploaded, or edited. They should always be generated. Individual rows can be turned inactive, and that's the extent of operator manual work."* The manual CSV route had gone in 16A PR 5, but **three paths still wrote assignment rows outside the engine** and none was operator-visible as such. **The item's load-bearing outcome:** `app/services/` now contains exactly one `Assignment(...)` constructor, `_generate.py:445`, reachable only through `replace_assignments`. **Slice 0 (unplanned, landed first)** gated rehydrate off after the author established nobody had run it on real data — the premise that prompted it was wrong (it was shipped and unguarded, with a spec header reading *"the whole pipeline is live"*), but the instinct was right. **Slice 1** restored a staleness signal retired for a missing count it did not need: the basis moved to the engine's own reconcile diff, cost measured at **10 queries for 5 instruments over a 10×10 roster**, reviving three dead consumers — one of them a *registered no-op* validation rule with a severity, a fix link and a `why` describing a situation it no longer detected. **Slice 2** stopped `+Instrument` and `Replicate` cloning assignment rows, which exposed that **eleven reviewer-surface tests depended on the clone as fixture setup** and that their later `generate` had been a no-op redirect the clone was quietly covering. **Slice 3a** removed the rehydrate backfill — and found that its seven drop sites each appended to a `warnings` list **nothing has ever read**; `counts.assignments_backfilled` became `counts.responses_dropped`, the audit event having reported the fabrication count where the loss was the number that mattered. **Slice 3b** delivers that loss to the operator: a commit that drops rows deliberately **does not redirect**, because a 303 cannot carry a file and a count cannot say *which* rows went; the CSV rides the stash already built for the Validate → Commit hand-off. **Slice 4** defaulted `created_by_mode` to `rule_based` — it had defaulted to `"manual"`, labelling as hand-made a row no hand could make. **Slice 5** was a live bug, not drift: `session_clone` copied `assignment_mode` into a clone carrying no assignment rows, defeating the NULL-means-never-generated skip that stops a pairless session *also* being told every reviewer is missing. **Slice 6** fixed two stale docstrings. **The recurring defect this item catalogued is the one it kept committing:** a signal specified, switched off for a good local reason, and papered over by something nearby that looks like cover — and, four times, prose asserting a consumer that does not exist. Three were caught by `spec-writer` in prose written minutes earlier; the fourth was in the plan's own option list, which proposed a download link on the `?rehydrated=1` banner — a query parameter written by the redirect since 18P PR H and **read by nothing**. Also found at close: **three specs were edited without a Doc impact bullet, which `close_check` structurally cannot catch** — C3 asks whether every declared path was edited, never whether every edited path was declared. The legacy-row data migration was **struck by the author** (*"don't worry about historic sessions — they are still dispensable at this point"*); `rehydrate_enabled` **stays false**, the author's reason having been broader than the defect that is now fixed. Suite 3,866 → **3,892**. |
| 2026-09-13 | **Segment 19M Items 9 and 10 — the sweep's remaining doc-only fixes, and the documentation that described its own tooling.** **Item 9** took the ten `SI` rows (a spec contradicting itself) and the `SS` rows a document can settle without the code moving — eight fixes. **Twice the right fix was to point rather than restate**, the same conclusion Item 7 reached about the role legend and the verification pass reached about `ui_elements.md` §1: *a second copy of a statement is a second thing to keep in step.* `setup_pages.md` stated the Upload confirm label two ways, three-clause and two-clause; the template renders three (count / assignments / responses) **and the response clause had gone missing once already**, so the short version now points at the full one. Three results the plan did not anticipate: **`SS-05` was not one-sided** — *"Manage Invitations"* appears three times in `email_infra_options.md` as well as in the audit's own heading, so it was a vocabulary in circulation rather than one file's slip, and all four now use the chrome label `Invitations`; **`SI-03` needed a distinction, not a deletion** — two `audit_events` rows carried opposite marks because they answered different questions (*is there an extract* / *is it in this inventory's scope*), so the survivor states what its ✅ does and does not mean; and **`SI-01`'s ASCII box had to stay aligned**, so adding `+Page break` meant shortening *"delete-confirm checkbox"* to hold all 66 characters — verified by measuring **characters, not bytes**, since box-drawing glyphs are three bytes each. `permissions.md`'s verification method now says to follow `Depends()` **transitively**, because a decorator scan flags ≥9 correctly-gated `_instruments.py` routes: *a check that flags a correctly-gated route is worse than none, because the next reader believes it.* **`SS-06` was not a mismatch at all** — the Workflow card has **twelve states over ten numbers** (1–10, with `4W` and `4Err` branching off 4), now stated in `workflow_card.md` so the five documents calling it a *ten-state cascade* stop reading as errors. `SI-07` stays registered: rewording does not fix it, since nothing pins the query-budget figures and the related test only asserts relative growth — **a guard is the right answer and a guard is code.** **Item 10** corrected `rrw_sdd_in_practice.md`, which described the rewritten `spec-writer` charter in four places — one already right (it named the phase), three not, including a verbatim quote of words the file no longer contains. **Four edits where the plan predicted three**, because §6.2's figure was the smaller half: it said *"only the functional spec dates itself: 4 of 36"*, and **no live spec carries a currency line at all now** — 19M replaced each with a sweep-record pointer, so the paragraph described a *mechanism* that no longer exists. Restated with 19J.1's reason (*a date nobody owns is what let §9.7 describe a card that never shipped*) and what it trades: a figure that rots quietly for one that rots visibly, since a sweep record carries its own date and scope. Facts corrected, arguments untouched — it is the author's measured analysis, so each edit is the smallest that makes the sentence true. **`DT-04` dissolved, and that is the finding**: it named `docs/practice-audit-2026-09-04.md`, a **dated** document whose quote was accurate on its date, and `guide/todo_master.md`'s five mentions, which record what a *pass found* rather than what the charter says. Repointing either falsifies a log — the call the sweep made four times over `spec/`, holding here for `docs/` and `guide/`. *Making it correctly on the last item is the only evidence the day's lesson took rather than being written down.* `close_check 19M.7`–`19M.10` PASS. Suite **3,868** passed / 16 skipped; `ruff` clean; **no behaviour changed**. |
| 2026-09-13 | **Segment 19M Items 7 and 8 — the sweep's doc-only fixes start landing** (plan `guide/archive/segment_19M_spec_history_sweep.md`; register `guide/archive/findings_2026-09-13_spec_discrepancies.md`). The sweep registered **75 discrepancies and actioned none**, on the rule that *a sweep recommends*; the author's instruction splits them — **doc-only fixes land in 19M**, and anything needing a code change or a contract decision stays in the register. *Kind and cost are different axes*, so the split is not the register's own five kinds: **ID** and **SI** are doc-only by construction, **SS** mostly, **SC** and **CC** not. **Item 7 — all 8 `ID` rows**, a spec naming an identifier that does not exist. **Every replacement was verified to exist in `base.html` before it was written**, because the sweep's own thirteen errors were mostly a name substituted inside an otherwise correct sentence. Two results the plan did not predict: `operator_button_audit.md`'s role legend **stops naming tokens at all** rather than repointing four of them — it exists to be a reading key for `ui_elements.md` §6, so pointing means it cannot drift from §6 again, which is the same fix §1 reached independently during the verification pass; and **one retired name covered two different live tokens** (`session_home.md`'s `accent-blue` was a card border *and* a button fill — `--card-active-border` and `--btn-primary-bg`), *which is the argument for the two-tier system, found by having to undo a flat name.* `--color-border` had **0 occurrences of any kind**; `ID-06`'s hard-coded `#f3f4f6` fallback went with its token, since a literal cannot follow the theme; `ID-08` was a **named** cross-reference to a heading that does not exist, which the `§N` guard cannot see. Two retired names remain in `spec/` and are correct — the do-not-reintroduce constraint must name them to forbid them. **Item 8 — `SC-01`…`SC-04`, on the author's ruling** *"update spec/comments; code is correct"*, which is the deliberate contract change §4 permits as against the silent one it forbids, so **the instruction is recorded in the register beside each row** rather than only in a commit. `settings_inventory.md` now states that an unrecognized `session_rule_sets[n]` attribute **is rejected** and the whole apply fails — with the reason (a misspelled attribute dropped in silence would change *which pairs generate*, unlike the top-level unknown-key ignore) **and the cost** (a bundle exported while `library_name` existed needs the column removed first). **I had argued the other way and the author's reading is better.** `assignments.md` now states an absence as the contract — *"there is no `stale` pill and no staleness signal on this page"* — with the consequence a reader needs, because *a reader who assumes the page warns them will not check*. `reviewer-surface.md`'s textarea factor becomes **0.5**, matching the code and the test that asserts it, so the gate is no longer on the wrong side. **Correcting the code comments turned up two findings the register did not have:** the `"generate"` next-action state is **unreachable** (gated on `any_stale`, forced `False`) while its docstring described what it would catch as though it ran; and the inline comment **forecast a PR that had already shipped** (*"until PR 5.3"*), so the force is the design rather than a pending state — rewritten with the honest reason, that an always-stale badge trains the operator to ignore it. *Both are the sweep's own defect sitting in code rather than in `spec/`.* Open questions 1 and 3 answered; `close_check 19M.7` / `19M.8` PASS. Suite **3,868** passed / 16 skipped; `ruff` clean; **no behaviour changed**. |
| 2026-09-13 | **Rehydrate gated off (Segment 19N slice 0)** — `rehydrate_enabled` ships **false**, the three `/operator/sessions/rehydrate` routes 404, and the lobby button no longer renders. *The feature is built, not withdrawn*: the machinery stays wired and covered, the suite sets `REHYDRATE_ENABLED=true`, and re-opening it is one flag. **Why now:** `SC-40` — a response the regenerated rules cannot place is dropped with a warning that reaches **nobody** (not the `session.rehydrated` audit counts, not the commit route, not the operator), while `spec/rehydrate.md` §9 promised *"no response is lost"*. Live silent data loss on a restore path, found by the Codex review on #2358 and verified here. Nobody had run rehydrate on real data, so the surface closes rather than shipping a fix under pressure. The author's framing: *not ready in the specific sense that not all the possible details are fully worked out, even though unproblematic cases will work.* **Before the flag re-opens** (19N behaviour 4): every response a legitimate assignment can carry is loaded, and every row that cannot be placed returns to the operator as a dropped-responses CSV — assignments are never invented to give a response a home. The spec header retracts *"the whole pipeline is live"* and §9 now states the gap rather than the promise. **A gate on the page alone would not have been one**: validate and commit accept a POST from anyone who knows the path, so all three routes carry the dependency, and a test asserts each 404s with an *authenticated* client — an unauthenticated request returns 401 before the gate runs and would have passed a naive check while proving nothing. 5 guards, **5/5 mutations caught**, each by the single test that should catch it. Suite 3,870 → **3,875**; `ruff` clean. |
| 2026-09-13 | **Segment 19M — a general sweep: history out of the specs — the six sweep batches all landed** (plan `guide/archive/segment_19M_spec_history_sweep.md`; record `guide/archive/sweep_2026-09-13_spec_history.md`; findings register `guide/archive/findings_2026-09-13_spec_discrepancies.md`). All **39 live `spec/*.md`** files read, **3,198 inserted against 4,115 deleted**. Opened at the author's instruction after the `ui_elements.md` sweep established the rule on one file and then had to be reversed to obey it. **The headline: plan apparatus 86 → 0** — the `*Current:*` / `*Canonical:*` / `*Migration delta:*` / `*PR:*` shape was confined entirely to `ui_elements.md` and is gone — and **dated lines 273 → 16**, every survivor a constraint, a pointer or an example payload. *Those two baselines were published as 252 and 239 and are wrong — re-measured against the git objects rather than by summing a printed table, they are 273 and 255, corrected in the plan, the roadmap row and here*. **The rule, from the author:** *"Express constraints as such, with brief reasoning if needed. But that's not strictly speaking 'history', even though the reasoning was confirmed through something in the history."* Three buckets: provenance out; constraints re-expressed **forward** with a brief reason, since a reason established historically is not history; absent subjects deleted. **A mechanical date-strip was refused** — the `ui_elements.md` dead-identifier pass had produced twelve candidates of which **four were correctly-recorded history** that would have made the file worse if "fixed". **The segment was corrected mid-flight and that is its most important event.** Three batches in, the author pointed at `rrw_sdd_in_practice.md` **§4**: `spec/` answers *"what is X supposed to look like and behave like"* and its authority is **the contract** — *"when the code drifts from a spec, the spec is the canonical source — fix the code"* — while *"how does X work today"* is **`docs/`**'s question. All six batches had been briefed to write "present-tense description of what ships", which is `docs/`'s function; removing history was right, what replaced it was the wrong **kind** of thing. Three consequences went to every running agent: no measurements of the current tree in a contract (a count self-stales, which is this sweep's own diagnosis one level up); a spec never hedges itself against the code, because a contract does not report its own confidence level; and **where spec and code disagree the spec wins, so the divergence is reported, not re-aligned**. **Four batches reverted rewrites they had already made** — Item 5 six, Item 6 two, Item 4 one, Item 1 one — so **ten contracts were saved from silent demotion into descriptions of the code**, which is the only place the correction could be observed working. Per item: **19M.1** the token family, finishing the apparatus conversion and settling the retired `accent-*` vocabulary, while **declining to touch `visual_style_general.md`'s 61 lines of it** because that file's own preamble states forward that they are the portable design system's role names and renaming them would contradict its own guard paragraph; **19M.2** operator chrome, whose largest deletion is the audit-to-contract conversion in miniature — `operator_button_audit.md`'s *"Drift / inconsistencies surfaced by the audit"* became six numbered **rules**, because an audit's findings are provenance and an audit's rules are the contract — and which removed a header the batch had itself written saying *"treat a contradicting row as the row being wrong"*, an inversion of §4's authority; **19M.3** core contracts, including six *"this document used to say X"* notes the 2026-09-10 sweep had left inside the functional spec, and two files that contradicted **themselves** (`lifecycle.md` claiming four live states directly above its own five-row table); **19M.4** the domain engine, the first batch under the correction and behaving as predicted — **ten divergence findings and two deletions across six files**; **19M.5** participant surfaces, which had to separate *deferred* from *historical* and kept `reviewer-surface.md`'s `beforeunload` deferral as a requirement while noting the pattern ships on the operator side; **19M.6** data IO, where a dated note is usually a **compatibility tolerance** rather than provenance — the `field_labels.*` silent ignore, case-insensitive `data_type` matching, the data-shape fallbacks and rehydrate's presence inference were all kept as obligations, because deleting one would break a real importer. **75 discrepancies are registered rather than fixed**, across spec-vs-code, spec-vs-spec, spec-internal, code-internal and stale identifiers; *a sweep recommends*. The one with a user-visible consequence: `settings_inventory.md` says a `session_rule_sets[n].library_name` row on input **must be recognized and skipped, not rejected**, and `_apply_rule_set.py:53` raises instead — and because apply is two-phase, **an older bundle fails to import entirely**. Two more verified: the `stale` pill is specified while `views/_assignments.py` hardcodes `is_stale = False` **and its own docstrings still describe the computation**; and `reviewer-surface.md` specifies a 0.75 textarea factor where the code has 0.5 **and a test asserts 0.5**, so the suite currently defends the divergence — *the gate is on the wrong side*. Five validation severities differ between the per-page specs and `validate_page.md`, which agrees with the code; **two are specified as errors where the code warns, and as errors they would block activation**, so all five were left untouched for a deliberate decision. **`.claude/agents/spec-writer.md` was rewritten** at the author's instruction: its charter contradicted itself — step 3 *"update the spec to match current behaviour"* against step 5 *"flag drift rather than silently rewriting"* — and now names two modes, aligning to the code **only at a segment close** (§6.1's *spec on the way out*) and otherwise reporting divergence, with *"if you cannot tell which mode you are in, you are in Mode B"*. Marked `<!-- sweep-scope: partial -->`: it reads every live `spec/` file and **no** `docs/` file or root document, so the 8-weeks / 500-merges corpus clock still runs from 2026-09-05. Suite **3,868** passed / 16 skipped; `ruff` clean; **no application code changed**. |
| 2026-09-13 | **`spec/ui_elements.md` swept, then the sweep reversed on the author's rule — history does not belong in a spec** (record at `guide/archive/sweep_2026-09-13_ui_elements.md`, marked `partial`, so the corpus cadence still runs from 2026-09-05). Requested after 19L.4's `spec-writer` pass surfaced three stale RTD references. **They were not three mistakes but three symptoms of one.** The status block has declared the file "migration-complete … now the implementation catalogue" since **2026-05-11**; the body still carried **23 `*Current:*`, 22 `*Canonical:*`, 27 `*Migration delta:*`, 25 `*PR:*`** blocks — the shape of the plan it was before that date. *A `*Current:*` block is a snapshot and nothing renews it*: written 2026-05-03 it describes 2026-05-03 for ever while calling itself current. The RTD card retired 2026-05-26, twenty-three days after the block naming its banners was frozen. This file **had never been swept as a subject** — the 18Aug sweep lists it among files it did not read and the 2026-09-05 corpus sweep cites it only as a cross-reference target; four months of drift accumulated because it was always the thing other documents pointed *at*. **Ten edits, and three of them were wrong** — caught by `spec-writer` after the words were written and before they merged, and recorded in the sweep's own §2b rather than quietly fixed. `.status-icon` is **not** defined in `base.html`: only `.status-icon-complete` / `.status-icon-incomplete` as **siblings**, so the entry's compound selector `.status-icon.status-icon-complete` never shipped and was aspirational when written. The `<pre>` / outbox item is **shipped, not moot** — `.code-block` landed in `024c48ed` (2026-05-03), *before* the migration-complete date the header cites, and `session_outbox.html` is gone only as a filename — and the citation propping the wrong claim up was taken from a 2026-09-05 sweep entry about **whether the page warrants its own spec document**, which says nothing about CSS: *the citation borrowed authority from a finding on a different subject, which is how a wrong claim acquires a footnote and stops looking wrong.* And the Lock / Unlock description **matched neither anchor**, taking the handler from one and the href from the other. *Three wrong out of ten — §1 argues a `*Current:*` block rots because nothing renews it; §2b is the same lesson one level up: a **finding** rots unless something checks it, and the checking is not optional because the finder is confident.* **Four of the twelve candidates a mechanical dead-identifier pass produced were correctly-recorded history and left alone** — notably §6's `session_edit.html`, a dated correction note that says in as many words that the template no longer exists; *a sweep that fixed all twelve would have made the file worse in four places.* **Then the author reversed the direction:** *"Let's keep history out of spec docs as much as possible."* §1 had diagnosed the defect correctly and §2 had then made six entries **more** historical, not less — rewriting snapshots as *dated* snapshots (*"Was, at 2026-05-03"*, *"Shipped — swept 2026-09-13"*, *"corrected the same day"*, commit SHAs, *"complete for four months and never marked"*) so that a reader wanting to know what a banner **is** had to read four months of provenance first. Those six are now plain present-tense description with the `*Current:*` / `*Migration delta:*` / `*PR:*` apparatus dropped from them entirely — **which is §4's deferred plan-to-catalogue conversion actually done for six entries, in the direction the author supplied**, since §4 had scoped that conversion without saying which way it ran. Two deletions rather than rewrites: the `form style="display: contents;"` entry (0 occurrences — *an absent subject, not a stale description*) and a §9 `*Current:*` line asserting inline-styled glyphs in `review_surface.html` that the extraction had removed, which §2.6 left standing while rewriting the delta beneath it, so the entry asserted the inline styles **and** their replacement. The header's dated bulletin went too, **including the sweep's own four block counts** — the same mistake one level up, a measurement pasted into a spec where nothing renews it — replaced by a **How to read an entry** note telling a reader that an unconverted `*Current:*` line is a 2026-05-03 snapshot and a lead to check, not a description of what ships. **The wider pass is scoped, not done, and the reason is a distinction rather than its size:** the file carries ~30 more history sites (a dated correction note in §6, three *"Corrected 2026-09-08"* annotations, retired-class records, and an `added <date>` / `restyled` column on §10's primitives table — a changelog inside a spec), but **history that explains why something must not change is load-bearing and history that records when something landed is not**. §10's `.session-row-selected` account of 19L.1's fill — that it resolved to `--status-info-bg`'s primitives and erased every pill, and that the 0.810–0.914 luminance band leaves no clearance above — reads as history and functions as a guard: delete it and the next author re-adds the fill. So a wider pass must sort three ways — provenance to `guide/` and here; rationale-expressed-historically rewritten **forward** as a constraint; absent subjects deleted — and that is an item's worth of work. No behaviour changed and no code moved: suite **3,868**, `ruff` clean. |
| 2026-09-12 | **`guide/archive/new_ux_ideas.md` opened — exploration only, beyond pilot, nothing scheduled.** A place for shape-level operator-UI ideas to live other than a conversation. Distinct from `deferred_consolidated.md`, which holds *scoped* work carrying a lift trigger; an entry here has only been *described*, and graduates solely by becoming a segment plan. Opened with the author's proposal to **consolidate Reviewers, Reviewees, Relationships and Observers into one Rosters page** — a top full-width table of all rosters and their columns, with select-to-edit opening an inline row (friendly labels, Clear all, Upload/Download CSV, guarding checkboxes) and select-to-preview showing a search box above the preview table. **The rationale was measured rather than taken on faith:** the card sequence is *identical* across Reviewers/Reviewees/Relationships and a superset in Observers — 3,038 template lines and 2,778 route lines, 5,816 across the four — and `spec/setup_pages.md` already asserts the shared body shape, so the idea partly proposes making the code match a shape the spec describes. The 12sep Codex assessment supplies independent support (Reviewers/Reviewees setup routes are the repo's most duplicated production pair, each roughly half duplicated) **and the strongest objection**, warning by name that a generic roster abstraction hides audience-specific behavior. Both are recorded. The honest framing the entry lands on: **the presentation is near-identical and the semantics are not** — Relationships is a join rather than a roster of people, Reviewees alone may carry non-email identifiers, and the author confirmed Observers will have features unique to it, which makes per-roster divergence a stated property of the target rather than an edge to design around later. Six open questions recorded so they are not rediscovered, the load-bearing one being where per-roster exceptions live. **The author then set a constraint against that objection's operator-facing half:** a consolidated page must show which roster is selected and being worked on, unmistakably, at all times — and offered the session lobby's row expander as a candidate, fanning the selected row out with the edit/delete/upload affordances while marking that row out visually. **The entry's first attempt at justifying the constraint was wrong, and the author's pushback was right.** It claimed the guarding checkboxes gate *intent* and not *target*, so a guard would have to be made to name its roster — *a stronger requirement than the four pages have today*. Reading the templates rather than imagining them: every one of the four **already** names its roster at the dangerous moment — *Yes, delete the existing **N reviewers*** in a pill, **Delete all reviewers** on the button, with a template comment reading *"The sentence names what goes"* (19I Item 3). The requirement is **exactly today's**, and the job is carrying it across rather than inventing it. The error came from reasoning off an invented checkbox instead of opening the file. So the author's *"the roster pages are already similar enough to mislead, I doubt consolidation makes it much worse"* holds: what protects the operator at the confirm step is per-**control** copy, which travels with the control and survives consolidation intact. What consolidation actually removes is narrower — nav highlight, breadcrumb, URL and heading, which orient *before* acting rather than *as* acting. **The lobby precedent was read rather than recalled, and cuts against literal copying twice:** its single-select title says *"1 session selected"* and never names the session, because identity arrives free from an editable Name box — a roster has no such field, so copied exactly it yields a panel headed *"1 roster selected"* on the one page holding Clear all and replacing Upload, which makes the author's mark-the-row-out addition the necessary part rather than a garnish; and the expander is still a **placeholder** with its action buttons disabled, so it is an agreed shape, not a proven interaction. **The author then corrected the entry twice more, and both corrections narrowed it.** First: *the point is not copy naming the selected roster, it is a visual state marking the row out* — a distinction the draft had blurred by arguing almost entirely on copy. Copy is read if it is read; visual state is seen before the operator attends to anything, and the operator this protects is the one moving too fast to finish the sentence, so the two are complementary rather than substitutes and act at different moments. Second: *the lobby does not do this beyond the check box.* Verified — the lobby's entire row-level selection signal is a ticked checkbox at the left edge of a full-width row, with no fill, border or weight, so a selected-row visual state is **new work rather than reuse**, and the lobby is a precedent for the fan-out and an example of the gap for the marking. **That gap became entry 2**, recorded as explicitly separable: the lobby's own marking wants improving whether or not the consolidation happens, degrades exactly where the lobby invites it (bulk selection carried by scattered checkboxes, a tall panel pushing the row off-screen, selection restored without the click that made it), and is the cheaper of the two — it would hand entry 1 the primitive it otherwise has to invent. Sequence, not bundling: entry 1 stays gated on pilot evidence and entry 2 is not. **The index guard from #2337 did its job on its author:** the new file matched no documented shape and failed the suite until `guide/README.md` gained a row. |
| 2026-09-12 | **`guide/archive/new_ux_ideas.md` — entry 1 refined, and a second consolidation entered as entry 3.** The author elaborated the Rosters sketch: *select to preview* opens a **half-width search box card** and a full-width preview table card beneath the roster table, **one roster's preview at a time** (selecting another closes the previous); *select to edit* keeps its inline row and gains a scope — **roster-level metadata only**; and **row-level work moves to the rows**, with selection in the preview table opening an action row in the manner of the lobby's expander. **That last part has a measurement behind it:** `spec/setup_pages.md`'s Operator actions card does **two jobs at once** — a search/filter strip *and* an action row (Edit, Inactivate, Activate, Delete, the destructive set) — and the proposal keeps the first and sheds the second, so each job gets a place and the row actions sit beside the rows they act on. Same argument 19L.1 made for marking a selected row, one level up. **Three of entry 1's six open questions moved.** Deep links from Validate are now a **requirement** rather than a question, on the author's instruction, leaving only the mechanism open. The nav question is answered at *one* item. And the ragged-table question narrows to the **index** table alone, since one-preview-at-a-time settles it for the preview. **The nav arithmetic was checked rather than accepted:** `session_top_nav.html` carries **13 destinations** today (Session Home, a Setup group of six, an Operations group of six); the author's eight-item target reconciles exactly — four rosters to one (−3), three Operations pages to one (−2), 13 − 5 = 8. Two consequences the list makes without stating: it is **flat**, so the Setup / Operations grouping goes, and it writes **Data Extract** where the app says **Extract data** — recorded as a possible rename rather than silently adopted. **Entry 3 is the Monitoring consolidation** — Previews, Invitations and Responses on the same structure, with **Assignments deliberately excluded** as sufficiently different, which is the one page a naive *consolidate-the-Operations-group* would have swept in. Filed as entry 3, not the "Item 2" the author said: entry 2 is the lobby marking, already graduated to 19L.1, and the numbering collision is flagged in the entry rather than resolved unilaterally. **Its case against is recorded as weaker evidence, not borrowed evidence:** nobody has measured whether those three duplicate each other the way Reviewers and Reviewees do, so entry 3 has **no figure** and must not borrow entry 1's 5,816 lines — and `_operations.py` sits on the watchlist at 1,038 lines with the 12sep assessment advising *split Invitations from Responses if it grows*, which points the opposite way from merging a third page in. Both entries remain **exploration only**, gated on pilot evidence. |
| 2026-09-12 | **Segment 19L Item 1 closed — a selected lobby row is marked, not just its panel.** Until now the only row-level signal that a session was selected was its own checkbox tick: the injected panel tinted its own `<td>` and **no class ever reached the source `<tr>`**. Adequate at the instant of clicking, weakest exactly where the lobby invites it — a bulk selection scattered down the table, a tall panel pushing the row off-screen. **The expression was chosen by looking, not arguing.** A specimen board rendered all four candidates in both the single and bulk cases, in both themes, with the app's real tokens; the author picked **edge *and* fill** — which was **not one of the plan's three candidates**, and was on the board only because it is what `.tag-chip.is-selected` already does. *An open question that asks for a rendering can be closed by producing it.* Ships as `--selected-bg` in a 3px inset shadow over `--row-selected-bg`, an inset shadow rather than a border so selecting a row does not change its height and reflow the table under the pointer. **A new token rather than the chip's.** `--chip-selected-bg` resolves to the right primitives and would have shipped without one; reusing it would have meant a row silently inheriting a repoint aimed at chips, which is the drift the two-tier system exists to stop. `--row-selected-bg` is its own role — same primitives today, its own name — and `spec/color_tokens.md` was **not in the plan's manifest**, so the bullet was added and the divergence recorded in `Status`. **Two of this repo's own 19K.7 guards made the change correct rather than merely plausible:** the Tier-2 catalogue check failed twice, once for the missing row and again because the spec states **80 primitives · 106 semantic** in prose (now 107) — a catalogue that counts itself is harder to leave half-updated; and the generated-tools check failed because both theme pages are generated *from* `base.html`, so a new token is invisible in the customizer until they are regenerated. **The reserved-shade guard passed unmutated**, which the plan required, and reading it rather than assuming gave the reason: its filter is pill / chip / `btn-icon` because its subject is elements with a **dual nature**, and a `<tr>` has one — a selected row *is* the thing the panel will act on, which is what that shade means, so it is correct here rather than tolerated. **Verified in Chromium, not asserted:** eight paths driven against the rendered page — at rest, single, bulk, un-tick one, un-tick all, select-all, clear, and the computed style (`rgb(37, 99, 235) 3px inset` over `rgb(219, 234, 254)`), plus two more because a code comment claimed them (**Unselect all** 3→0, **Unselect others** 2→1). *A claim in a comment is a claim.* **Select-all justified the placement**: it sets `checked` programmatically and fires no row events, so a per-checkbox listener would have missed it — the marking lives in `refreshExpander()`, the one funnel every path meets, clearing all rows each pass before marking the selected set. **No hydration is needed, and that is a finding:** the filter and search hide rows with `style.display` rather than re-rendering, and selection is not persisted (the lobby's only `localStorage` key is `rrw-lobby-tag-filter`), so a server re-render returns unchecked boxes and an unmarked table — recorded with the condition that would change it. **8 mutations, 8 caught**, and the guard states at its own definition what it cannot do: it holds the mechanism, never that a row *becomes* marked, because the suite has no JS runtime. Open questions 2 and 3 left open deliberately — a fifty-row render, and *not yet* on promoting the class to a shared primitive, since it has one caller and the second is a speculative page in an ideas file. Suite 3,852 → **3,857**. **UI-visible: verify on the dev slot after deploy.** |
| 2026-09-12 | **Segment 19L Item 2 closed — the selected row's fill was erasing the pills it carried, so the fill is gone and a bracket replaces it.** 19L.1 shipped an edge *and* a fill; the fill was `--row-selected-bg`, which resolves to the **same primitives as `--status-info-bg`** — and under `body.ui-v2` that token backs both `.pill-count` and `.pill-info` from a single rule (`base.html:3238`). A lobby row carries four to six of those (Created by, Created, Deadline, Timezone, one per tag), so on a selected row **every one of them vanished**, along with a Validated status pill. Measured at **1.00** against the pill, where under ~1.15 a pill has no visible boundary; the author's own deployed screenshot shows *Local Operator*, both timestamps, *GMT+8*, *TESTING* and *VALIDATED* as bare text while *DRAFT* keeps its amber at 1.10. **No replacement fill escapes it, which is why the fill went rather than changed.** The six pale pill fills occupy relative luminance **0.810–0.914** against a 1.000 card: there is no clearance above the band — `--blue-wash` at 0.915 lands on its ceiling, which is why it reads as too faint — and today's `--blue-pale` sat at the band's *floor*, so lightening it moved *through* Activated (0.876) and Draft (0.893) rather than away from them. Only `--gray` #9ca3af cleared every pill, and at 2.54 against the card it stops reading as a highlight. **Four specimen boards drove the decision**, each rendering the real lobby with the app's own tokens; the author rejected a grey fill, a wash fill, bordering the blue pill (it would make `.pill-count` the only bordered pill in the app), and bracket caps (4px of reflow on selection, against 19L.1's reason for using an inset shadow at all). **What ships:** no row fill; `--selected-bg` as a 6px inset rail on `td:first-child` and its mirror on `td:last-child`; the same pair carried through the injected expander row, whose single `colspan` cell is first and last child at once. The rail reaches **5.17** against the card in light and **4.85** in dark, where no fill in this palette passes 2.54. **The token was renamed, not retired** — `--selection-panel-bg`, same two primitives, now filling the action panel, which holds no pills. That was the author's suggestion and it measured better than expected: the panel's inputs and buttons are `--surface-page`, scoring **1.09** against the `--surface-muted` it replaces — *the same invisibility, already shipping inside the panel*, legible only by their borders — and they reach 1.22 light / 1.41 dark on the blue. `spec/color_tokens.md`'s count stays **107**, as a rename should. **The opt-in class earned itself in the blast radius.** `sessions_archived.html` injects a panel carrying *the same two classes* as the lobby's bulk panel, from its own `refreshExpander()`, with no row marking at all; a rule on `.session-expander` would have given that page the closing half of a bracket with no opening half. **A stale figure found and fixed:** 19L.1 doubled the edge 3px → 6px in `base.html` and in the guard's floor and left **both** `spec/ui_elements.md` and `spec/sessions_overview.md` saying 3px — undetected for a day because the guard asserted `>= 2`. *A range cannot pin a figure.* A new guard now reads the shipped width and requires the spec's to equal it. **Also fixed, one declaration:** `.session-expander-fields label` never reset `body.ui-v2 label`'s margin, so 12px stacked on the panel's 12px flex gap and the field row sat **24px** below the title in a panel whose stated rhythm is 12px — `.exp-allow-delete` two rules away already carried that reset, with a comment naming the hazard. Merging the bulk panel's Tags label onto the title row was mocked up and **declined**: only the bulk expander can take it, and the two panels would stop being structurally alike. **8 mutations, 7 caught, 1 escaped and fixed** — reverting the panel to `--surface-muted`, undoing half the item, passed all ten tests, because every guard checked the rails and none checked the interior. *A suite written around the striking half of a change will pass the half nobody photographed.* **`guide/archive/new_ux_ideas.md` entry 2 removed** on the author's instruction — session lobby work is segment work, not an idea awaiting pilot evidence — which retires the numbering collision recorded in the row above: Monitoring is now entry 2, matching the author's own "Item 2". Suite 3,857 → **3,862**. **UI-visible: verify on the dev slot after deploy.** |
| 2026-09-12 | **Segment 19L Item 3 closed — the archived sessions page now brackets a selection, as the lobby does.** 19L.2 deliberately kept the bracket off `sessions_archived.html`: it injects a panel carrying **the same two class names** as the lobby's bulk panel, from its own `refreshExpander()`, and its rows had never been marked — an unscoped rule would have handed it the closing half of a bracket with no opening half, so the lobby opted in by class instead. **Correct as a mechanism and wrong as an outcome**: the two pages are siblings, reached from one another and rendering the same table shape, and they selected differently. The author's ruling, 2026-09-12: *"Archive page should follow the same conventions"* — which **reversed 19L.2's open question 2**, closed hours earlier as *undecided* on the reasoning that the dev-slot pass had covered the lobby and said nothing about a different page. That was the right call on the evidence then available and the wrong answer: the author had a convention in mind, not a per-page verdict, and the reversal is left visible in the plan rather than tidied away. **The gap was narrow because 19L.2 had built for it without meaning to** — the opt-in class existed, the CSS behind it existed, and that page's `refreshExpander()` was the same shape as the lobby's, `currentAnchor()` and `tickOrder` included. Measured at `2d51c18e` and confirmed at build: **zero new CSS rules**, a class on a `<template>`, and a ten-line `markSelectedRows()`. **Placing the call before the empty-selection early return is load-bearing**, not stylistic: after it, un-ticking the last row removes the panel and leaves that row still bracketed — confidently wrong, which is worse than unmarked. It has its own guard. **The 19L.2 guard that asserted the archived page did *not* carry the class was rewritten, not deleted** — its reason (the class is the gate) is still true, so the test keeps its name and its explanation, flips what it expects, and carries the history in its docstring; *a guard that vanishes takes its reason with it.* **5 mutations, 5 caught**, and one of them is the author's own fourth clause made executable: *"action row should not host any pills"* was stated as design and lived only in a comment and in prose until a guard began scanning every `<template>` carrying the opt-in class, on **both** pages. **A planned doc target turned out not to exist** — the manifest committed to "the archived-sessions child page section" of `spec/sessions_overview.md`, and there is none; the behaviour is recorded inline in the lobby's passage, the bullet corrected to say so, and the absence stated rather than pointed at, after a draft had written *"see the archived child page below"* — the same dangling cross-reference `spec-writer` had caught one item earlier, nearly reintroduced by the hand that fixed it. **Not a promotion to a general primitive**: Item 1's question 3 declined that the same day (*"Local lobby"*), and two callers in one surface family is not generality — `spec/ui_elements.md` now says so instead of leaving *"exactly one caller"* to rot. Suite 3,862 → **3,866**. **The `spec-writer` pass then found undeclared doc impact, and it is the third instance of this session's recurring failure and the sharpest**: `guide/archive/new_ux_ideas.md` carried two clauses this item falsified — *"the archived page injects the same panel without it"* and *"the archived sessions page — which injects the same classes and marks no rows — still renders that way"* — and the manifest did not name the file, so nothing would have caught them. Both were written four commits earlier, in a commit titled *"bring entry 1's account of the lobby up to date"*, whose whole purpose was fixing this kind of staleness in this file. True when written, false two items later. *The lesson is not "check that file" but that a statement of present fact about another surface acquires a maintenance obligation the moment it is written, and the manifest is the only place that obligation can be recorded.* Three smaller flags fixed with it: the `spec/sessions_overview.md` bullet still attributed the mechanism to "19L.1, restyled by 19L.2" while its body described 19L.3 at length; its why-opt-in sentence had gone elliptical once both pages carried the class; and `spec/ui_elements.md`'s entry still opened "a selected row on the sessions lobby", correcting itself three sentences later. **Segment 19L stays open.** **UI-visible: verify on the dev slot after deploy.** |
| 2026-09-12 | **Segment 19L Item 4 closed — one NUL byte was making the largest template invisible to `grep`.** `operator/instruments_index.html` line 2048 carried a **literal NUL**, written straight into a JavaScript string as the separator in a composite key (`.join(...)` over a cohort-preview grouping). The *value* was right — a separator that cannot occur in roster data — but written as the raw byte it reclassified the whole file: `grep` treats any file containing a NUL as binary and emits `Binary file … matches` instead of the matching lines. At roughly 3,700 lines this is the repo's biggest template, so **every plain `grep` against it returned something that looked like nothing**. **It cost three wrong findings in one day.** Two were caught on re-check. The third was not: a search for `beforeunload` ran as `grep -rn "beforeunload" app/ spec/ docs/ | grep -v Binary`, and the `| grep -v Binary` — added to tidy the output — **deleted the one line that would have falsified the conclusion**. The search reported nothing, *nothing* was written up as a finding, and a design discussion in `guide/archive/new_ux_ideas.md` was built on a feature's supposed absence, corrected once to a narrower wrong claim, and corrected again only when the author sent a screenshot of the feature working. *A check whose output-tidying step removes the disconfirming evidence is worse than no check: it returns a confident negative rather than a silence.* **The fix is the six-character escape** `\u0000` — identical to every JavaScript engine, identical composite key, file text again. **Verified against the failure rather than a description of it:** `grep -rn "beforeunload" app/` now prints `instruments_index.html:3737` with no `-a`. **Two guards, and the second is the unusual one:** the general assertion (no template under `app/web/templates/` carries a NUL) generalises, while a second pins this line so a future author who "simplifies" the escape back gets a message naming the line and the reason — *a guard that only says somewhere is weaker than one that also says here, because.* A `CLAUDE.md` note telling agents to pass `-a` was rejected as a standing instruction to remember something in place of removing the thing to remember (`constitution.md`, *retire rather than mechanise badly*). **1 mutation, 2 caught.** No behaviour changed and no spec moved. Suite 3,866 → **3,868**. **Segment 19L stays open.** |
| 2026-09-12 | **Assessment snapshots are two lineages, and each retires its own.** Codex contributed `guide/codex_assessment_12sep.md` (#2339), a fresh-context whole-repository read pinned at `00e525e3`. Author's call: **both lineages stay live** — they answer the same question independently, and a reader comparing them wants both reads rather than the later one only, so neither supersedes the other. `guide/README.md`'s retention rule is scoped accordingly: a newer `codebase_assessment_` retires older `codebase_assessment_` snapshots and nothing else, and Codex's one-off row becomes a `codex_assessment_*.md` **shape** row so the lineage is a documented family rather than a single file. **The decision had a defect hiding behind it.** `DATED_DOC` in `tests/unit/test_doc_conventions.py` exempts dated records from the live-prose path-reference check — *a reference that was right when it was written is not a defect now* — and it enumerated `codebase_assessment_` but not `codex_assessment_`. So a dated snapshot was being held to live-prose standards. It passed, which is the uninteresting half: **it passed because none of its 17 path references had moved yet**, while the `codebase_assessment_11sep.md` beside it already carried a dangling one — `guide/inplace_pagination_assessment.md`, archived the previous day — and was correctly exempt from failing on it. One lineage sat a single archiving away from a failure the other was already immune to. **Proved load-bearing rather than assumed:** adding a dangling path to the Codex snapshot passes with the exemption and fails without it, naming the file and line. Enumerated rather than generalised to `assessment_` — a broader token would silently exempt a live document carrying the word, and a new lineage needs a README row anyway, so one line here is the same edit. Suite **3,852** passed, unchanged: this adds no test, it stops an existing one asking the wrong question of a dated file. |
| 2026-09-12 | **The sweep cadence now runs off a corpus sweep, not whichever sweep is newest** (`tools/close_check/_sweep.py`). `last_sweep_date()` took `max()` over every dated `guide/sweep_*.md` with no notion of what each had read, so the **single-file** 2026-09-10 sweep reset the whole-folder clock from **230 merges / 7 days to 61 / 2** (measured at `bb38111f`). Nothing misfired — neither figure is near 500 / 56 — and that is the point: the clock was running from the wrong event, silently, and the error compounds with every later partial sweep. The 09-10 sweep's own opening says the folder-scoped cadence is a separate thing; the tool had no way to know. **The scope cannot be read off the filename.** `sweep_<YYYY-MM-DD>_<scope>.md` puts a scope in the name, but the slug is free text — `spec-docs` and `rrw_functional_spec` are the two live examples — so the document declares it instead, in the comment-marker style the repo already uses for `doc-impact-waived`, `cites` and `path-ref-ok`. An unmarked dated sweep **raises** rather than defaulting: one default would re-create the bug, the other would suppress a real reset. **The template ships the marker set to an invalid placeholder** (`PICK-corpus-OR-partial`), because a template defaulting to `corpus` would make the likeliest mistake — copy it for a narrow sweep, don't touch the marker — silently reproduce the defect. **6 mutations, 5 caught, and the one that escaped is recorded rather than papered over:** setting the live 09-10 sweep's marker to `corpus` reproduces the original bug with every test green. *No tool can check a declaration against what a person actually read* — the fix removes the accidental case and leaves the deliberate one to authorship, and the test that would have hidden that says so at its own definition. **A published number failed to reconcile and was wrong.** #2337's body said the clock moved from **232** merges; re-measured at the commit it was taken from, the figure is **230** (`git log --merges --since=2026-09-05 bb38111f`). The ratio and the argument are unaffected — 230 against 61 is still ~3.8× — but the number was wrong in a merged PR, and it is corrected here in every place this change restates it. Third time this session that a figure refused to reconcile and the re-measurement, not the prose, was right. `guide/README.md`'s retention rule tightens with it: the newest **corpus** sweep is the one that must stay in `guide/`, which is why `sweep_2026-09-05_spec-docs.md` is not archivable and its 51-file backlog is still the next folder sweep's starting point. Suite 3,847 → **3,852**. |
| 2026-09-12 | **`guide/` swept after the 19K close — three settled documents archived, and the two indexes that track them made enforceable.** A read-through of all 14 live `guide/` documents found three whose question was answered: `pill_style_audit.md` (its rule shipped into `spec/ui_elements.md` §9, and its one flagged **defect** — `b3_static_pill`, a label wearing `tag-chip` and inheriting `cursor: pointer` — fixed in 19J.7 rung 1 and now test-guarded), `inplace_pagination_assessment.md`, and `theme_customizer.md`. The other eleven stay: `14B`, `18Q` and `20` are unstarted and externally gated, and the rest are live ledgers. **Two were not archivable as they stood, and the prerequisite is the point.** The pagination assessment held a *settled* decision — a page turn reloads, and the in-place swap is rejected **outright, not deferred** — recorded in no spec; archiving it first would have filed the reasoning under history, so it moved to `spec/ui_elements.md` §10 beside the pager, with the intent argument that decided it (cost argues *not yet* and invites the question back every few segments; intent argues *not at all*). And `deferred_consolidated.md` pointed **into** the theme-customizer plan for its wire-up, the same dependency 19K.9's move existed to remove; the entry is now self-sufficient — and carries the **correct** half of a contradiction the plan shipped with, where one passage says Stretch reads defaults via `getComputedStyle` while two others say the CSSOM with the reason (under an applied custom theme the computed value *is* the custom one, so Reset would reset to what it is undoing). **The four defects the survey found were all one defect.** `guide/README.md` still described the pagination assessment with figures 19K.2 had corrected (*four blocks, ~500 lines* against **one block, 127 lines**) and called its delegation fix outstanding when 19K.2 shipped it; the assessment itself carried the same stale count in its Decision section, **two of three sections updated**; `pill_style_audit.md` had no index row; and `guide/archive/` held **three** files unindexed since August, against a README that says in terms *a file in the directory with no row here is a bug*. Every one is a correction that reached the paragraph it was found in and no further. **So the rule is now a test** (`tests/unit/test_guide_indexes.py`, 5 tests): both indexes must name every document, with the allowed shapes **parsed out of each README's own table** rather than hardcoded, per the constitution's constant-derived-gates rule. **9 mutations, 9 caught — two of them only after the guard was rewritten.** The first version asserted the parse found *at least ten rows*, and that broke the same day, when archiving two documents took `guide/README.md` from eleven document rows to nine: a hardcoded number inside a test whose docstring argues against hardcoded numbers. Its replacement probes for catch-all shapes — and **a mutation escaped it**: `*assessment*.md` matches no probe while silently covering any file with *assessment* in its name, which is exactly how `inplace_pagination_assessment.md` would have gone unindexed. Now a shape may not begin with a wildcard, because a leading wildcard names no family. *A guard against vacuity can itself be vacuous in a narrower range, and only a mutation says where.* Also found: an existing guard, `test_every_path_reference_in_live_prose_resolves`, caught a dead path in prose written **in this same change**. Suite 3,842 → **3,847**. |
| 2026-09-12 | **Segment 19K Item 8 closed — the Guide's operational half has its screencaps, and Segment 19K is complete.** Eight images, four surfaces × two themes, captured from a seeded **9 × 9** session driven through the real import, generate, prepare, activate and `/me/.../save` routes — the plan required real rows, since *an Invitations page with nothing on it would document nothing*. **Two capture decisions changed what the images show, and both came from looking at a first attempt rather than from planning.** *Validate is captured mid-setup, not after activation*: the activated session validates clean, one Info issue and nothing else, but the section's copy describes the **find-and-fix loop**, a pre-activation activity — so the clean image would document the end state rather than the thing the words describe. Re-captured before Generate: two gates, severity chips at 2/0/1/1, two "Fix on … ↗" links. *A screencap is an illustration of a sentence; if the sentence is about a loop, the image has to be taken inside it.* And *the seed was rewritten to show three progress states* — the first had every engaged reviewer part-way, so the table read `IN PROGRESS` nine times with **Completed reviews 0**. Now three submit, two are part-way, four have not started. *A screencap of a single state documents one state.* **A silent identity slip nearly shipped four copies of the wrong page.** The first run reported four 200s and wrote four files — all **193,762 bytes**, all titled *"Your reviews"*. `make_client` installs a **global** dependency override, so the reviewer clients used for seeding left the app authenticated as the last reviewer and the operator fixture rendered the participant surface throughout. **What caught it was the identical byte count, not the assertion.** The capture now re-establishes the operator and asserts each page's title is not the reviewer's and that no two responses are byte-identical. *Four identical sizes is a tell; four 200s is not.* **The screencap guard was mutated in both directions before being trusted**, this rung's whole safety argument resting on it: an unreferenced asset fails (3 tests), a missing referenced asset fails (5), a typo'd filename fails (7). Suite **3,842 passed**, up from 3,810 — the guard is parameterised per asset, so eight files bring 32 assertions. **Two things named rather than hidden.** The capture script is **not** in the repo: it would make retakes cheap — 19J.7's retake question is exactly that problem — but its rendering half needs Playwright, declared in neither `pyproject.toml` nor `requirements.txt`, so CI could not exercise it; recorded as a candidate `tools/guide_shots.gen.py`. And the Responses screencap is **uniform** (`AT RISK · 3/9` on every row) because in a full-matrix session every reviewer reviews every reviewee, so coverage cannot vary unless the reviewers do — the page, not the seed. **UI-visible: verify on the dev slot after deploy.** |
| 2026-09-12 | **Segment 19K Item 8 rung 1 — the Guide's operational half gets its words** (rung 2, the eight screencaps, still to come; the item is not closed). The Guide taught how to *build* a session in detail and how to *run* one in four paragraphs — and running it is the half an operator does under time pressure with real people waiting. **Measured before and after with one parser over the same file:** the operational sections went **32 lines → 75**, with `validate` going **0 → 23**; the setup sections are untouched at 379. Both columns from the same command, one via `git show HEAD:` — deliberate, because this item's own Judgment calls record a sibling quoting two measurements of one quantity, by two methods, disagreeing in one document with nobody comparing them. (The plan's table gave 16/18 where this parser gives 15/17 — a boundary convention, named rather than silently reconciled.) **The author saw a rendered mock-up before a word was written**: the four sections in the Guide's real markup under the app's real stylesheet, both themes, dashed boxes where the screencaps go. *A scaffold-first rule for documentation is cheap to honour when the scaffold can be rendered rather than described.* **Three shape decisions the plan left to the build.** Validate's card is titled **"Check readiness"**, not "Validate" — every other heading is a verb phrase, and a bare page name in that sequence reads like a button rather than a step. Invitations and Responses stay in **one** card, being one job seen from two angles. And **Close / Release moved to the top** of Download responses, since neither the Extract data page nor the participants' own pages do anything useful before them. **Every claim traces to a spec read during the build** — the deep links and read-only body from `spec/validate_page.md`, the eight counters and three coverage counts from `spec/operations_pages.md`, the lens-and-chips framing and the "not an in-app analysis tool" line from `spec/extract_data.md`. None from memory of the pages: this item's first draft was wrong in seven places for writing prose from impressions and formatting it as measurement, and told the build to trust none of it unre-run. The email caveat the spec requires is carried, in the register the Guide already uses for reminders. **Adding a section is three files, and the suite says so before the build does**: registering `validate` in `SECTIONS` and its heading in `SECTION_HEADINGS` without writing the card failed two tests immediately. No `<figure>` was added — `test_guide_screencaps.py` fails on a referenced-but-missing file, so referencing the images before capturing them would correctly break. Suite 3,810 passed. **The `spec-writer` pass then found two false claims in the new copy — the exact failure this item warned itself about, committed anyway.** *"Each issue carries a Fix on … ↗ link that drops you onto the exact row"* is false for **6 of the 16 active rules** — no name, no code, empty reviewers, empty reviewees, no included pairs, no help contact all set no `fix_anchor` and land on the right *page*; and `spec/validate_page.md` §6 already draws that distinction in a file I had read. *"The Workflow card asks you to acknowledge them"* names the wrong element: `acknowledge_warnings` renders in exactly one template, the Validate page body, and the Workflow card only fires the redirect that sends you there. Both are prose written from a fast read and formatted as fact — the register this item's Judgment calls describe, having told the build to *trust none of the prose it did not re-run*. **Reading a spec is not re-running it**: what caught these was counting `fix_anchor` assignments and grepping for the template that renders the control, a minute's work done only when asked. **Two pre-existing spec drifts corrected with the discrepancy named**, both in files the manifest already commits to: `spec/validate_page.md` listed `instruments.stale_generated` as live when its check has been a no-op since Wave 5 PR 5.1 — one line below a row correctly annotated as retired, so the file disagreed with itself; and `spec/extract_data.md` still said *"Setup data lives on Home; response data lives here"*, a split Segment 18R ended by moving the Extract setup card onto this page, with this item's own new note sitting eight lines beneath the contradiction. **UI-visible: verify on the dev slot after deploy.** |
| 2026-09-12 | **Segment 19K Item 10 closed — the palette clears WCAG AA normal in both themes.** The last four failures were **one root cause**: white on `--blue-glow` in dark, the reserved "you can act on this" shade, or on its hover step. The 2.54 read like a transient dip and was not — the same control measured **3.33 at rest**. **The palette had already answered the question once.** `--text-on-amber` is `--white` in light and `--ink` in dark, and `--btn-alert-fg` likewise, because the dark alert fill is bright; the accent family was the lone outlier still carrying white onto a bright dark fill, and that outlier *was* the failure. Inverting three dark mappings — `--btn-primary-fg`, `--selected-fg`, `--text-on-accent` — clears all four at once: **5.33 / 5.33 / 5.33**, and **6.98** for the hover that had been the worst pair in the palette. No primitive moved. **Darkening the fill was rejected with the measurement:** white on `--blue-strong` reaches 5.17 and is the *only* step that works, since `--blue-deep` gives 6.70 for the label but drops the fill to **2.76** against the page — under the 3:1 WCAG 1.4.11 asks of a control boundary, trading a text failure for a boundary one. One usable value, nine dark tokens moved, against three mappings. **The plan's open question was answered by producing what it asked for.** It reserved `--ink` vs `--ink-deep` for "a rendered sample"; the sample did not exist, so both were rendered through `theme_preview.gen.py` in dark mode and put side by side. They are **near-indistinguishable**, so the choice collapses onto the only axis that separates them and `--ink` (5.33) takes it. *An open question that asks for a sample can be closed by making the sample.* The render also confirmed visually what the token table argued: the dark Primary button now reads like Alert and Amber beside it. **The audit's own test sequenced the work.** The inversion did not make the suite pass — it made the both-directions pin fail with *"these now clear AA — delete their `OPEN_SHORTFALLS` entries"*, naming all four with their new ratios. A fix fails until its record is updated, so a stale number cannot outlive what it described. `OPEN_SHORTFALLS` is now **empty and kept**: an empty record is a claim, and the sweep is what keeps it true — deleting the machinery the day it first reads empty is how the next failure goes unnoticed. **19K.7's Tier-2 catalogue guard paid for itself on the very next item**, failing because the spec's dark columns still said `--white` / `#ffffff`. It exists because 19K.7's own collapse left four such rows stale and a review bot found them. 4 mutations, 4 correct — the instructive one being that `--ink-deep` *passes* the AA sweep (4.85) while *failing* the catalogue check, two checks doing two different jobs. Verified in Chromium: the Contrast panel reads **0 open in light and 0 open in dark**, the first time this segment it has shown no red anywhere. Suite 3,810 passed. **The `spec-writer` pass reproduced every ratio and found two stale passages.** The module docstring still read *"Seven pairs still fall under AA: four open, three accepted"* while `OPEN_SHORTFALLS` sat empty sixty lines below — and the block comment *immediately above the dict* had been rewritten correctly in the same commit. **Same file, same failure, three items running.** *The fix is not remembering harder: the docstring and the dict are one claim in two places and nothing checks they agree* — recorded as a candidate rather than fixed here. Worse than stale, the second: `spec/color_tokens.md`'s "Collapsing a tier" closed with *"when a collapse is not available, the value has to move"* — and this item closed those four **without moving `--blue-glow` at all**. The limit was right that no collapse was available and **wrong about what the alternative had to be**. Corrected with the mistake kept visible, because *a rule that names the options it can see will make the unseen one look impossible.* Also upheld: two tests and the `DRIFT` constant are **inert while `OPEN_SHORTFALLS` is empty** — intended, but none said so at its own definition, and a test that proves nothing while looking like a guard is the shape of the vacuity this file has caught twice; all three now name it. **UI-visible: verify on the dev slot after deploy** — a dark primary button changing its label color is the most visible change this segment has made. |
| 2026-09-12 | **Segment 19K Item 9 moved out of the segment — the inline-stylesheet question now lives in `guide/post_azure_todo_checklist.md` item 4.** The item was opened alongside 19K.6–8 as "unblocked work while Azure is outstanding", and that framing was **two-thirds right**: it is unblocked by *provisioning* and gated on **a request the agent container cannot make**. Re-tried 2026-09-12 — the network policy still refuses the dev slot, `403 CONNECT tunnel failed`, logged by the proxy as `connect_rejected`, a policy denial rather than a TLS fault. Its rung 1 is the only first rung in 19K.6–9 that cannot start in the container: 6, 7 and 8 are all "build here, verify on the slot after deploy", while this one needs the number *before* there is anything to build. The item's own `Open questions` had already asked whether it belonged in 19K and answered by **splitting** it — measurement to the checklist, decision staying in the plan. Author's call: split it the rest of the way, **because a segment that cannot close until someone reads a header is held open by something it does not own.** **The body moved rather than a summary of it**, and the checklist entry is self-sufficient by design — a pointer back into a closed segment plan is exactly the dependency this removes. What travelled: the four-page weight table (**157.7 KB** of inline CSS, **64.6–80.5%** of a rendered page, byte-identical across all four, re-sent across 34 templates), the three candidate answers with the "do not land 2 and 3 together" constraint, the `_RevalidatingStaticFiles` `no-cache` caveat, the stale-stylesheet failure class, the blast radius, and the `<style>`-in-a-Jinja-comment finding that 19K.6 hit independently and that `tests/unit/_base_css.py` anchors around. **Retired in place rather than deleted**: the heading and a `Status` block stay so a reader arriving from this file, from 19K.6's cross-reference or from the item numbering finds where it went instead of a gap, and the `Doc impact` heading is **suffixed** — Segment 19A's mechanism — so `close_check` stops reading it as a live manifest. Verified: `close_check 19K.9` now reports *no `### Doc impact` heading*, which is the retirement state, while 19K.1 and 19K.7 still PASS and 19K.8/19K.10 fail only on being open. **19K can now close on items 1–8 and 10.** **Nothing is resolved by this.** The stylesheet is still inline, still 157.7 KB, still re-sent per navigation; the item is relocated, not answered, and both documents say so. |
| 2026-09-12 | **Segment 19K Item 7 follow-up — the review bot found what three checker passes had not, and both findings were mine.** **I reintroduced the staleness bug while fixing it:** the unresolved-row branch clears `below-aa`; I added `accepted-below-aa` and never revisited the branch, so breaking an accepted pair left the blank row wearing its dashed marker and still counted as accepted. Reproduced in Chromium first — accepted stayed at **3** with two rows unresolved, **2** after the fix. *Third time in this item that a correction failed to travel past the line it was made on*, and the first two were written down before this one happened. **And the collapse stopped after `base.html` and the customizer:** `tools/theme_preview.html` was stale by a day and four Tier-2 rows in `spec/color_tokens.md` still read `--green-strong` / `#059669` after `--green-deep` / `#166534` shipped — a reader designing from the catalogue would have designed against values that no longer existed, off a table that looks authoritative. **Both root causes are now checked, which is the part that matters.** `test_every_semantic_token_is_catalogued_with_its_shipped_mapping` does for Tier 2 what the existing check did for Tier 1 — which is exactly why `--slate` was caught the day before and these four were not — over 95 rows, both themes, primitive and hex. And `tests/unit/test_generated_tools_are_current.py` regenerates each `*.gen.py` into a temp tree and compares bytes, with a companion test that fails when a new generator appears uncovered; it regenerates into a *copy*, because a test that quietly fixes the drift it reports never fails twice. That closes a gap this item had **named and left open twice** — "nothing tests generator-versus-output drift" is in its own PR body, and the drift then happened in the next commit. **My own guard failed on a correct page first**: the mechanism test scanned every `classList.toggle` (catching `is-coupled`, `tc-orphan`) and the first `classList.remove`, which belongs to the colour picker. Scoped to `updateContrast`'s body it passes and still catches the revert. 4 mutations, 4 caught; suite 3,810 passed. |
| 2026-09-12 | **Segment 19K Item 7 follow-up — collapsing a tier becomes policy, and closes four of the eight open shortfalls.** *It is fine to solve an AA failure by collapsing a hierarchy, as `--text-dim` into `--text-subtle`* (author). Stated as general policy, immediately concrete: the audit had already put the evidence side by side. Both failing light families carried **two text tiers on one tint, the darker already passing** — `--status-success-fg` was already `--green-deep`, `--status-error-fg` already `--red-deep` — while `--lifecycle-ready-fg`, `--role-reviewee-fg`, `--status-success-accent` and `--lifecycle-expired-fg` sat on the `-strong` tier and failed. One surface, two text colours of one hue, one passing and one failing, for no reason a reader could state. Repointed: **3.32 → 6.29**, **3.95 → 6.80**. `--status-success-border` stays on `--green-strong` (a boundary at 1.4.11's 3:1, which it clears) by the same line that keeps `--decor-muted` outside the text floor; dark was left alone, its mappings differing and already passing. **Light mode now has no open shortfall at all.** The remaining four are dark and are *one root cause* — white on `--blue-glow`, the reserved shade — which is the useful shape: a single question about that shade rather than a mixed bag. It is **not** collapsible, and the policy needs that limit stated beside it: a collapse is available where the palette has already produced the answer; where it has not, the value must move. **The third `spec-writer` pass** confirmed every ratio and found three things. **A stale module docstring** still reading "Eleven pairs still fail and are named, **not excused**" while three had just been excused — the block comment twenty lines below had been rewritten, the file's own opening had not; *a correction reaches the paragraph it was found in and no further unless someone walks it*, the second time this item has recorded that and the second time it caught me. **Size evidence wrong for half the set:** "the ui-v2 pills render at `--fs-tiny`" is true of the four tint pairs and false of the button ones (`body.ui-v2 .btn` is `--fs-small`; `--selected-fg` also renders at 0.8em and at inherited body size). The conclusion held — none reaches AA large's 18.66px — but *citing one measurement for a set you have not checked is how a true conclusion acquires a false reason*. And **nothing asserted the accepted marking**: the panel test checked the red outline only, so the `data-accepted-<theme>` mechanism rested on one manual look, and an accepted pair losing its attribute would render red — the exact failure the split exists to prevent. Now tested per theme, including that a light acceptance does not leak into dark. 4 mutations, 4 caught; suite 3,805 passed. Verified in Chromium: light **0 open + 3 accepted**, dark **4 open + 0**. |
| 2026-09-12 | **Segment 19K Item 7 follow-up — eleven shortfalls become eight open and three accepted.** The author looked at the Contrast panel this item had just built — which is the panel doing its job; it was made to be looked at, and the first look changed a decision — and read the sub-AA pairs as acceptable: anything above 3:1, plus the single 2.54, that one being a hover style. **Two corrections, and then the call stood.** The **2.54 is not a transient dip**: it is the dark primary button on hover, and the same control measures **3.33 at rest**, so it is the worst point of a button already below the line. And **3:1 is not these pairs' line** — the ui-v2 pills render at `--fs-tiny` (0.75rem, weight 500), where AA large wants 18.66px or 14pt bold, so accepting 3.32 for a 12px label is a substantive decision rather than a technicality. So the split went by **transience, not threshold**: three light button labels that dip only under the pointer, against resting states of **7.09 / 5.17 / 4.83**, are accepted; the eight that fail **at rest** stay open, in two families of one decision each — four dark accent pairs resolving through the reserved `--blue-glow`, and four saturated-on-pale-tint pairs sharing two values across the lifecycle, role and status families. **The thresholds did not move**, deliberately: moving the measurement would make the panel stop telling the truth, and a check that flags nothing is the one that rots — the whole subject of this item. What moved is the *record*, and the panel now marks an accepted pair with a dashed edge rather than red instead of falling silent about it. **The acceptance is stored as its premise, not its conclusion.** Each `ACCEPTED_BELOW_AA` entry names the *resting* pair it rests on, and a test fails if that pair stops clearing AA — so darkening a button's resting fill voids the hover exemption automatically. Pinning the hover ratio to a number would have kept the conclusion and discarded the premise, which is precisely how the `--text-muted` entry this item repaired outlived its own reason by four months. Mutation-proved: `--btn-alert-bg` → `--amber-strong` drops the resting pair to 3.19 and the exemption dies with it. **Two mutations were no-ops before they were mutations.** M1 and M2 reported *12 passed*, which reads as a hole in the guards until you notice the `sed` named `--amber-strong` where the file says `--amber-deep` — nothing was changed and the suite was right. Re-run against the real names, both caught. *A clean result and a no-op are indistinguishable until you check which one you have* — the third time this item has said so. Verified in Chromium: light **4 open + 3 accepted**, dark **4 open + 0**. 5 mutations on the new guards, 5 caught; suite 3,804 passed. |
| 2026-09-12 | **Segment 19K Item 7 closed — the muted text tiers collapse into one, and the palette gets a contrast sweep.** `docs/known_limitations.md` had said since **2026-05-18** that `--text-muted` fails WCAG AA. The plan opened on the observation that no such token exists — true today, and **false when the entry was written**: `guide/archive/semantic_tokens.md` records `--slate-soft #9ca3af → --text-muted` in the flat scheme, and the 14A PR 5 row in this file names the token and the ratio. 19C Item 6 renamed the palette and nothing updated the entry, so it was **stale, not fictitious** — and the fault stayed live the whole time at **2.31:1**, roughly half what AA asks. **The shape changed from the Decision, on the author's question.** It had said "the two get separate answers"; asked what collapsing the hierarchy would cost, the measurement said the collapse is cheaper. `--text-dim` is **retired**: its 20 text uses take `--text-subtle`, its 5 live decorative uses take a new `--decor-muted` carrying the *same* primitives, so nothing decorative moves a pixel. **Two errors in the item's own blast radius**, both from running the commands rather than reading them: the `grep 'color: var(--text-dim)'` that counts 21 also matches a `border-color:`, so the split is **20 text / 7 non-text**; and **2 of the 7 were dead code** — `.btn-cta.disabled` painted the token as a *fill* and is overridden on every page by the `ui-v2` rule, which all **34** templates extending `base.html` reach. **The finding is that the scope widened twice, each time because the narrower version was lying by omission.** Checking the fix on the one token under repair found **`--text-link` at 4.22** in dark — a second live failure in the same cluster that four months of an entry naming one token never mentioned; fixed here on instruction, via the existing `--blue-glow-soft`, to **5.53**. Widening to *the Text cluster against every surface* then found nothing more — and was **still wrong**. `--nav-home-bg` (`#e5e7eb`) is darker than any `--surface-*` token and carries muted text on the Session Home anchor: **3.90** before this item, **4.04** after the first fix. A cluster-versus-cluster sweep never reads that pair: its worst case was 4.56, on a *surface*, which passes while the anchor sits at 4.04. Both figures belong to the interim `#667080`, which ships nowhere. *A check whose scope is a token list will pass while the thing it is named for fails.* So the check became a sweep over **every pair the palette forms** — **73 pairs, 146 theme-resolved pairings**, gathered from the rules, from `-fg`/`-bg` name siblings, from text × surface, and from a two-entry map for the foregrounds whose fill no convention predicts — and `--slate` went to `#616874` rather than `#667080`, clearing every pair with margin instead of the enumerated ones by 0.06. **The audit found 12 failures: 1 fixed, 11 recorded.** None is body text — six are white on a mid-tone accent (2.54–3.68, all resolving through the reserved `--blue-glow`), five are saturated text on its own pale tint (3.32–3.95, two shared values). Each is pinned at its measured ratio in **both** directions: a regression fails, and so does a fix, because a fix must delete its entry rather than leave a stale number behind — which is exactly how the entry this item repaired went wrong. **The blast radius omitted `tools/` entirely**: both theme pages are generated from `base.html`, and their generators carry 17 hand-written references the lift does not reach. **Nothing tests generator-versus-output drift**, so all of it was silent. **A vacuity caught in the negative probe.** The first mutation run reported 9 of 9 caught and its one *negative* probe failing. Not a false positive: an earlier mutation had been reverted with `git checkout`, restoring the file to `HEAD` and silently undoing this item's uncommitted edits, so six later mutations ran against a dirty tree. Re-run from a snapshot: **11 mutations, 11 caught by the intended test**, probe silent. *Reverting a mutation with the version-control tool reverts the work too, while the work is uncommitted.* `--text-subtle` also improves `--btn-secondary-border` from 4.83 to 5.61 in passing, and `spec/color_tokens.md`'s "Deliberate couplings" now records that `--text-link`'s coupling to the reserved pair holds in light and is one step off in dark. The stylesheet parser moved to `tests/unit/_base_css.py`, `base.html` having acquired a third test-side reader. **The audit is now inspectable, not only enforced.** The 11 shortfalls were recorded in four documents; the **135 passing pairings were recorded nowhere**, so the audit could be enforced but not looked at. The theme customizer's Contrast panel existed for exactly this and carried **12 hand-listed pairs** — the same defect one level up, missing 62 of the palette's pairs including the `--nav-home-bg` one that was failing. (The `spec-writer` pass corrected a compression here: that list had *also* named `--text-dim`, but **this item's own earlier commit had already removed that row**, so the twelve actually replaced were correct-and-incomplete rather than drifted. Two ways a hand-kept list decays, a few commits apart, and worth not merging into one.) It now renders a row per pair from the *same* `collect_contrast_pairs` the test imports, sub-AA **outlined in red**, with live counts, the shipped ratio beside the live one, and each pair's source in its tooltip. Flagging is computed in the browser, so grouping is structural rather than pass/fail — a static "failures first" layout would be wrong the moment you remap — and **a test asserts the generated page carries a row for every audited pair**, which is the generator-versus-output drift this item found nothing tests. **Verified in Chromium rather than asserted:** 73 rows, 0 unresolved, **7 outlined light + 4 dark = 11, exactly `KNOWN_SHORTFALLS`**; repointing `--text-subtle` back to `--gray` moves it to 17 and restoring returns 7. Playwright is installed in the container but declared in neither `pyproject.toml` nor `requirements.txt`, so this cannot become a test without a dependency decision — 19K.6's finding again — and the two new tests assert the mechanism and say so. The first render ellipsised labels to `subtle → ti…`, so a reader could see a red 3.32 and not what it belonged to; only looking caught that. **The pass also found a real staleness bug in the panel's own JS**, which is the finding worth keeping: when a remap leaves a token unmapped or cyclic, `resolve` returns null and the row was **skipped**, keeping its last-good ratio *and* its last-good red outline — a stale row reading as current, in precisely the case where recomputing is the point. Reproduced against the pre-fix page (unmap a token carrying one of the 7 outlined rows: count stays 7), then fixed and re-verified (count drops to 6, the row clears to `—` with a dashed `?` badge). *The promise a tool makes in prose is the one place to look for the case it does not handle.* **UI-visible: verify on the dev slot after deploy** — the suite cannot see a rendered colour. |
| 2026-09-12 | **Segment 19K Item 3 closed — the Invitations / Responses N+1, decided and fixed.** The item was *to decide*, and "defer with a trigger" was a permitted answer; the re-measurement made the decision easy. **Re-taken on `main` with paging in place, every query count is identical to 19J.4's at all four roster sizes** — 708/1,332 at 25×25 up to **40,433 / 80,432 at 200×200** — so paging changed the HTML and not the work behind it, which the plan had asserted "by design" and is now measured. Three things the re-measurement added. **Responses' second pass is exactly half of it:** `summary_counts` costs **40,404 of the 80,432** queries and contributes one integer, `incomplete_count`. **That integer is not cheap another way** — `pill_state` bottoms out in per-assignment completeness, so the obvious win, a scalar `COUNT`, is not available; checked before it was recommended. And **it is two lines, not a diffuse problem**: attributing every query to its call site, `responses/_core.py` and `monitoring.py` account for ~99% of both pages, and the 7 other occurrences of the same query shape never fire here. That is what made the fix cheap — a prefetch *inside* those two functions changes no signature a caller passes, so the four-caller blast radius the plan worried about does not apply. One query per session in place of one per assignment: **40,433 → 434 and 80,432 → 434** at 200×200 (93× and 185×; 6.9 s → 2.3 s, 12.6 s → 2.5 s). Not flat like Assignments' 43 — the residual is roughly two queries per reviewer — but **linear in the roster where it was quadratic**, and the remainder is recorded rather than chased. **The finding: two vacuous tests, the same mistake twice in one item.** The equivalence test seeded no responses, so it compared empty against empty and **passed against the one mutation it existed to catch**. Then the scoping test — added *because* a mutation had escaped — made the identical mistake one level along, seeding a second session with no rows to leak. *A fixture that produces no data makes every assertion about that data true*, and writing a test after a mutation escapes does not stop it happening again; only re-running the mutation does. One mutation was **reclassified rather than fixed**: dropping the `session_id` filter is not a correctness bug, since assignment ids are globally unique, but a performance one — it would read every response row in the database to render one page — and it is guarded as that. 6 mutations, 6 caught. **The `spec-writer` pass ran *before* the push for the first time** (the three prior items ran it in parallel and each collected corrections after merging). It caught a docstring that said the old lookup "scaled linearly" when the point is that it scaled with the *square* of the roster; `guide/todo_master.md` still listing this item as open and `guide/codebase_assessment_11sep.md` still calling the N+1 "measured and unfixed" — a snapshot overtaken a day after it was written, now struck and annotated; and an ordering slip. It also **verified what the item had only reasoned about**: the last-write-wins concern in `by_field` is moot, `Response` carrying a unique constraint on `(assignment_id, response_field_id)`. Eight minutes, and this item merged correct instead of collecting a follow-up. |
| 2026-09-12 | **Segment 19K Item 6 closed — a same-specificity tie in `base.html` now fails a test.** During 19J.9 `body.ui-v2 `.table-pager-step`` was **dead from rung 1 until the fifth of that item's seven merges**: it and `body.ui-v2 .btn-icon` are both (0,2,1), `.btn-icon` sits later, and the loser's declarations happened to match what the winner already set, so nothing showed. It surfaced only when someone asked for a bigger glyph and the glyph did not change. `tests/integration/test_cascade_ties.py` resolves the cascade **in Python** — rendered class sets against parsed rules — and fails when a canonical class's declaration is dead. **The method changed from a browser on a prerequisite check**: the suite had zero browser tests, neither CI workflow installs one, and `playwright` is declared nowhere, so a computed-style test would have been green in the container and absent from CI — *the prerequisite that matters is not "can I run this?" but "can CI?"*. **The check found a live tie on its first run and improved the design rather than the stylesheet**: `.table-pager-cluster` and `.table-pager-cluster-bottom` tie on `display`, and the `-bottom` override is deliberate and commented. That gave the discriminator the plan lacked — a variant that comes *later* and wins is the idiom; a specialisation that comes *earlier* and loses is 19J.9 — with both directions pinned, since suppressing variants outright would hide the defect the item exists for. Nothing in `base.html` changed. **Two vacuity findings.** With an empty session the fixture collected 46 class sets and **not one** carried `btn-icon`: the pager renders only above the page size, so the end-to-end check would have passed against 19J.9's own collision. And the `@media` test pinned nothing — with one rule in the block the parse drops it either way; a *second* rule is what leaks. The coverage guard is now a separate named test, because a mutation showed an assertion living inside the check it guards can be deleted invisibly. **9 mutations on the resolver, 9 caught**, plus the mandatory one: 19J.9's collision reintroduced into the real `base.html` fails the check, naming both rules and both dead properties. **The `spec-writer` pass then found a false positive** — the one kind this check cannot afford. Given two tied rules where the *earlier* carries `!important`, real CSS gives it the win and `find_ties` called it dead, contradicting the module's own promise that *none of its exclusions can make it report a tie that is not there*. Fixed rather than documented, with both directions pinned, since skipping every `!important` property would silence a real defect; `base.html` has **zero** today, so the case was latent until constructed. It also corrected a count that implied more coverage than it had — the check resolves **236** selectors, not the 253 first claimed, which had included the 17 inside `@media` that the parser discards on purpose — and **disconfirmed** this item's own leading suspicion, that the element regex over-counts inside hyphenated class names. It does not. |

**Rows dated 2026-09-11 and earlier live in `docs/status_history.md`** —
moved there on 2026-09-19, verbatim and unabridged, when this file
reached 1,260 lines. Nothing was summarised away: the older table is a
file rather than a paragraph.


---

## Segments shipped

<!-- path-ref-ok: section -->

| Segment | What it added | Completed |
|---|---|---|
| 1 | Repository skeleton, `/health`, local dev install | 2026-04-27 |
| 2 | Azure App Service deployment via OIDC | 2026-04-27 |
| 3 | Microsoft Entra ID sign-in via Easy Auth | 2026-04-27 |
| 4 | 12-table schema + Alembic migration infra | 2026-04-27 |
| 5 | Postgres provisioning + migrate-on-deploy + operator session CRUD-lite | 2026-04-28 |
| 6 | Reviewer / reviewee CSV imports + setup validation | 2026-04-28 |
| 7 | FullMatrix + Manual assignment generation + roster Manage views | 2026-04-28 |
| 8 | Reviewer dashboard + review surface (save / submit / clear / cancel); active-only roster filter retrofit | 2026-04-29 |
| 9.1 | Session activation lifecycle (draft↔ready), edit-lock, per-instrument open/close, response-window gates | 2026-04-29 |
| 9.2 | Invitation generation + dev email outbox + `/me/invite/{token}` landing route | 2026-04-29 |
| 9.3 | Per-session monitoring page + per-row and bulk reminder send | 2026-04-29 |
| 9.4A | Global page chrome (app identity + user card + breadcrumb), `/about` stub, sessions list per-row Access/Delete + Create-new-session button | 2026-04-29 |
| 9.4B | Session detail four-card layout (Session / Session setup / Run Session / Danger zone), inline validate-summary card via `?validated=1`, `POST /delete-data` with `responses.deleted_all` audit event | 2026-04-29 |
| 9.4C | Reviewers / reviewees / assignments Manage pages with anchored Upload-CSV cards and disabled Edit buttons; Assign by Rules placeholder card; `/operator/sessions/{id}/instruments` index page; `/operator/sessions/{id}/setupinvite` stub; setup-table Manage buttons for Instruments and Set up invites enabled | 2026-04-29 |
| 9.5A | `validated` stored state in `SessionStatus` (between `draft` and `ready`); `GET ?validated=1` flips draft→validated when no errors; activation now requires `is_validated`; setup-mutating routes (reviewer/reviewee/assignment import + delete-all + assignment generate + session edit) flip validated→draft via dedicated `session.validated` / `session.invalidated` audit events; instrument open/close/visibility and `/delete-data` deliberately do not invalidate | 2026-04-29 |
| 10A | Consolidated `/operator/sessions/{id}/instruments` page: per-instrument card with friendly description, acceptance + visibility toggles, response-fields table (add / edit / delete / reorder, per-field help text + visibility), session-wide Instruments Settings card with bulk Open all / Close all toggles. Migration adds `help_text` (Text, NULL) and `help_text_visible` (Bool, default true) on `instrument_response_fields`. Reviewer surface refactors to loop-by-instrument with section heading from `Instrument.description` (fallback to system handle) and a per-field help block above each table. Empty-instrument validation now blocks activation. Description / field mutations invalidate `validated → draft` via `lifecycle.invalidate_if_validated()` called from inside each mutating service (post-PR for items #3 + #16, 2026-05-02); bulk accepting + per-instrument open/close/visibility deliberately do not invalidate. Body width bumped from 900px to 1400px globally with a `.table-scroll` overflow utility. | 2026-04-30 |
| 10B-1 | Backfill migration (`c2143bd329c7`) seeds three `InstrumentDisplayField` rows (`source_type='pair_context'`, `source_field='1'|'2'|'3'`, `label=''`, `order=0..2`, `visible=true`) on every existing instrument; destructive within that filter (operator-typed labels on those slots are not preserved across upgrade); operator-added `reviewee` rows left intact. `ensure_default_instrument` seeds the same three rows on new sessions. Reviewer surface renders pair-context values as separate columns sourced from the display-field rows (no longer inline in the identity cell); reviewee identity (name + email) is the always-first column, mandatory and non-toggleable. New service helpers `display_field_label(field)` and `display_field_value(field, assignment)` cover the seven D6 sources (`reviewee.tag_1/2/3`, `reviewee.profile_link`, `pair_context.1/2/3`); empty/NULL labels fall back to inferred strings. `profile_link` cells render as plain `<a>`. No operator UI yet (picker + bulk form land in 10B-2; preview route in 10B-3). No new audit events. | 2026-04-30 |
| 10B-2 | Per-instrument display-fields card on `/operator/sessions/{id}/instruments`: Add (combined source picker over the seven D6 sources minus those already on the instrument; colon-delimited values like `reviewee:tag_1`), inline Edit (label override + visibility), Delete (no cascade-confirm — display fields carry no per-row dependent data). New shared "Field order & visibility" bulk form covering both display + response fields, interleaved in operator-chosen order, with per-table independent repack to `0..N-1` on save. Four new audit events: `instrument.display_field_added`, `instrument.display_field_updated`, `instrument.display_field_deleted`, `instrument.display_fields_saved` (D11 diff shape; `added` / `removed` always empty since adds + deletes are row-level only). Reuses 10A `instrument.fields_reordered` when bulk save reorders response fields. Display-field mutations invalidate `validated → draft` and 409 when `status=ready` (mirrors 10A). Rank-based change detection on bulk save means submitting current state is a no-op. | 2026-04-30 |
| 10B-3 | New `GET /operator/sessions/{id}/preview` route renders the reviewer surface in operator-only preview mode — pads with up to three synthetic rows (`Sample Reviewee 1/2/3`, `sample1@example.edu`, …) when fewer real assignments exist; bypasses session-status / deadline / acceptance gates; all inputs render disabled (via the existing `accepting=False` template branch); save / submit / clear / cancel forms suppressed via a single `preview_mode` template flag; "Preview — not visible to reviewers" banner at the top. Two operator-side entry-point anchors at ship time (instruments page header + session detail's Run Session card); the instruments-page anchor was disabled in 10C. No new audit events (read-only — also skips the `lifecycle.observe_deadline` lazy-close side-effect). Completes Segment 10B. | 2026-04-30 |
| 10D | Instruments-page rebuild taking the per-instrument card from frame to fully-functional. Slice 1 wired the Display Fields table + URL-driven `?editing={iid}` Save / Cancel / Edit state machine on every per-instrument card (with two locked Name + Email rows, inline Friendly Label edit, ▲/▼ reorder, and operator-defined visibility on the rest). Slice 2 reused the same state machine for the Response Fields table (inline label + Required edit, ➕ row-level Add via JS-deferred `<template>` clones bound to the bulk-save form, ✗ row-level Delete via queued hidden inputs, ▲/▼ client-side reorder). Slice 3 wired Response Fields Help (per-row textarea + Show checkbox) into the same bulk-save round-trip. Slice 4a introduced the `response_type_definitions` table (10 seeded rows per session: `Long_text` / `Short_text` / `Yes_no` / `Grade` / `Likert5` / `100int` / `0-to-2int` / `1-to-5int` / `1-to-5half` / `1-to-5dec`), migrated `instrument_response_fields.response_type` (text) into `response_type_id` (FK with `ON DELETE CASCADE` + SQLite `PRAGMA foreign_keys = ON`), and rendered the new Response Type Definitions card read-only with the Response Fields Type cell as a `<select disabled>` over RTD names. Slice 4b shipped operator-add / -edit / -delete on operator-defined RTD rows with the cascade-on-delete confirmation banner; Min / Max / Step / List stay editable post-create with `update_response_type_definition` re-deriving every dependent RF's `validation` block on save. Slice 4c wired Response Fields ↔ RTD on add (Type `<select>` is enabled on JS-deferred new rows so operators can pick from the RTD catalog; saved rows stay locked per spec). Slice 4d closed the cross-cutting consistency gaps: per-instrument and RTD card editing state machines are mutually exclusive; bulk-save refuses to commit an instrument with zero Response Fields; cascade-delete that would empty an instrument is hard-blocked with a banner naming the affected instrument(s). Banner-convention follow-ups added a Cancel button + auto-scroll-on-display + Cancel-returns-to-source-row to the new error / cascade banners and pinned the convention into `spec/domain_assumptions.md`. Slice 5 enabled multi-instrument support: `Add new instrument` + `Delete this instrument` are wired through their existing POST routes, with native `confirm()` on Delete; both buttons share an `is_ready` / mutual-exclusion / single-instrument disable gate matching the per-instrument Edit button. The action row at the bottom of every per-instrument card is a `.bottom-grid` of two half-width cards: an invisible left card hosts Save / Cancel / Edit and Add new instrument together on a right-flushed row (state-machine pair sits immediately to the left of Add); a red-bordered, white-interior Danger Zone right card hosts Delete this instrument (also right-flushed) with cascade warning copy. Post-Slice-5 polish PRs (#262 → #268) landed the half-card layout, white inner Danger Zone background, tightened cascade copy, restyled `Add a Response Type` as a proper half-width card with a single inline Name + Data Type + Add row, and right-flushed the per-instrument Delete button. | 2026-05-02 |
| 10C | Operator UI clean-up consolidating the post-10B surface: every session-scoped operator page renders a 6-button **setup nav** header card (Session / Reviewers / Reviewees / Assignments / Instruments / Email Invites); session detail adopts a `.page-grid` two-column layout (Session Details / Session Setup / Run Session) with Danger Zone in `.bottom-grid`; the inline session-detail revert form is replaced by a reusable yellow lock card pattern (with `return_to` allowlist `{reviewers, reviewees, assignments, instruments}`) shared across the four mutating setup pages; sessions list adds a `Created by` column; reviewers / reviewees / assignments pages standardise on info-card + status-pill rows + `#upload-csv` anchored card + Danger Zone, with upload + Danger Zone hidden while locked. Instruments page restructured: All Instrument Status full-width card carries three pill rows + bulk Open/Close + bulk Show/Don't-show + a disabled Preview button; per-instrument card uses pastel-tint cycling, a top `.bottom-grid` (description + per-instrument status), a `.field-builder` `.bottom-grid` of Display + Response Fields half-cards, and a live client-rendered Preview Instrument table; bottom button row (Back / Save / Edit / Add an instrument / Delete) with a JS-only Save/Edit `field-builder.locked` toggle. Response Fields gains inline label edit (per-row hidden form via HTML5 `form=` attribute), Required auto-submit, row-level Add (`/fields/add-row`) + Delete; Type stays read-only by design. Display Fields renders a hardcoded 6-row CSV-named placeholder; persistence is deferred. Multi-instrument data layer fully shipped (`Instrument.session_id`, `order`, FK cascades, `create_instrument` / `delete_instrument` services + routes + `instrument.created` / `instrument.deleted` audit events) with the operator UI behind a disabled Add button; Delete is reachable when more than one instrument exists. Bulk visibility toggles emit `instruments.bulk_visibility_when_closed`. Cross-cutting primitives in `base.html`: `.page-grid`, `.bottom-grid`, `.card-tl/r/bl/l/tr/br`, `.setup-nav`, `.setup-grid`, `.btn-row` / `.btn-pair`, `.fill-col`, `.col-shrink`, `.session-meta-row`, `.session-status-row`, `.field-builder` (+ `.locked`), `.display-edit`. `.btn[hidden]` honours the standard hidden attribute. | 2026-05-01 |
| 11A | Tier 1–3 cleanup punch list across Segments 1–10. Code shipped: shared `_parse_email` helper for CSV (#314), CSV cross-table identity check (#315), reviewer-surface polish batch (heading display logic, help text inline, photo View link, column-width hints, status-column hide-when-empty; PRs #319 → #324), `correlation_id` threaded into deadline lazy-close (#329), `invitations.py` decoupled from FastAPI `Request` (#330), `get_or_create_default_instrument` docstring refresh (#309). Decisions recorded: AG Grid → Segment 15; queue-based batch invitations → Segment 15; help-contact merge field source = per-session column on `ReviewSession`; CSRF → rely on Easy Auth + `SameSite=Lax` cookies (`docs/authentication.md`). UI: v2 chrome rebuild rolled out across the session-centric pages (`session_reviewers.html`, `session_reviewees.html`, `session_assignments.html`, `session_invitations.html`, `session_monitoring.html`, `session_outbox.html`, `session_validate.html`, `session_setupinvite.html`, `session_previews.html`, `session_detail.html`, `instruments_index.html`, plus the `session_setup_status_row.html` partial). Two follow-on Tier 4 items remain open (#21b non-session-centric v2 sweep; #24 operator-editable email template editor). Plan: `guide/archive/segment_11A_cleaning_up_unfinished_business.md`. | 2026-05-03 |
| 11E | Operator-editable email template editor + per-operator SMTP scaffolding. PRs #461 (PR 1 — schema + service-layer renderer), #462 (PR 2-A — placeholder cards), #463 (PR 4 — operator Settings page + encrypted credential storage), #464 (PR 5 — `EmailTransport` Protocol + `SmtpEmailTransport` + typed-stub `GraphEmailTransport`), #465 (PR 2 — editor UI), #468 polish (button consistency + Settings `?return_to=` plumbing), **#532 (PR 6 — responses-received template editor third tab)**. New columns `sessions.help_contact` (String 320, nullable) + `sessions.email_template_overrides` (JSON, nullable; recognises invitation / reminder / responses_received subject / body / cc / bcc keys plus the `responses_received_enabled` bool toggle) + seven `users.smtp_*` columns. New `app/services/email_templates.py` (`render_invitation` / `render_reminder` / `render_responses_received` over per-template merge-field sets — invitation / reminder share the canonical five tags, responses-received drops `$invite_url` and adds `$submitted_at` resolved via the reviewer's responses), `app/services/operator_settings.py` (`EmailSettings` dataclass, `get_email_settings` / `save_email_settings` / `clear_email_settings`), `app/services/_secrets.py` (`cryptography.fernet`-backed encrypt / decrypt keyed off `SMTP_ENCRYPTION_KEY`), `app/services/email_send.py` (transport abstraction + SMTP backend). Editor at `/operator/sessions/{id}/setupinvite` with per-template selection, per-field "Reset to default", `email_template.updated` / `email_template.reset` audit events; the responses-received tab carries an extra "Send this confirmation when a reviewer submits." checkbox backed by the `responses_received_enabled` flag. Settings at `/operator/settings` reachable from the chrome user-menu, with `?return_to=` honoured per `app/web/return_to.py`. Reviewer surface picks up a per-session `Questions? Contact <X>` line. **Nothing sends real mail yet** — outbox rows still write `status="queued"`; **Segment 14B Part A** is the first call site for the transport interface. Spec at `spec/email_infra_options.md` for the broader transport landscape (Options A / B / C / D — SMTP, Microsoft Graph application permission, Azure Communication Services, third-party transactional). Plan: `guide/archive/segment_11E_email_template_editor.md`. Closes `guide/archive/unfinished_business.md` #24 (editor side). | 2026-05-07 |
| 12B | **Audit-events export** — smallest possible slice after 12A absorbed the response-data export. 1 PR **#788** + follow-on relocation PR **#789**. **PR 1 (#788)** new `app/services/extracts/audit_events_extract.py` with `serialize_audit_events()` — 8-column wide CSV (`EventType` / `Severity` / `Summary` / `ActorEmail` / `CorrelationId` / `CreatedAt` / `DetailJson`) with the canonical Segment 11K detail envelope JSON-encoded (`json.dumps(..., sort_keys=True)`), LEFT JOIN against `users` for ActorEmail, streamed via `yield_per(1000)`. `session.audit_log_extracted` registered in `EVENT_SCHEMAS`. `GET /operator/sessions/{id}/export/audit_log.csv` route. New Audit log tile in the Extract Data card. **PR 2 (#789)** moves the operator-facing surface out of Extract Data per industry best practice (GitHub / Stripe / Slack / Notion / Atlassian put audit data behind an admin / diagnostics doorway, not alongside everyday data exports). Route + service + audit event + 13 tests stay live; the operator-facing tile relocates to the Sys Admin page when Segment 16A ships. Segment 16A stub (`guide/archive/segment_16A_sys_admin_page.md`) upgrades audit log download from "Future" to a planned **Anchor item §3**. Plan archived: `guide/archive/segment_12B_audit_retention.md`. | 2026-05-10 |
| 12A-3 | **Export / import updates for 15D** — last leg of the locked sequence `13E → 12C → 15D → 12A-3`. 4 PRs **#779 / #780 / #782 / #783** plus polish PR **#781**. Brings Extract Data + Quick Setup into alignment with the post-15D session model. **PR 1 (#779)** Relationships export + Extract Data tile — `serialize_relationships()` extract service, `/export/relationships.csv` route, `session.relationships_extracted` audit event, new "Relationships" tile in the Extract Data card. **PR 2 (#780)** Assignments-CSV retirement sweep — drops the Extract Data tile, `/export/assignments.csv` route, `assignments_extract.py` service, `session.assignments_extracted` registration end-to-end. Reorders the Extract Data row list to lock in the target left/right column layout (Reviewers · Settings · Reviewees · Responses · Relationships · Zip-all). **Bonus #781** — Reviewers / Reviewees / Relationships / Responses Download buttons grey out when the count is 0. **PR 3 (#782)** Settings importer + route — `apply_session_config(db, session, rows) -> ApplyResult` in `session_config_io.py` (the inverse of `serialize_session_config`). Two-phase parse + apply (validate every row first, then wipe-and-replace). `POST /operator/sessions/{id}/import-config` route with the lifecycle gate; `session.settings_imported` audit event. Round-trip is byte-stable on the export's own output. Round-trip stability fixes folded in: `_datetime` formatter normalises naive readbacks to UTC; the importer's RTD `data_type` validation accepts both lowercase tokens and the model's capitalized values. **PR 4 (#783)** Quick Setup Settings slot graduation — flips slot 4's `is_wired=True`, points it at PR 3's route. Submit-all chain on Session Home runs reviewers → reviewees → relationships → settings; the Create New Session POST handler dispatches the same per-slot pipeline when uploads are staged on the new-session form. Plan archived: `guide/archive/segment_12A-3_export_import_updates.md`. The previously-planned 12A-2 (Settings CSV import) was absorbed into 12A-3 PR 3 — its plan also archived: `guide/archive/segment_12A-2_import.md`. | 2026-05-10 |
| Post-Segment 15 clean up | Bundle of small UI / behaviour polish PRs **#760 → #769** on top of the freshly-shipped 15D, plus bookkeeping PRs **#770 → #773**. Highlights: Relationships page mirrors Reviewers / Reviewees with a single-line stats card (`Number of pairwise relationships: N · Fields with data:` pills); Setup status row drops the **Assignments:** slot; Operations Assignments page picks up its layout polish (chrome moves Assignments between Validate and Previews, Rule Based card retitled **Assignment Rule** and lifted into a half-width slot in `.bottom-grid` next to the half-width Self-reviews card, "Current pairs" → "Assignment pairs", "Pair" Ctx-toggle group label → "Relationship"); Quick Setup card on Session Home + Create New Session goes two-column (Reviewers + Reviewees left, Relationships + Session settings right); seeded RuleSets default `excludeSelfReviews` to **false** (Migration `d92f4a710e88` flips `rule_set_revisions.exclude_self_reviews` for revisions belonging to seeds; Personal forks untouched); Rule Builder "+ New blank RuleSet" picks up the same `false` default; Reviewers / Reviewees / Relationships / Assignments tables wrap their trailing `status` (or `Include`) cell in a `pill-info` (active / yes) / `pill-empty` (inactive / no) span. Bookkeeping PRs **#768 / #770 / #771 / #772 / #773** archive the shipped 13E / 12C-1 / 15D plans, refresh `guide/todo_master.md` (Done + Upcoming), and bring `CLAUDE.md` / `AGENTS.md` / `README.md` / `docs/status.md` in line with the post-15D world. | 2026-05-10 |
| 15D | **Assignments revamp** — Pair Context becomes Setup-primary, Assignments table becomes always-derived. 10 PRs across the 8-PR plan with PR 6 split 6a / 6b and PR 7 split 7a / 7b / 7c (PR 8 carved out into Segment 15E). PRs **#749 → #758**. **PR 1 (#749)** new `app/services/relationships.py` with `parse_relationship_csv` / `save_relationships` / `delete_all_relationships` / `existing_count` / `list_for_session` / `pair_context_lookup`. **PR 2 (#750)** Relationships Setup page (`/operator/sessions/{id}/relationships`) + chrome integration between Reviewees and Instruments. **PR 3 (#751)** `pair_context.tag_N` rule grammar + UI surface — new field-source class in `app/services/rules/fields.py`; Rule Builder picker exposes the three tags. **PR 4 (#752)** engine consumes `pair_context` via eager lookup — ContextVar-scoped `_pair_context_lookup` dict pre-built once per `engine.evaluate` call so the predicate evaluator dodges N×M re-queries. **PR 5 (#753)** Alembic data migration backfilling existing `Assignment.context.pair_context_*` JSON values into `relationships` rows; lazy-seeding hook moves to `save_relationships`. **PR 6a (#754)** Operations Assignments page + chrome restructure — manual upload card retired; new bulk Include toggle for self-reviews; chrome row label moves from Setup to Operations. **PR 6b (#755)** drops `Assignment.context` JSON column; pair-context readers rewritten to consult `relationships` directly; round-trip-friendly downgrade re-creates the column nullable. **PR 7a (#756)** retires the legacy Quick Setup Assignments slot. **PR 7b (#757)** dev-only docstring labels on the manual-CSV path (route still exists for test fixtures). **PR 7c (#758)** re-introduces a Quick Setup Relationships slot at position 3 (file-upload only); the chain is now Reviewers → Reviewees → Relationships → Settings. New audit events `relationships.imported` / `relationships.deleted_all` registered in `EVENT_SCHEMAS`. PR 8 (Validate + Generate / + Activate "super buttons") carved out into Segment 15E. Plan archived: `guide/archive/segment_15D_assignments_revamp.md`. | 2026-05-10 |
| 12C-1 | **Self-review revamp** wiring the 13E PR 1 column. 3 PRs **#745 → #747**; sub-segment 12C-1 only — 12C-2 + 12C-3 deferred / absorbed into 15D under the holistic-sequence revision. **PR 1 (#745)** `replace_assignments` consults `sessions.self_reviews_active` for self-review pairs when no explicit `includes` mapping is supplied; new `set_self_reviews_active` writer + `self_review_include_breakdown` reader. **PR 2 (#746)** Rule Builder card surfaces the `exclude_self_reviews` checkbox so it's editable per RuleSet rather than only at generate time. **PR 3 (#747)** full-matrix dead-code cleanup — retires the standalone Full Matrix card / route (the seeded `Full Matrix` RuleSet covers the same case from inside the Rule Based Assignment card). Plan archived: `guide/archive/segment_12C_self-review_revamp.md`. | 2026-05-10 |
| 13E | **DB prep for the 12C / 15D block.** 2 PRs **#743 → #744**. Mirrors the 13D playbook (additive, nullable / DEFAULT-shaped, no-backfill). **PR 1 (#743)** `sessions.self_reviews_active` Boolean column, default `FALSE`; lands inert (12C-1 PR 1 is the first reader / writer). **PR 2 (#744)** new `relationships` table with `(session_id, reviewer_id, reviewee_id)` unique constraint, three `tag_N` slots, and `status` enum (`active` / `inactive`); lands inert (15D PR 1 is the first writer). Plan archived: `guide/archive/segment_13E_db_prep.md`. | 2026-05-10 |
| 12A-1 | **Session export** — five CSV downloads off the Extract Data card on Session Home. 5 PRs **#713 / #716 / #717 / #718 / #721** plus PR 4a polish. Splits the umbrella "Segment 12A" into export (this segment) and import (12A-3 under the locked block). **PR 1 (#713)** Settings export + shared `extracts/` plumbing; new `app/services/session_config_io.py` + `app/services/extracts/__init__.py` with `stream_csv` + `filename({code}_{kind}.csv)` helper; new `_extracts.py` route slice; `session.settings_extracted` audit event. **PR 1a (#716)** seeded-RuleSet audit-log fallback for pre-15B sessions. **PR 2 (#717)** Reviewers + Reviewees extracts; column shapes match the import importers so files round-trip. **PR 3 (#718)** Manual assignments extract; raises `ManualOnlyError` on `assignment_mode != "manual"`; route returns 404 for non-manual sessions; multi-instrument N×M fanout. **PR 4 (#721)** Responses extract — 19 cols streamed via `yield_per(1000)` cursor. **PR 4a** adds `SelfReview` column to the responses extract (uppercase `TRUE` / `FALSE`, Excel idiom; computed via the canonical `is_self_review(reviewer, reviewee)` helper). New audit events `session.{settings,reviewers,reviewees,assignments,responses}_extracted` registered in `EVENT_SCHEMAS`. **Segment 13A-2** (small DDL follow-on, PR **#711**) shipped 2026-05-09 — adds `uq_session_rule_set_session_name` constraint on `session_rule_sets(session_id, name)`, mirroring the parallel constraint already on `response_type_definitions`. Plan archived: `guide/archive/segment_12A-1_export.md`. | 2026-05-09 |
| 13D | **DB prep for the operator RTD / RuleSet library / per-session-copy split** plus 13B / 13C ride-along columns. 7 PRs **#696 → #702**. Every migration shipped inert — no service or web code reads or writes the new shape until its owning feature segment lights it up. **PR 0** (#696) renames `rule_sets` → `operator_rule_sets`. **PR 1** (#697) adds `session_field_labels` table (15A friendly-label resolver). **PR 2** (#698) adds `session_rule_sets` snapshot table (15C per-session RuleSet copies). **PR 3** (#699) adds `operator_response_type_definitions` library table + `response_type_definitions.library_origin_id` provenance pointer (15C). **PR 4** (#700) adds `instruments.rule_set_id` nullable FK targeting `session_rule_sets` (ON DELETE SET NULL) for 15B per-instrument selection. **PR 5** (#701) adds `instruments.sort_display_fields` JSON column for 13B. **PR 6** (#702) adds `instruments.group_kind String(32) NULL` column for 13C. Established pattern (mirror of 11C Part 2) lets 15A / 15C / 15B / 13B / 13C land without authoring any migrations of their own. Plan archived: `guide/archive/segment_13D_db_prep.md`. | 2026-05-09 |
| Major refactor §12 | Three large monoliths split into per-concern packages with re-export walls (callers stay byte-identical), plus a hygiene bundle and a test-file split. **20 PRs #663 → #683**. **§12.A** (PRs #663 → #667) split `app/services/instruments.py` (2,469 LOC) into `_state.py` / `_rtds.py` / `_display_fields.py` / `_response_fields.py` / `_instrument_crud.py`. **§12.B** (PRs #668 → #678) split `app/web/views.py` (3,483 LOC) into ten per-page sub-modules. **§12.C** (PRs #680 → #682) cross-cutting hygiene — public `csv_imports.decode_csv`, 14 inline imports lifted to module scope, new `app/services/_queries.py::session_scoped`. **§12.D** (PR #683) split `tests/integration/test_display_field_routes.py` (2,167 LOC, 53 tests) into 6 per-surface files + a shared `_display_field_helpers.py` module. Plan: `guide/archive/major_refactor.md` §12. (Earlier in the same series, **§11** PRs **#651 → #659** had split `app/web/routes_operator.py` into the 10 feature-area sub-modules + `_shared.py`.) | 2026-05-09 |
| 13A-2 | **`session_rule_sets` name uniqueness within session** — DDL-only follow-on to 13A. **PR #711.** Adds the `uq_session_rule_set_session_name` constraint on `session_rule_sets(session_id, name)` mirroring the parallel `uq_rtd_session_name` already on `response_type_definitions`. Pure DDL; the table was empty on every deployment running the migration (lands inert from 13D PR 2). Underpins 12A-1's name-based `instruments[N].rule_set_name` reference + 15B's per-instrument selection + 15C's Save-to-library / Add-from-library flows. Follow-on details appended to `guide/archive/segment_13A_1_rule_based_editor_revamp.md`. | 2026-05-09 |
| 13A-1 | **Rule Based editor revamp** — supersedes 13A's two-column editor with a single-card **Rule Builder** at `/operator/sessions/{id}/assignments/rule-based-editor` paired with an **Available Rulesets** sibling card. **5 PRs** **#587 (PR 1) → #588 (PR 2) → #589 (PR 3) → #601 (PR 4a) → #602 (PR 4b)** plus iterated layout-spec stream **#590 → #600**. Three render branches share one form: seeded read-only (sentence-shaped rule lines), saved Personal (PR 5b/5c indented inline-composite editable form), unsaved draft (Copy from seed/Personal **and** "+ New blank RuleSet"). Friendly Description textarea on editable branches; "Available rulesets" sibling card lists every visible RuleSet with its description and a seed/personal pill. PR 4b retires 13A's standalone editor surface (`/edit/{rule_set_id}` + companion POSTs `/copy`, `/save`, `/save-as`, `/rename`, `/delete`, `/preview`); net diff after 4b: **-3487 lines** of legacy editor surface. Plan archived: `guide/archive/segment_13A_1_rule_based_editor_revamp.md`. As-built layout: `spec/archive/rule_based_assignment.md` §7.2. | 2026-05-07 |
| 13A | **Rule-based assignment builder.** Replaces the placeholder Rule Based card on the assignments page with a real RuleSet-driven rule menu — schema (`rule_sets` + `rule_set_revisions`), pure-Python engine (predicates / combinators / quotas / deterministic ordering), five seeded RuleSets, an editor child page, and a server-side live preview pane reusing the engine. **PRs #563 / #565 / #566 / #569 / #570 / #576 → #581** plus follow-on polish **#564 / #571 → #575**. The retired-card cleanup (PR 8) removed the standalone Full Matrix card from the assignments page; the seeded `Full Matrix` RuleSet covers the same case from inside the new card. New audit emitters `rule_set.created` / `.updated` / `.deleted` registered in `EVENT_SCHEMAS`. Plan archived: `guide/archive/segment_13A_rulebased_assignment_builder.md`. | 2026-05-07 |
| 11F | Previews page — pre-flight Reviewer Experience Preview hub at `/operator/sessions/{id}/previews`. PRs **#517 / #520 (PR A — page chrome + reviewer picker)**, **#521 / #522 (PR B — tabbed email previews region + invitation card)**, **#523 (PR C — reviewer-surface iframe card + retire `/preview` singular)**, **#524 (layout split into `Previewing as` + `About this reviewer` half-cards)**, **#532 (PR E — responses-received tab activation, shipped via 11E PR 6 since both depend on the same `render_responses_received` helper)**, and **PR D 2026-05-07 (reminder tab activation — single dispatch branch in `views.build_email_preview_body` calling `email_templates.render_reminder` with `PREVIEW_INVITE_URL_PLACEHOLDER`; `EMAIL_PREVIEW_TABS["reminder"].is_shipped=True`)**. Reviewer picker is a typeahead `<input list>` + `<datalist>` with Apply / `← Previous` / `Next →` / Random; `?reviewer_email=` URL state survives a full-cohort re-upload. New `views.PreviewPickerContext` / `PreviewPickerOption` / `EmailPreviewTab` / `EmailBody` / `SurfacePreviewContext` / `SurfacePreviewMissing` dataclasses + `build_preview_picker_context` / `build_email_preview_body` / `email_preview_from_display(user)` / `build_surface_preview_context` adapters. Reviewer-surface iframe uses `sandbox="allow-scripts"` only (no `allow-same-origin`) so the inline page-toggle JS keeps working while opaque origin blocks parent-cookie / localStorage access; `routes_reviewer.build_preview_context` grows an optional `target_reviewer` parameter. Standalone `/sessions/{id}/preview` retired as a 308 permanent redirect. Tests reshape via `tests/integration/_preview_iframe.py` helper that extracts + unescapes the iframe srcdoc. Send-test affordances per artifact moved to **Segment 14B Part A**. Plan: `guide/archive/segment_11F_previews_page.md`. | 2026-05-07 |
| 11G | Validate page rebuilt as a find-and-fix surface. PRs **#505 → #508** (the four-PR segment plan) plus polish **#509 → #511**. Replaces the thin issue-list body with a setup-coverage card (4-col grid of label + status cells) + severity filter chip strip + per-issue "Fix on {Setup page} ↗" deep-links + per-issue "Why this check?" native-disclosure + activate-warns detour banner. New `ValidationRule` registry in `app/services/validation.py` carries each rule's `key` / `severity` / `why` / `fix_url` / `fix_page_label`; `validate_session_setup` iterates the registry. Two new rules: `email_template.no_help_contact` (info) and `instruments.no_display_fields` (warning). `ValidationIssue` schema gains `rule_key` / `fix_url` / `fix_anchor` / `fix_page_label` / `why` fields stamped by the orchestrator. Setup-page tables gain `id="reviewer-row-{id}"` / `id="reviewee-row-{id}"` anchors so per-issue deep-links scroll to the offending row. `ReadinessReport.has_non_blocking_findings` tightened to only consider warnings (info is advisory only). `acknowledge_warnings` checkbox removed from the Next Action card on Session Home; Activate 303s to `/validate?activate=1` when warnings exist, banner inlines the warnings + Cancel + Acknowledge-and-activate. New helpers: `views.build_validate_context`, `views.validate_lifecycle_copy`, `views.SetupCoverageRow` / `SeverityChip` / `IssueSourceGroup` dataclasses. New tests: `tests/integration/test_session_validate_page.py`. Plan: `guide/archive/segment_11G_validate_page.md`. | 2026-05-06 |
| 11K | Audit-event `detail` schema convention. 8 PRs (**#544 → #545 → #546 → #547 → #548 → #549 → #550 → #551**). Pins the canonical envelope schema for `AuditEvent.detail` and migrates every emitter in the codebase to it. Spec at `spec/architecture.md` "Audit-event detail schema": four payload envelopes (`changes` / `snapshot` / `counts` / `set_changes`), top-level identity slots (`session_id` / `session_code`), orthogonal slots (`reason` / `refs` / `context`). Typed envelope constructors `audit.changes(...)` / `.snapshot(...)` / `.counts(...)` / `.set_changes(...)` in `app/services/audit.py`. Migrated emitters (file-by-file): session lifecycle (PR 1), instruments / display fields / response types / bulk events (PR 2, ~18 emitters), invitations (PR 3), responses (PR 4), assignments (PR 5), settings family covering CSV imports + operator settings + email templates (PRs 6-7). PR 6 lifted `email_template.updated` / `.reset` from `routes_operator.py` into `email_templates.py::record_template_change` / `.record_template_reset` so the route stays thin and the settings sweep covered them. PR 8 lands the Pydantic write-validation gate: per-event-type `EVENT_SCHEMAS` registry, `validate_detail` hook in `write_event`, `settings.audit_strict_mode` flag (False in production, flipped True in `tests/conftest.py`); strict mode raises `AuditDetailValidationError`, lenient mode logs and writes through. New `tests/unit/test_audit_helpers.py` and `tests/unit/test_audit_detail_schema.py` cover the helper composition + the gate. Cutover boundary 2026-05-07 — older rows keep their legacy shapes (the audit log is append-only). Closes catalog `guide/archive/unfinished_business.md` #5. Unblocks **Segment 12B** (audit retention + export reads against the canonical shape). Plan: `guide/archive/segment_11K_audit_event_detail_schema.md`. | 2026-05-07 |
| 11J | Quick Setup card wiring on Session Home. PRs **#526 → #527 → #528**. Reviewers / Reviewees / Assignments slots flip from inert (Segment 11H scaffold state) to live POST forms over the existing per-entity import pipelines. New routes `POST /sessions/{id}/quick-setup/reviewers` / `.../reviewees` / `.../assignments` delegate to thin wrappers (`_handle_quick_setup_import` / `quick_setup_assignments_submit`) that reuse the existing parse / save / `invalidate_if_validated` pipelines; `POST /sessions/{id}/quick-setup/lock` flips a per-session `HttpOnly` cookie (`qsu_{session_id}`, scoped to `/operator/sessions/{id}`) that the context-builder reads to decide `is_locked`. Status awareness collapses on one body-greying signal — `.quick-setup-body.locked` — applied in every editable-conceivable state including `ready`; `.card.disabled` retired. Lock / Unlock toggle renders consistently across `draft` / `validated` / `ready`; on `ready` unlocking is purely visual and `_require_editable` stays the hard gate (rejection surfaces as a scoped `banner-error` with "Pause first" copy). Assignments route auto-detects mode from the form payload (file attached ⇒ manual-CSV pipeline; else FullMatrix rule generation). Cascade-aware confirmation copy lives in new `views.cascade_message_for_replace` helper and renders as a banner-warning inline above the submit form. Slot 4 (Session settings / configuration-import) stays inert per the seam 11H pinned — graduates with Segment 12A PR 6. New tests: `tests/integration/test_quick_setup_card.py`. Spec follow-ons in `spec/quick_setup_card_spec.md` and `spec/session_home.md` align with the unified status-awareness model. Plan: `guide/archive/segment_11J_quick_setup_card.md`. Closes catalog `guide/archive/unfinished_business.md` #30 (modulo slot 4). | 2026-05-07 |
| 11H | Placeholder card scaffolds (Quick Setup + Extract Data on Session Home). PRs **#480 → #481 → #482 → #483 → #484 → #485 → #486 → #487**. Replaces the post-11B `placeholder_card(...)` stubs with full inert scaffolds: Quick Setup card with four slots (Reviewers / Reviewees / Assignments / Session settings) + Extract Data card with the per-entity row set, all controls disabled until the wiring segments. New partials `_quick_setup_card.html` + `_extract_data_card.html`; new dataclasses `QuickSetupSlot` / `QuickSetupContext` / `ExtractDataRow` / `ExtractDataContext` + builder helpers in `app/web/views.py`. The Quick Setup card on `/operator/sessions/new` consumes the same scaffold via `build_new_session_quick_setup_context`. Polish stream pinned the visual + DOM contract (2-column top grid, lock toggle, Exclude self-review checkbox, card reordering on Session Home). Plan: `guide/archive/segment_11H_placeholder_card_scaffolds.md`. Unblocks Segment 11J (Quick Setup wiring) and Segment 12A PR 6 (Extract Data wiring). | 2026-05-06 |
| 11C Part 2 | Outbox audit-log scaffolding (schema-only). **PR #541** (PR F). Migration `c4f6a8b0d2e5` adds the seven nullable audit-log columns to `email_outbox` per `spec/email_infra_options.md` "Future-target additions" — `error_message`, `from_address`, `backend`, `backend_message_id`, `delivered_at`, `payload_hash`, `correlation_id` (the last is **indexed** because the Segment 14B dispatch helper looks rows up by it on idempotent retry). `app/db/models/email_outbox.py` gains matching `Mapped[X | None]` declarations and the canonical value-set constants `EMAIL_OUTBOX_STATUSES = (queued, sending, sent, failed)` / `EMAIL_OUTBOX_KINDS = (invitation, reminder, responses_received)` so any future widening at the service layer is a deliberate edit; the class docstring is updated to reflect the broader audit-log role and to point at `spec/email_infra_options.md` for the field semantics. **Pure additive** — all columns nullable, no defaults beyond column defaults, no backfill, no service-layer reads or writes. Today's enqueue paths continue to write only `status="queued"` / `"sent"` and `kind="invitation"` / `"reminder"` plus the existing baseline columns; the new columns sit inert until **Segment 14B Part A** lights up the dispatch helper against this stable schema. New tests at `tests/integration/test_email_outbox_schema.py`: round-trips every new column (including the timezone-aware `delivered_at`) on both SQLite and the `ci-postgres` Postgres dialect, exercises an indexed `correlation_id` lookup, and pins the canonical value-set constants. Plan: `guide/archive/segment_11C_operations_consolidation.md` "Part 2". | 2026-05-07 |
| 11C Part 1 | Operations consolidation — Manage Invitations rewrite + new Responses page + Monitoring retired. PRs **#490** (chrome: restore Outbox tab — later removed in #493), **#491** (Manage Invitations rewrite with the seven-column reviewer-centric table + `email_outbox.cc_emails` / `bcc_emails` migration `b3d5e7f9a1c4` + reviewer drill-in scaffold), **#492** (new Responses page + reviewee drill-in scaffold + retire `session_monitoring.html` + `/monitoring` 303 redirect to `/invitations`), **#493** (drop Outbox from chrome — now reachable only via the "View outbox" button on Manage Invitations — and pillify the five data cells on Manage Invitations). Manage Invitations columns: Reviewer / Email Status / Email Sent / Review Progress / Required Fields / Last reminder / Action; data cells render inside `<span class="pill ...">` (`pill-count` for filled / good states, `pill-empty` for absent / not-yet states). Responses page (`/operator/sessions/{id}/responses`) classifies reviewees per `monitoring.AT_RISK_THRESHOLDS` (`adequate_fraction=0.5`) into Complete / Adequate / At risk / No responses. Bulk reminder dispatch funnels through `POST /operator/sessions/{id}/invitations/remind-incomplete` (single-source — both pages call it). Drill-ins at `.../invitations/{inv_id}/detail` and `.../responses/{reviewee_id}/detail` ship as scaffolds; per-assignment / per-response detail deferred. Operations row is now **four tabs**: Validate / Previews / Invitations / Responses (Outbox is a dev-diagnostic surface, not a chrome tab). New helpers: `monitoring.per_reviewee_coverage`, `monitoring.AT_RISK_THRESHOLDS`, `views.build_invitations_rows`, `views.build_responses_rows`, `email_templates.cc_bcc_for`. New breadcrumbs: `breadcrumbs.operator_session_invitations_reviewer`, `.operator_session_responses_reviewee`. Test reorg: `tests/integration/test_monitoring.py` → `test_reminders.py`; new `test_segment_11c_pr3_responses.py`. Plan: `guide/archive/segment_11C_operations_consolidation.md`. Functional spec: `spec/operations_pages.md`. Part 2 (the schema scaffolding for the 14B dispatch helper) shipped as PR #541 on 2026-05-07 — see the row above. |
| 11D follow-on | Reviewer surface multi-instrument rewrite plus the numeric-field validation polish stream. PRs #428 (α — URL routing + dashboard rewiring), #430 (β — top-row layout + per-page status pills), #431 (γ — unified action row + multi-instrument heading), #432 (δ — client-side page navigation + dirty preservation), #433 (ε — page-aware missing-required banner + preview adaptation), then polish #434–#448. Highlights: per-page `PageStatus` (`not_started` / `in_progress` / `complete` / `submitted`); URL `/me/sessions/{id}/{instrument_position}` with `current_position` hidden field driving Save / Submit; client-side group toggle + `pushState` keeps reviewer-typed dirty edits across pages; per-position Save filter; missing-required moved from a panel banner to its own full-width 2-column `.rs-missing-card`; Submit became a hard gate on missing required (acknowledge-and-submit-anyway retired); save / submit flash banners removed; numeric-field validation landed as `<input type="number">` with `min`/`max` + `step="any"` + hidden spinners + `title` constraint hint + JS `setCustomValidity` for step-grid violations (with `1e-6` tolerance) + server-side `validate_value` backstop in `responses.py`; per-instrument constraint summary line above each table reads e.g. `**Rating** (1-5, steps of 1), **Comments** (0-2000 char)` (List rows omitted). New helpers: `views.placeholder_for_field`, `views.constraint_summary_for_field`, `views.page_button_label`, `views.instrument_heading`. Plan: `guide/archive/segment_11D_v2_sweep_non_session.md` "Follow-on". | 2026-05-05 |
| 11L | `Instrument.short_label String(32) | NULL` column + Setup-side editor (`/operator/sessions/{id}/instruments`). Reviewer-surface Page #N buttons and per-instrument H2 headings consume it via `views.page_button_label(instrument, position)` and `views.instrument_heading(instrument, position, total_count)`. Bare `Page #N` fallback when unset. Independent prereq for Segment 11D follow-on PR γ. PR #429. Plan: `guide/archive/segment_11L_instrument_short_label.md`. | 2026-05-04 |
| 11B | Session Home rebuild end-to-end across PRs #380 → #393. New `app/services/lifecycle_display.py` + `lifecycle_label` Jinja filter so `ready` renders as **"Activated"** wherever an operator reads it (status pill, sessions list, prose). `session_detail.html` body rebuilt around three left-column cards (Next Action / Quick Setup / Extract Data) and two right-column cards (Session Details / Danger Zone). The **Next Action card** is the page's center of gravity: constant H2 ("Next Action"), `accent-blue` border matching the Primary button, fixed `min-height: 200px`, three vertically-stacked children (`.next-action-body` flex-grows; optional `.next-action-confirm` for the Pause checkbox; `.next-action-buttons` pinned at the bottom). All actions render as buttons in the bottom row — Primary (Validate Setup / Activate Session / Pause Session) plus Secondary supporting actions (See validation details / See previews / Revert to draft / Manage invitations / Monitor responses), with state-conditional trims and sentence-case copy. POST forms (Activate / Revert to draft / Pause) declare a hidden form id in the body and the bottom-row button declares `form="next-action-{name}-form"` so the form definition stays near its checkbox. `POST /operator/sessions/{id}/revert` extended to dispatch by current status: `validated → draft` calls `lifecycle.invalidate_session(reason="operator_revert")`; `ready → draft` calls `lifecycle.revert_session_to_draft`. Quick Setup and Extract Data on Home plus Rule Based Assignment on the Assignments page now share a canonical `.card.placeholder` class + `placeholder_card` Jinja macro (uniform muted treatment regardless of state); Quick Setup greys in `ready`; Extract Data greys in `draft` / `validated`. Danger Zone Delete Session is **visible-but-disabled** in `ready` rather than hidden (server-side gate in `_require_editable` is the source of truth). Stale `.pill-lifecycle-closed` v2 CSS retired. Spec at `spec/session_home.md`; plan at `guide/archive/segment_11B_session_home.md`. | 2026-05-04 |
| 12A-1 | Session export: Settings + Reviewers + Reviewees + Responses CSVs from a new **Extract Setup** card on Session Home. | 2026-05-09 |
| 12A-3 | Export / import updates for the 15D Relationships shape: Relationships CSV download, Settings CSV round-trip (export + import), retire the legacy Manual Assignments tile. | 2026-05-10 |
| 12B | Per-session audit-events CSV download route (now reached through the Sys Admin gate per the operator UI's no-Extract-tile decision). | 2026-05-10 |
| 12C-1 | Self-review revamp: per-session toggle gating reviewer / reviewee identity collisions in assignment generation. | 2026-05-10 |
| 13A | **Rule-based assignment builder.** Replaces the placeholder Rule Based card on the assignments page with a real RuleSet-driven rule menu — schema (`rule_sets` + `rule_set_revisions`), pure-Python engine (predicates / combinators / quotas / deterministic ordering), five seeded RuleSets, an editor child page, and a server-side live preview pane reusing the engine. **PRs #563 / #565 / #566 / #569 / #570 / #576 → #581** plus follow-on polish **#564 / #571 → #575**. The retired-card cleanup (PR 8) removed the standalone Full Matrix card from the assignments page; the seeded `Full Matrix` RuleSet covers the same case from inside the new card. New audit emitters `rule_set.created` / `.updated` / `.deleted` registered in `EVENT_SCHEMAS`. Plan archived: `guide/archive/segment_13A_rulebased_assignment_builder.md`. | 2026-05-07 |
| 13B | Sortable tables on every server-rendered operator + reviewer table (column-header click cycling Asc / Desc / off; per-table cookie persistence; SSR-decoded sort). Display-field tri-state Sort control on Instruments page. | 2026-05-12 |
| 13C | Group-scoped instruments (one reviewer answer covers a whole group of reviewees) + per-instrument **Replicate** button + per-instrument operator-actions card (Search by dropdown, row-select, bulk include / exclude). | 2026-05-18 → 2026-05-19 |
| 13D | DB prep for the library / per-session-copy split: `session_rule_sets` table + `instruments.rule_set_id` FK; `instruments.group_kind` + `sort_display_fields`; `InstrumentDisplayField` / `InstrumentResponseField` split. | 2026-05-09 |
| 13E | DB prep for the 12C / 15D block: `relationships` table; per-pair attribute columns. | 2026-05-10 |
| 14A | Production hardening (in-app ladder): structured logging + error pages + security headers + accessibility audit + runbooks (`docs/operations_runbook.md`, `docs/troubleshooting.md`, `docs/backup_restore.md`, `docs/known_limitations.md`, `docs/security_posture.md`); deploy workflow serialised via `concurrency` group. | 2026-05-18 |
| 15A | Pervasive friendly labels: operators rename the 12 in-scope display slots once per session via inline editors. | 2026-05-12 |
| 15B | Per-instrument assignments — each Instrument carries its own pinned rule + materialised pair set, replacing the pre-15B "one rule per session" model. Per-instrument rule picker on the Instrument card; Assignments page reshape (page-level Generate + per-instrument status blocks). | 2026-05-13 |
| 15C | Operator RTD / RuleSet libraries: symmetric two-tier library + per-session-copy model wired end-to-end; library contents on the Operator Settings page in two half-width cards. **Retired in Wave 5 PR 5.2** — the library tier collapsed back into the per-session shape. | 2026-05-12 |
| 15D | Assignments revamp: per-pair attributes move from `Assignment.context` JSON onto a new `relationships` table; **Relationships** Setup page (`/operator/sessions/{id}/relationships`); cross-pair predicates supported by an eager `pair_context_lookup` dict in the rule engine. Manual assignments retired (assignments are always derived post-15D). | 2026-05-10 |
| 15E | Operations Workflow card on Session Home — single super-button (`Validate setup → Generate assignments → Activate`) with confirmation modals + reconciliation prompts; replaces the staged Next Action card. | 2026-05-14 |
| 15F | Enhanced Setup pages: per-row inline Edit + bulk inactivate / reactivate + single-row Add + search / status filter strip + sortable trailing **Updated** timestamp column on Reviewers / Reviewees / Relationships. | 2026-05-15 |
| 16A | Sys Admin page + workspace user / role management (allowlist of operator emails, role audit, account-management chrome behind a top-bar Admin link). | 2026-05-10 → 2026-05-11 |
| 16B PR 2 | Per-session owner management on the Edit Session page (Owners card, last-owner `SELECT … FOR UPDATE` race fix per Codex review). | 2026-05-11 |
| 16C MVP | In-app per-session audit log viewer (filter strip + pretty-printer); per-session **Outbox** view inline on Admin Sessions Diagnostics; entity drill-in + cross-session search deferred to pilot feedback. | 2026-05-11 |
| 17A | Housekeeping (file splits + test-suite runtime): three production files over ~1.3k LOC carved; `pytest-xdist -n auto` parallel runs + ORM-metadata schema build cuts the SQLite test suite ~90s → ~22s. | 2026-05-16 |
| 17B | Reviewer surface refinements: textarea autosize, page-navigator cleanup, sticky bottom-grid action row, per-page status pills. | 2026-05-16 → 2026-05-20 |
| 18A | Sessions lobby enhancements: selection-aware row-expander, free-form session tagging, `draft ⇄ archived` archiving + `/operator/sessions/archived`, one-click clone, client-side search, sortable columns. | 2026-05-17 |
| 18B | Date and time settings: canonical bare format via `app/services/date_formatting.py` + Jinja filters; per-operator default timezone + per-session timezone; **Timezone** column on sessions lobby; per-session CSV extracts localised to session zone (audit-events extract held at UTC as the documented exception). | 2026-05-15 |
| 18C | Operator-triggered purge: "Purge and archive" action on the 18A row expander; selective hard-delete of responses / rosters / audit log via `session_purge`. Scheduled / policy-driven retention deferred. | 2026-05-17 |
| 18D | Export / import update: Settings CSV round-trips `display_timezone` + `self_reviews_active` + provenance cell; Responses extract restructured for downstream analysis (per-instrument preamble + positional naming); **Zip-all** tile graduates to a real `{code}_bundle.zip`. | 2026-05-17 |
| 18E | Small enhancements: column-visibility chip rows on Reviewers / Reviewees / Relationships Setup pages (per-column `Show columns:` chips, friendly labels, per-browser localStorage persistence); eligible-pair count caching for the Instruments page (`cached_eligible_pair_count` on `session_rule_sets`, content-hashed); pinned-rules-only eligibility passes. | 2026-05-18 |
| 18F | Workflow optimisation: Danger Zone moved to Edit Session Details; Create / Clone redirect to Edit; chrome **Invitations** pill split into four states (Not created / Not sent / Partially sent / All sent). | 2026-05-20 |
| 18G | Scheduled events Parts 0 → 3: schema pre-positioning (eight `sessions` columns); scheduled activation (timed `validated → ready`); auto-send invitations (offsets anchored on Start); auto-send reminders (offsets anchored on End). Parts 4 (auto-archive) + 5 (scheduled purge) deferred to pilot feedback. | 2026-05-21 |
| 18H | Post-assessment update + small enhancements (codebase assessment QQ-flag clean-up cluster). | 2026-05-24 |
| 18I | New-model instruments (exploration + main push): per-instrument card with Bands 1+2+3, "Add new-model instrument" entry point alongside the legacy add buttons. | 2026-05-24 |
| 18J | New-model takeover mopping-up: legacy individual + group flavours collapsed into one uniform new-model card; per-instrument response-field inline `_inline_data_type` / `_inline_min` / `_inline_max` / `_inline_step` / `_inline_list_csv` / `visible` columns; `response_type_definitions` table retired 2026-05-26. | 2026-05-24 → 2026-05-26 |
| 18K | Reviewer-surface visibility (Band 3 follow-on): reviewer summary HTML + reviewer-record CSV filter response fields by `InstrumentResponseField.visible`; Band 2 chip un-pin gated by a `confirm()` + 409 guard when the field has saved responses; reviewer surface shows a dropped-fields banner on next load. | 2026-05-27 → 2026-05-28 |
| 18L | Multi-page reviewer surface (operator-defined): instruments grouped into pages on the reviewer surface; per-page status pills + per-page Save filter + page navigator. | 2026-05-27 → 2026-05-28 |
| 18M | Operator instrument ordering + page breaks: per-instrument drag-gripper reorder + per-instrument `starts_new_page` toggle authored on Instruments. | 2026-05-27 → 2026-05-28 |
| 18N | Housekeeping (file splits + reviewer-surface asymmetry + settings round-trip): unified `validate_page_n` helper; three biggest production files carved into per-concern slices; settings CSV round-trip closes 17 gaps (8 18G fields + 6 response-field inline fields + `column_widths` / `starts_new_page` / `band2_state`). | 2026-05-28 |
| 18O | Post-participants-model file splits: four-track housekeeping pass on the 1,300+ LOC band (`scheduled_events.py`, `assignments.py`, `routes_reviewer/_surface.py`, `session_config_io/_apply.py` all carved into per-concern packages). ~5,500 LOC redistributed across ~23 small modules; biggest production file now ~764 LOC. | 2026-06-03 |
| 18P G1 | Harmonize the round-trip (PRs #1864 → #1870): export→import + clone stop silently dropping hand-set config — feature toggles, instrument view policies, reviewer / reviewee / observer roster `status`, observer cohort rules, scheduling / retention anchors, data shapes, session tags, `band1_touched_links`. Closes the `spec/roundtrip_coverage.md` gaps. | 2026-06-05 |
| 18P G2 | Rehydrate a complete extracted session (PRs #1872 → #1878): **Rehydrate** lobby button → `/operator/sessions/rehydrate` (Validate → Rehydrate); pure pre-flight analyzer; operator-scoped Postgres-backed stash (`rehydrate_stashes`, the segment's one migration); sectioned-`responses.csv` importer (`responses_import.py`, group fan-out + own size bound); `session_rehydrate.rehydrate_session` orchestrator lands a `<name>_REHYD` draft (assignments regenerated, not activated) with all-or-nothing rollback + `session.rehydrated` audit. See `spec/rehydrate.md`. | 2026-06-05 |
| 18Q | Blob-storage **deferral** — no code. Evaluated an Azure Storage seam for large extracts + the rehydrate stash; decided the Postgres `bytea` stash + streamed extracts suffice for the pilot, so blob storage stays deferred (`spec/blob_storage.md`, `guide/deferred_consolidated.md`). | 2026-08 |
| 18R | Operator-UX refinement (holding segment): Item 1 — instrument-card identity relabel (Instrument assignment rule + Who does / is / unit-of-review links, #1899); Item 2 — save/lock harmonization of the Instruments card into one consolidated `/save` path (PR ladder 1–7, cleanup #1921); Item 3 (open) — small card tweaks (taller description box, Link 2 operator-cycle reorder, #1923). | 2026-08-17 |
| 18S | Security refinements (holding segment): Item 1 — three-tier role model (operator ⊂ admin ⊂ **super-admin**). Config-derived, protected super-admin (`SUPER_ADMIN_EMAILS`, `app/auth/roles.py`); every-sign-in self-heal; actor-super guard on promote/demote + target-super protection on demote/revoke/remove; Sys Admin tier badges + conditional controls; `(super admin)` chrome. Localhost `fake_auth_super_admin` toggle. No migration. Item 2 — no-super-tier fallback (#1930, admin promote/demote falls back to any-admin when `SUPER_ADMIN_EMAILS` is empty). Item 3 — tighten sys-admin cross-session writes (#1935): edit / lobby-edit / owners-remove require ownership; `owners/add` self-only for non-owners; `clone` stays relaxed; the Diagnostics "Manage"/adopt door self-adds as owner (audited). | 2026-08-17 |
| 19B | Consistency remediation — the code-level "same functionality, divergent call paths" fixes from `guide/archive/consistency_audit.md`; sibling modules brought to one shape. PRs #1987 → #2003. | 2026-08-19 |
| 19C | Refinements (**closed**; was a standing holding segment), ten items: Item 1 friendly tag labels via roster CSV headers as the sole round-trip carrier (#2005 → #2013); Items 3–4 Danger Zone hardening + button refinements and Item 2 light / dark Display mode (#2014 → #2031); Item 6 two-tier semantic colour tokens — 79 primitives under 103 role-named tokens (#2047 → #2062); Item 5 theme customizer v1, a three-part developer designer in `tools/` (#2032 → #2083), **v1.1** primitive readout + unused-primitive marker + stale-document merge fix + 3:1 contrast gate (#2152 → #2155); Item 7 the first drift sweep's eight findings (#2116 → #2118); Item 8 input boundaries — `--border-default` to `--slate-dim` for 3:1, `.rs-help-card` onto its own `--card-help-*` family (#2126 → #2128). **Item 9** — the Settings-CSV import refuses a visibility cell the Band 3 editor refuses (#2188): two writers created `instrument_view_policies` rows and only one validated the `(audience, window)` cell, so an imported bundle could persist a `reviewee` `while_ongoing` grant — responses mid-flight — that no editor path can author. The guard reads the same `_PER_CELL_VALID_MODES` table both writers now answer to, rejects the whole apply in the parse phase before any write, and names the field. Found by 19F's close audit; pre-dated 19F (18P PR A2). **Item 10** (#2191 + #2192) is its retrospective half — a read-only **Visibility grid audit** card on Sys Admin → Sessions Diagnostics listing any stored cell whose mode its `(audience, window)` pair disallows, workspace-wide behind `require_sys_admin`, live findings first. It writes nothing: clearing a cell is the owning operator's call on the Band 3 editor. Scaffold-first per `CLAUDE.md`, and it has not yet run against a real database — the pilot has not deployed, so a green card today means "no rows here". **Closed 2026-09-08**: the holding shape had outlived its use — ten items over nineteen days made a plan too long to read end to end, and its `close_check` window spanned three other segments' work. New refinements get their own segments. Two never-built items parked in its Future-items list moved to `guide/todo_master.md` (Upcoming → Stubs) rather than archiving with the plan: the theme-customizer element pass, and the global technical-support contact. | 2026-08-20 → 2026-09-08 |
| Practice audit | Development-practice audit + follow-through (not a numbered segment): what gates a merge, measured (`docs/practice-audit-2026-09-04.md`); `tests/unit/test_doc_conventions.py` (lifecycle labels from `DISPLAY_LABELS`, retired button vocabulary, `CLAUDE.md` / `AGENTS.md` twins); `.claude/agents/diff-reviewer.md`; merge policy in `CONTRIBUTING.md`; `new_project_practices_setup.md`; `CLAUDE.md` 266 → 183; `tools/code_metrics.py` duplication + churn (deterministic, full-history); codebase assessment 2026-09-04 + `guide/assessment.json`; `rrw_sdd_in_practice.md`; `spec/permissions.md` + `spec/email_template_editor.md` (19A Part 1). PRs #2085 → #2101. | 2026-09-04 → 2026-09-05 |
| 19E | **Operator onboarding.** `/guide` — the in-app, audience-filtered operator documentation, in the chrome row beside `/about`; `docs/quickstart.md` retired to `docs/archive/` with its 11 live references repointed. The lobby **first-run card** on zero *visible* sessions, four stepped tiles that are a table of contents for the Guide. Two CSV **template sets** — starter and demo — byte-derived from the four serializers, the demo one proved by a download → Quick Setup → `validated` round-trip rather than a fixture. A `What this page is for` **disclosure** on all six Setup pages, copy contract in `spec/setup_pages.md` §"Shared body shape" item 0. **Role-aware sections** (rung 7): `visible_audiences()` unions the operator predicate with `participants.roles_held_anywhere`; a viewer holding nothing sees everything. Plus the author's Guide rewrite — the walkthrough with twelve screencaps, behind the app's first `StaticFiles` mount at `/static`. Planned #2130; rungs 1–7 across #2131 → #2173; closed #2176. Per-rung PR numbers in `guide/todo_master.md`. | 2026-09-07 |
| 19F | **Reviewee participation disclosure.** A reviewee's `/me` row, role chip and `/results` surface now require a **currently-resolving visibility grant** (`visibility_policies.reviewee_has_current_grant`) rather than roster membership — the row appears and disappears as the windows move, and an archived session closes the grant. Two window corrections behind it: `while_ongoing` is `status = "ready"`, and `after_release` requires `expired` **and** the anchor passed, so a session reverted to draft no longer sits inside an open release window. The wide rung is a security change beyond the theme: **all four session-scoped gates answer 404** with no role-naming detail, so a signed-in stranger cannot enumerate session ids from status codes (sys-admins exempt — 403 on an existing session they do not own, existence checked first). `/guide` closes to a viewer who resolves no audiences and 303s them to `/about`, reversing 19E rung 7's fallback; `roles_held_anywhere` → `disclosable_roles`, grant-aware. Observers keep their rows in every state, unlinked when archived. The same gating reaches the **role-navigator chip strip** (rung 7): the reviewee chip is omitted without a grant, the observer chip greys on an archived session, so the two doors onto a role agree. Planned #2174, replanned #2175 + #2178, rungs across #2179 → #2186, closed #2187. Per-rung PR numbers in `guide/todo_master.md`. | 2026-09-07 |

Migration round-trips on both SQLite (every test session) and Postgres
(every PR via the `ci-postgres` job, which also runs the full pytest
suite against a `postgres:16` service container).

---

## Capabilities today

### Infrastructure & dev loop

- **Azure App Service** (Linux, Python 3.12, gunicorn + uvicorn).
- **Azure Postgres Flexible Server** (Burstable B1ms, Pg 16, Southeast
  Asia). Public access with firewall allow-list ("Allow Azure
  services" + dev IP). VNet integration is deferred infrastructure
  (`guide/deferred_consolidated.md`).
- **Deploy on push to `main`** — three jobs: `build` → `migrate` →
  `deploy`. The `migrate` job runs `alembic upgrade head` against
  Azure Postgres before the App Service swap; deploy is skipped if
  migration fails.
- **CI on every PR**: SQLite pytest plus a `ci-postgres` job that
  applies and round-trips migrations and runs the full pytest suite
  against a `postgres:16` service container. The `engine` fixture in
  `tests/conftest.py` honours `TEST_DATABASE_URL` / `DATABASE_URL` so
  the same suite covers both dialects without duplication.
- **Test infrastructure**: in-memory SQLite engine running real
  Alembic migrations once per session; per-test savepoint-based
  isolation so service-layer commits don't leak across tests;
  `make_client` factory for multi-user integration tests.
- **Documentation**: `docs/{authentication,database,imports}.md`,
  `docs/deployment_dev.md` (incl. one-time Postgres GRANT bootstrap),
  segment plans in `guide/`.

### Authentication & permissions

- **Microsoft Entra ID via Azure Easy Auth** in deployed environments.
- **Local fake-auth fallback** (`ALLOW_FAKE_AUTH=true`) for offline
  development.
- **`AuthenticatedUser`** dataclass parses `X-MS-CLIENT-PRINCIPAL` and
  the simpler `X-MS-CLIENT-PRINCIPAL-{NAME,ID,IDP}` headers.
- **`get_or_create_user`** dependency creates a `User` row on first
  sign-in. We don't pre-provision.
- **`require_operator`** is a router-level dependency on the whole
  `/operator/*` package (the workspace allowlist; a miss 303s to `/me`),
  and **`require_session_operator`** gates every session-scoped route on
  a `SessionOperator(user, session)` row. Since 19F a non-member gets
  **404**, not 403, and the body is indistinguishable from an unknown
  session id — the same refusal all four session-scoped gates give, so a
  signed-in stranger cannot enumerate session ids from status codes. One
  exemption: a **sys-admin** gets 403 for a session that exists and they
  do not own (existence is checked first, so the exemption cannot invent
  sessions), with the message pointing at Sys Admin → Sessions. The full
  gate catalogue, per-route matrix and failure semantics are
  `spec/permissions.md`.
- **Three-tier role model** (Segment 18S): operator ⊂ admin
  (`is_sys_admin`) ⊂ **super-admin**. Super-admin is derived from
  `SUPER_ADMIN_EMAILS` (config, never stored), self-heals to full admin
  rights on every sign-in, is the only actor who can promote/demote
  admins, and is protected from in-app demote/revoke/remove. See
  `docs/security_posture.md` + `spec/audience_and_identity_model.md` §4.
- **Sys Admin → Sessions Diagnostics** (`/operator/sys-admin/sessions`) lists every session in the workspace with three per-row **Secondary** actions — **Manage** (self-adds the sys-admin as an owner, audited, then opens the session), **Outbox** and **Audit log** — plus the read-only **Visibility grid audit** card (19C Item 10).
- **Diagnostic pages**: `/auth/me` (JSON), `/auth/me/debug` (HTML with
  the raw claims list and a sign-out link). `/me` itself is the participant
  dashboard.

### UI / branding

- Inline-SVG favicon (bird emoji 🐦) defined in
  `app/web/templates/base.html`. Edit the emoji or the SVG markup
  in the `<link rel="icon">` data URI to change it; for a real
  graphic asset, mount `StaticFiles` and point `href` at
  `/static/favicon.png`.
- **Manage-page reshape (Segment 9.4C)**: the reviewers, reviewees,
  and assignments Manage pages now render an always-present
  `<section id="upload-csv">` card with the existing import form;
  the Upload CSV button is `<a href="#upload-csv">` (no JS, no
  `<details>`, stateful via the URL fragment). Validation errors on
  POST re-render the Manage page itself — there is no longer a
  standalone `…/import` GET. The assignments page also carries an
  anchored `<section id="rules">` "Assign by Rules" placeholder
  (Rule editor — Segment 13) with a Cancel anchor that drops the
  fragment. **Edit Reviewers / Reviewees / Assignments** buttons
  render as disabled anchors (`<a class="btn disabled"
  aria-disabled="true">`) per the 9.4B disabled-affordance
  convention. New `/operator/sessions/{id}/instruments` index
  introduced (Segment 10C reshaped this page substantially — see
  the Segments-shipped 10C entry and the operator URL table for
  the current contract). New `/operator/sessions/{id}/setupinvite`
  is the operator-editable email template editor (Segment 11E
  shipped the editor + the SMTP transport scaffolding; the page
  was a stub through Segment 11D follow-on). Session-detail
  Setup table Manage buttons for Instruments and Set up invites
  are now real links.
- **Page chrome (Segment 9.4A)** in `app/web/templates/base.html`:
  top-left "Review Robin Web App (version {num})" link to `/about`,
  breadcrumb trail rendered just below, top-right user card with
  "Signed in as ..." + Sign out. Per-page back-links across pages
  are removed — the breadcrumb replaces them. (Segment 10C
  reintroduced one in-page Back affordance: the per-instrument
  card's bottom button row carries a Back button that
  smooth-scrolls to the top of the Instruments page. This is a
  same-page navigation aid, not a cross-page back-link.)
  Operator-page crumbs root at `Sessions → /operator/sessions`;
  reviewer-page crumbs root at `Reviewer → /me`. Crumb
  factories live in `app/web/breadcrumbs.py`; the partial is
  `app/web/templates/_partials/breadcrumb.html`. Version string
  comes from `app.config.app_version` (`"dev"` for now;
  pipeline-driven version bumping is a Segment 14A concern).
- **Setup nav + lock card (Segment 10C)**: every session-scoped
  operator page (Session detail, Reviewers, Reviewees,
  Assignments, Instruments, Set up invites) renders a 6-button
  `.setup-nav` header card and — when the session is `ready` — a
  reusable yellow lock card immediately below it. The lock card
  posts to `/operator/sessions/{id}/revert` with a hidden
  `return_to` field; the route allowlists
  `{reviewers, reviewees, assignments, instruments}` so the
  operator lands back on the same page. The session-detail lock
  card omits `return_to`. While locked, each page hides its own
  mutation affordances (upload cards, Danger Zone, per-instrument
  Save button); `<input>` / `<select>` elements inside
  `.field-builder` are disabled. See `spec/operator_ui_concept.md` for
  the per-page contract and `spec/domain_assumptions.md` for the markup.
- Card-based layout, monospace tabular code spans, severity pills
  (`error` / `warning` / `info`) for validation issues. All inline
  `<style>` in `base.html`. CSS framework / extraction is a Segment
  14 concern.
- **Session Home rebuild (Segment 11B + 2026-05-22 Danger Zone move)**: `session_detail.html`
  renders the **Next Action card** at the top (constant H2 "Next
  Action", `accent-blue` border; height grows to fit content; per-state
  Primary + Secondary buttons at the bottom for most states; the
  Activated state lays out as two body sections separated by an `<hr>`
  with their own inline buttons — Manage invitations + Monitor
  responses, then Pause Session), followed by a two-column bottom
  grid. Left column: Session Details (with an inline Edit link to
  `/operator/sessions/{id}/edit`). Right column: Quick Setup, then
  Extract Data. The Danger Zone card (Delete Data + Delete Session)
  was moved off Session Home into the bottom-right of the Edit
  Session Details page on 2026-05-22 (commit b490825). The two
  placeholder cards plus the Rule Based Assignment card on
  `/assignments` share the canonical `.card.placeholder` class +
  `placeholder_card` Jinja macro
  (`app/web/templates/operator/partials/_placeholder_card.html`), <!-- path-ref-ok -->
  so all three render with identical typography and contrast.
  See `spec/session_home.md`.
- **Lifecycle display label mapping (Segment 11B)**: a single
  helper in `app/services/lifecycle_display.py` translates
  `ReviewSession.status` enum values into operator-facing strings.
  Today's only divergence is `ready` → "Activated"; other states
  pass through capitalised. Registered as the `lifecycle_label`
  Jinja filter on the operator templates instance and used by every
  surface that renders a lifecycle state in user copy (status pill,
  sessions list table, Session Home prose, lock-card prose on
  Invitations / Monitoring). URL slugs, query params, API
  responses, log messages, audit-event detail, and CSS class names
  continue to use enum values.

### Operator-facing app

| URL | What it does |
|---|---|
| `GET /` | service metadata |
| `GET /health` | unauthenticated `{"status": "ok"}` |
| `GET /about` | unauthenticated stub page; chrome's app-identity link target |
| `GET /me`, `/me/debug` | identity introspection |
| `GET /operator/sessions` | list of sessions where user is operator |
| `GET /operator/sessions/new` | create form |
| `POST /operator/sessions` | create + insert `SessionOperator` + audit + 303 |
| `GET /operator/sessions/{id}` | Session Home — **Next Action card** on top (constant H2, blue border, state-conditional Primary + Secondary buttons at the bottom; surfaces Validate Setup / Activate Session / Pause Session per lifecycle state), then a two-column bottom grid. Left column: Session Details (with Edit link). Right column: **Quick Setup** (4-slot two-column fully-wired card post-12A-3: Reviewers + Reviewees on the left, Relationships + Session settings on the right; submit-all chain is reviewers → reviewees → relationships → settings), then **Extract Data** (5 live CSV downloads in a 2-column layout post-12A-3: Reviewers · Settings · Reviewees · Responses · Relationships, with Zip-all row inert; per-entity Download buttons grey out when their count is 0). The Danger Zone card (Delete Data + Delete Session) moved to Edit Session Details on 2026-05-22 (commit b490825). `?validated=1` re-runs setup validation and (when no blocking errors) marks the session `validated`; the Next Action card switches its primary button to **Activate Session**. When warnings exist, that button 303s to `/validate?activate=1` for a confirmation banner with the warnings inline (Segment 11G PR D); when warnings don't exist, the button POSTs directly. Spec at `spec/session_home.md`. |
| `GET /operator/sessions/{id}/edit` | **Retired (18R Item 4).** The standalone Edit Session Details page was retired: session config now displays + edits inline on Session Home via `?editing=1#session-config` (the same display↔edit swap the instruments card uses), and the Danger Zone lives on Home. `GET .../edit` is a thin **301-redirect stub** to `?editing=1#session-config`. |
| `POST /operator/sessions/{id}/config` | Apply session-config changes + audit (replaced the retired `POST .../edit` in 18R Item 4). |
| `POST /operator/sessions/{id}/delete` | delete session and all dependents (confirm; locked while `ready`) |
| `POST /operator/sessions/{id}/delete-data` | wipe every reviewer Response for the session; preserves setup; allowed in any status; emits `responses.deleted_all` audit event |
| `GET /operator/sessions/{id}/validate` | read-only setup validation deep-dive (Activate moved to the inline summary card on session detail) |
| `GET /operator/sessions/{id}/reviewers` | roster Manage view with anchored `#upload-csv` import card and disabled Edit Reviewers button |
| `POST /operator/sessions/{id}/reviewers/import` | parse + replace + audit; on validation errors re-renders the Manage page |
| `POST /operator/sessions/{id}/reviewers/delete-all` | delete every reviewer + cascade |
| `GET /operator/sessions/{id}/reviewees` | roster Manage view, on the shared roster shape since 19P.3: full-width guidance, roster card with the **Unlock panel** holding the tag-label editor, the `Danger Zone` and the anchored `#upload-csv` import card, then the preview-table card with its two-pane toolbar and **row expander**. Nothing below the table. |
| `POST /operator/sessions/{id}/reviewees/import` | parse + replace + audit; on validation errors re-renders the Manage page |
| `POST /operator/sessions/{id}/reviewees/delete-all` | delete every reviewee + cascade |
| `GET /operator/sessions/{id}/relationships` | Relationships Setup page (Segment 15D PR 2; rebuilt to the shared roster shape at 19P.3). Full-width guidance, then a roster card whose **Unlock panel** holds the tag-label editor over the `Danger Zone` on the left and `Upload Relationships` on the right; preview-table card below it, its two-pane toolbar carrying the `Show columns:` chips and pager left and the filter strip right, and a **row expander** for the selection actions. Nothing below the table. Trailing `status` cell renders as a `pill-info` (active) / `pill-empty` (inactive) span. The stats info card (`Fields with data:` pills) retired in Segment 19I Item 12; the roster index in the card above replaced it at 19P.3. |
| `POST /operator/sessions/{id}/relationships/import` | parse + replace + audit; on validation errors re-renders the Setup page; emits `relationships.imported` |
| `POST /operator/sessions/{id}/relationships/delete-all` | delete every relationships row + cascade; emits `relationships.deleted_all` |
| `GET /operator/sessions/{id}/assignments` | **Operations** Assignments page (chrome row label moved from Setup to Operations in 15D PR 6a). Per-instrument status table, then the **Assignments preview** card, which since 19P.5 carries the filter strip in its **two-pane toolbar** and the bulk `Inactivate` / `Activate` in an injected **row expander** — the roster idiom, taken by the one Operations page with selection. The half-width operator-actions card that held both, its `.bottom-grid` and the `.grid-right` rule are gone. Rule authoring lives on the Instruments page's Band 1, not here |
| `POST /operator/sessions/{id}/assignments/rule-based/generate` | generate assignments from a Personal or seeded RuleSet via `app/services/rules/engine.py`. Reads pair-context tags from the `relationships` table via `pair_context_lookup`. Inactive reviewers / reviewees / relationships rows excluded; audit `excluded_counts` records reasons. Emits `assignments.generated` |
| `POST /operator/sessions/{id}/assignments/self-reviews/active` | flip `sessions.self_reviews_active` Boolean — bulk-activates / deactivates self-review pairs the engine produced; per-row `Assignment.include` overrides individually post-flip. Emits `assignments.self_reviews_active_changed` |
| `POST /operator/sessions/{id}/assignments/manual/import` | dev-only manual-CSV pipeline; the operator-facing card retired in 15D PR 6a. Route still exists for test fixtures |
| `POST /operator/sessions/{id}/assignments/delete-all` | delete every assignment, clear mode |
| `POST /operator/sessions/{id}/activate` | flip session draft→ready (warn-and-acknowledge for non-blocking findings) |
| `POST /operator/sessions/{id}/revert` | dispatched by current status (Segment 11B): `ready → draft` calls `lifecycle.revert_session_to_draft` (confirm checkbox required; closes all instruments); `validated → draft` calls `lifecycle.invalidate_session(reason="operator_revert")` (no confirm checkbox). Wired to the Pause Session and Revert to draft buttons in the Next Action card on Session Home. |
| `GET /operator/sessions/{id}/instruments` | consolidated instruments page (post-10D shape) — setup nav header, yellow lock card when ready, **session status card** (half-width in a `.card-columns` pair beside the guidance card: deadline + accepting-count pills and the Expand/Collapse buttons — **no** bulk accepting or visibility control, the accepting one never wired and the visibility one removed, both at 18R Item 3; corrected 19O Item 6, and `spec/instruments.md` owns the contract), then one pastel-tinted card per instrument with a top `.bottom-grid` (description + per-instrument status), a `.field-builder` `.bottom-grid` of Display + Response Fields half-cards (both fully wired post-10D — add / edit / delete / reorder rows + per-source picker for Display Fields), a live client-rendered Preview Instrument #N table, and a Back / Save / Edit / Add an instrument / Delete button row. Multi-instrument schema + services + UI shipped (Add + Delete with mutual-exclusion / `is_ready` / single-instrument gates and a native `confirm()` on Delete). See `spec/setup_pages.md` for the per-section contract. |
| `GET /operator/sessions/{id}/setup-invite` | operator-editable email template editor (Segment 11E). Two-card `.bottom-grid`: composer left, merge tags + Save / Cancel right. `?template=invitation|reminder` selects the active template; per-field "Reset to default" forms remove individual override keys. POSTs to `POST .../setup-invite` (save) and `.../setup-invite/reset` (per-field reset). |
| `GET /operator/settings` | per-operator Settings page (Segment 11E). SMTP credentials (host / port / username / app-password / display name / encryption mode) stored on `users.smtp_*`; password encrypted at rest via `cryptography.fernet` keyed off the `SMTP_ENCRYPTION_KEY` env var. Honours `?return_to=<path>` per `app.web.return_to`. Reachable via the chrome user-menu Settings link. POSTs to `POST /operator/settings` (save) and `.../settings/clear` (wipe). |
| `GET /operator/sessions/{id}/instruments/{instrument_id}` | legacy redirect — 303 to `/instruments` (back-compat for bookmarks; 10A) |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/edit` | edit friendly description (`Instrument.description`); audit `instrument.described`; invalidates `validated → draft` |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/fields` | add a response field; auto-derives `field_key` from label when blank; audit `instrument.field_added` |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/fields/{field_id}/edit` | edit a response field (label / required / validation / help text + visibility); audit `instrument.field_updated`; banner-warns when optional → required leaves existing reviewer rows incomplete |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/fields/{field_id}/delete` | delete a response field; cascade-confirm flow when responses exist; audit `instrument.field_deleted` |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/fields/{field_id}/move` | up / down reorder; repacks `0..N-1`; audit `instrument.fields_reordered` |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/display-fields` | add a display field (one of the seven D6 sources, posted as `source_pair=reviewee:tag_1`); audit `instrument.display_field_added`; invalidates `validated → draft`; `DisplaySourceError` (unknown source / duplicate) redirects with `?display_source_error=<pair>` |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/display-fields/{df_id}/edit` | edit label override + visibility; `(source_type, source_field)` are immutable; audit `instrument.display_field_updated` (diff-shaped) |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/display-fields/{df_id}/delete` | delete a display field; no cascade-confirm; audit `instrument.display_field_deleted` (with snapshot) |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/fields/save` | shared bulk form covering display + response fields — repacks `order` to `0..N-1` per table independently; persists display rows' `visible` + `label`; audit `instrument.fields_reordered` (when response order changes) and / or `instrument.display_fields_saved` (D11 diff shape) |
| `GET /operator/sessions/{id}/preview` | the standalone reviewer-surface preview, **retired in Segment 11F PR C**; permanent (308) redirect to `/sessions/{id}/preview-surface/1`, kept for stale bookmarks. (The attribution was dropped when the row was rewritten at 19Q Item 1 and restored at 19O Item 6 — this table is the one document whose job is history.) |
| `GET /operator/sessions/{id}/previews` | retired hub; permanent (308) redirect to `/sessions/{id}/invitations` |
| `POST /operator/sessions/{id}/previews/random` | retired with the hub; returns 404 |
| ~~`.../instruments/accepting/all-{on,off}`~~ | **Retired 18R Item 3.** The bulk "Open / close all" control was never wired into the UI and was dropped from the spec, along with `bulk_set_accepting` + the `instruments.bulk_accepting_responses` audit event. Per-instrument open/close (below) remains. |
| ~~`.../instruments/visibility/all-{on,off}`~~ | **Retired 18R Item 3.** The session-level bulk visibility-when-closed toggle was removed from the Instruments page, along with `bulk_set_visibility` + the `instruments.bulk_visibility_when_closed` audit event. Visibility when closed is now governed by the per-instrument visibility policy; `responses_visible_when_closed` persists for config round-trip. |
| `POST /operator/sessions/{id}/instruments/add` | create a new instrument under the session (optional `after={instrument_id}` for placement); audit `instrument.created`; invalidates `validated → draft`. UI button currently disabled — multi-instrument operator UI is intentionally deferred |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/delete` | delete an instrument and its dependents (cascades response fields, display fields, and assignments via FK delete-orphan); audit `instrument.deleted`; invalidates `validated → draft`. UI button only renders when more than one instrument exists; 400 when deleting the last instrument |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/open` | start accepting responses (requires session ready, pre-deadline) |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/close` | stop accepting responses (manual) |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/visibility` | toggle `responses_visible_when_closed` |
| `POST /operator/sessions/{id}/instruments/{instrument_id}/fields/add-row` | append a new response field with a default key/label/type after `after={field_id}` (or at the end when omitted); audit `instrument.field_added`; invalidates `validated → draft`. Powers the Response Fields ➕ button on the per-instrument card |
| `GET /operator/sessions/{id}/invitations` | consolidated reviewer-centric Manage Invitations page (Segment 11C Part 1). Pill-styled table — Reviewer / three chip-toggled tag columns / Email Status / Sent / Progress / Required Fields / Reminder / Actions, ten with every tag slot populated — absorbs the retired Monitoring page's per-reviewer progress + reminder affordances. (It shipped seven-column in 11C; the three tag columns arrived at 19I Item 11 and this row had not caught up.) Full-width info card since 19P.5, and the filter strip sits in the table card's **two-pane toolbar** with its submit relabeled `Apply` → `Search`; no selection and no expander — the rows stay links |
| `GET /operator/sessions/{id}/invitations/reviewers/{rid}` | reviewer drill-in from a Manage Invitations row. **Keyed on the reviewer since 19P.6**, so the row's link renders whether or not an invitation exists. Invitation card reports three facts — created / email sent (with delivery state) / last reminder — plus a four-state URL region; Review Progress card carries **Open reviewer surface** to `/preview-surface/1` in a new tab |
| `GET /operator/sessions/{id}/invitations/{iid}/detail` | the pre-19P.6 invitation-keyed drill-in (Segment 11C Part 1). **308** to the reviewer URL above, kept for bookmarks |
| ~~`POST /operator/sessions/{id}/invitations/generate`~~ | **Retired 19Q Item 2 rung 3** — returns 404. Prepare creates one invitation per eligible reviewer inside `POST /workflow/prepare`, so the step has no route of its own. |
| `POST /operator/sessions/{id}/invitations/send-all` | write outbox row per **sendable** invitation — `pending` and the reviewer still assigned-and-active (`invitations.list_sendable_invitations`, 19Q.2 rung 1); route gates on `validated` or `ready`, button on `validated` onward |
| `POST /operator/sessions/{id}/invitations/{iid}/send` | send a single invitation (rotates token; route gates on `validated` or `ready`, per-row button `ready`-only) |
| `POST /operator/sessions/{id}/invitations/{iid}/regenerate` | rotate token + reset to pending (route gates on `validated` or `ready`, per-row button `ready`-only) |
| `POST /operator/sessions/{id}/invitations/{iid}/remind` | send a single reminder reusing the prior invitation URL (route gates on `validated` or `ready`, per-row button `ready`-only) |
| `POST /operator/sessions/{id}/invitations/remind-incomplete` | bulk reminders to every incomplete reviewer (route gates on `validated` or `ready`; the Workflow card's Send reminders button is `ready`-only). Single-source endpoint for both Manage Invitations and Responses bulk-remind buttons (Segment 11C Part 1) |
| `GET /operator/sessions/{id}/responses` | reviewee-centric coverage view (Segment 11C Part 1). Each row classifies a reviewee per `monitoring.AT_RISK_THRESHOLDS` (Complete / Adequate / At risk / No responses). Same 19P.5 move as Invitations: full-width info card, filter strip in the table card's **two-pane toolbar**, `Search` for `Apply` |
| `GET /operator/sessions/{id}/responses/{reviewee_id}/detail` | reviewee drill-in scaffold from the Responses table (Segment 11C Part 1) |
| `GET /operator/sessions/{id}/export/settings.csv` | Settings extract (Segment 12A-1 PR 1) — 3-column CSV `key,value,...`; emits `session.settings_extracted` |
| `GET /operator/sessions/{id}/export/reviewers.csv` | Reviewers extract (12A-1 PR 2) — column shape matches `parse_reviewer_csv` so files round-trip; emits `session.reviewers_extracted` |
| `GET /operator/sessions/{id}/export/reviewees.csv` | Reviewees extract (12A-1 PR 2) — column shape matches `parse_reviewee_csv` (incl. `PhotoLink`); emits `session.reviewees_extracted` |
| `GET /operator/sessions/{id}/export/relationships.csv` | Relationships extract (12A-3 PR 1) — 6-column CSV (`ReviewerEmail` / `RevieweeEmail` / `PairContextTag1..3` / `Status`) matching `parse_relationship_csv`; round-trips with the importer shipped by 15D PR 1; emits `session.relationships_extracted` |
| `GET /operator/sessions/{id}/export/responses.csv` | Responses extract (12A-1 PR 4 + 4a) — 20-column wide CSV streamed via `yield_per(1000)`; lifecycle (saved / submitted / version) + uppercase `TRUE` / `FALSE` `SelfReview` flag; emits `session.responses_extracted` |
| `GET /operator/sessions/{id}/export/audit_log.csv` | Audit-events extract (12B PR 1) — 8-column wide CSV (`EventType` / `Severity` / `Summary` / `ActorEmail` / `CorrelationId` / `CreatedAt` / `DetailJson`) with the canonical Segment 11K detail envelope JSON-encoded in the trailing column; LEFT JOIN against `users` for ActorEmail; streamed via `yield_per(1000)`; emits `session.audit_log_extracted`. **No operator-facing UI surface today** — the Extract Data tile retired in 12B PR 2 (#789); the route relocates to the Sys Admin page when Segment 16A ships |
| `POST /operator/sessions/{id}/import-config` | Settings importer (12A-3 PR 3) — multipart `file` upload of a 3-column Settings CSV; `apply_session_config()` parses + applies as wipe-and-replace inside one transaction; lifecycle gate `status in {"draft", "validated"}` invalidates `validated → draft`; emits `session.settings_imported`; reachable via Quick Setup slot 4 (12A-3 PR 4) |
| `GET /operator/sessions/{id}/outbox` | dev-mode email outbox view for the session. **Not a chrome tab** — reachable via the "View outbox" button on Manage Invitations |
| `GET /operator/sessions/{id}/monitoring` | 303-redirects to `/invitations`. The Monitoring page itself was retired in Segment 11C Part 1; the redirect preserves old bookmarks |

### Reviewer-facing app

| URL | What it does |
|---|---|
| `GET /me` | dashboard: sessions where user has an active `Reviewer` row; per-session pill (`not started` / `in progress` / `submitted`) |
| `GET /me/invite/{token}` | invitation token landing — Easy Auth required, email-match check, stamps `opened_at`, 303 to surface |
| `GET /me/sessions/{id}` | review surface: editable table of assigned reviewees and Default Instrument fields; ?saved=ok / ?submitted=ok flash banners |
| `POST /me/sessions/{id}/save` | upsert response cells (empty value deletes the row); 303 → surface with `?saved=ok` |
| `POST /me/sessions/{id}/submit` | persist + validate required; 400 + warn-and-override on missing without acknowledge; else stamps `submitted_at` and 303 → surface with `?submitted=ok` |
| `POST /me/sessions/{id}/clear` | delete every response for this reviewer in this session (confirm checkbox required); 303 → surface |

The Cancel link on the surface is just `<a>` back to `GET /me/sessions/{id}` — no server-side state change.

### Sessions

- Create with name, code (unique per operator), description, deadline.
- Session creation **also synchronously creates the Default
  Instrument** with two seed response fields (`rating` integer 1–5
  required; `comments` long text optional) and three seed display
  fields (`pair_context_1/2/3`, `visible=true`, `label=''`). Operator
  edits both kinds via the consolidated `/instruments` page (10A:
  response-field builder + friendly description; 10B-1: data-driven
  reviewer-surface render; 10B-2: display-field picker + shared
  field-order bulk form, replaced by the 10C per-instrument card
  shape — Display Fields renders a hardcoded 6-row CSV-named
  placeholder with persistence deferred, while the 10B-2
  schema-level routes remain in place; Response Fields inline edit
  + Required auto-submit + row-level Add/Delete are wired). The
  seven supported display-field sources at the schema layer are
  `reviewee.tag_1/2/3`, `reviewee.profile_link`, and
  `pair_context.1/2/3`; `assignment_context_*` is deliberately
  excluded. See `spec/architecture.md` "Conceptual hierarchy."
- View detail with live counts of reviewers, reviewees, assignments,
  and the current `assignment_mode`.
- **Edit** name / code / description / deadline; changes recorded as
  `session.updated` with a `changes: {field: [old, new]}` map.
- **Delete** session — removes operators, reviewers, reviewees,
  instruments, assignments, invitations, and the session's audit
  events; a final `session.deleted` event with `session_id=None`
  survives in the global audit log. Requires explicit confirm
  checkbox.
- **Rehydrate** a complete extracted session (Segment 18P Group 2) —
  the **Rehydrate** button in the lobby Filter-card row opens
  `/operator/sessions/rehydrate`, where a complete set of extract CSVs
  (loose or zipped) is uploaded and **Validate**d (a mandatory
  pre-flight: completeness + cross-file integrity + a `_REHYD`
  name/code + entity-count preview). On a clean verdict the set is
  stashed (`rehydrate_stashes`, operator-scoped, 1-hour TTL) and
  **Rehydrate** re-runs the analyzer then rebuilds a `<name>_REHYD`
  **draft** — settings, rosters, relationships, observers, regenerated
  assignments, and byte-equal submitted responses — landing not
  activated, with a provenance note and all-or-nothing rollback.
  Invitations, email send history, and results-acknowledgements are
  **not** restored. Emits `session.rehydrated`. See `spec/rehydrate.md`.

### Reviewers & reviewees

- **CSV upload** with required `ReviewerName/ReviewerEmail` (or
  `RevieweeName/RevieweeEmail`); optional `Tag1/2/3` for future
  RuleBased; optional `PhotoLink` on reviewees.
- **One-shot replace** with explicit confirm checkbox when the session
  already has rows. CSV files cap at 1 MiB / 5000 rows. Unknown
  columns are silently ignored. UTF-8 with BOM tolerated.
- **Browseable Manage views** showing the saved rows in a table,
  with an anchored `Upload CSV` card on the same page (no separate
  `…/import` GET) and a disabled `Edit Reviewers` / `Edit Reviewees`
  button reserved for the future inline-edit pattern.
- **Setup validation** page lists structural issues (no reviewers, no
  reviewees, duplicate emails) plus info-level placeholders for not-
  yet-implemented surfaces.
- **Cascade safety**: re-uploading a roster on a session with
  assignments deletes those assignments via ORM cascade. Operator
  sees a warning before they confirm. Audit event records the
  cascaded count.
- **Delete all** reviewers / reviewees from the roster Manage page
  with explicit confirm checkbox. Cascades to assignments. Audit
  events `reviewers.deleted_all` / `reviewees.deleted_all` record
  both the deleted count and the cascaded assignment count.

### Assignments (post-15D)

- **Operations Assignments page** at
  `/operator/sessions/{id}/assignments` (chrome row label moved
  from Setup to Operations in 15D PR 6a). Hosts the **Assignment
  Rule** card (formerly "Rule Based Assignment"), the
  **Self-reviews** bulk-toggle card, and an **Assignment pairs**
  preview table. Assignments are now **always derived** —
  manual-row authoring retired in 15D PR 6a; the route still exists
  for test fixtures only (15D PR 7b).
- **Generation flow**: pick a RuleSet (5 seeds + caller-owned
  Personal RuleSets) from the Assignment Rule card, click Generate.
  The rule engine (`app/services/rules/engine.py`) reads pair-
  context tags from the `relationships` table via
  `pair_context_lookup` and runs predicates / combinators / quotas /
  deterministic ordering against the live populations. Inactive
  reviewers / reviewees and inactive `relationships` rows are
  excluded; audit `excluded_counts` records the reasons.
- **Self-reviews bulk toggle**: a per-session
  `sessions.self_reviews_active` Boolean drives bulk activation /
  deactivation of self-review pairs the engine produced. Per-row
  `Assignment.include` overrides individually post-flip.
- **`assignment_mode`** column on `sessions` records the strategy
  that last generated. `AssignmentMode` has one member,
  `rule_based`, so that is the only value written today; NULL means
  the session has never generated (a clone starts NULL). Legacy
  `manual` / `full_matrix` rows predate 16A PR 5 and no data
  migration has rewritten them. `Assignment.created_by_mode` records
  the same per row and defaults to `rule_based`; it defaulted to
  `"manual"` until 19N.1, naming a mechanism retired in 16A PR 5.
- **Per-pair attributes** live on the new `relationships` table
  (Setup page at `/operator/sessions/{id}/relationships`), not on
  `Assignment.context` — that JSON column dropped in 15D PR 6b.
  See "Pair-level context post-15D" below for the full picture.

### Participant surfaces

The `/me/` chrome serves three audiences from one set of routes;
multi-role users (e.g. a workspace operator who is also a
reviewer on a session they don't own) get a role-navigator chip
strip to switch between surfaces. Identity matching on every
audience is case-insensitive email equality against the
session's active roster rows. Reviewees with non-email
identifiers fail the `/results` gate by construction (the
`reviewees.unreachable_for_results` Validate-page soft warning
flags this on Setup).

#### Reviewer at `/me/sessions/{id}/{page}`

- **Identity matching**: authenticated user matched to `Reviewer`
  rows by case-insensitive email equality. Only `Reviewer.status ==
  "active"` rows count.
- **Dashboard** at `/me` lists the user's sessions across all
  three audiences (Reviewer / Reviewee / Observer) with per-session
  pill computed from the role's progress state. The three audiences are
  **not symmetric** since 19F: a reviewer's and an observer's rows follow
  roster membership, but a **reviewee's row requires a currently-resolving
  visibility grant** (`visibility_policies.reviewee_has_current_grant`),
  so it appears and disappears as the windows move and is absent
  entirely on a session that grants them nothing. A user who is also a
  reviewer or observer on that session keeps the row; only the
  `reviewee` chip drops out. An archived session's observer row renders
  **unlinked**, beside the reviewer's "not opened".
- **Surface** is a multi-page form — instruments grouped into pages
  per `Instrument.page_number` + `Instrument.starts_new_page`
  (Segments 18L + 18M). Each page renders one instrument's table of
  (reviewee × response field) cells. A **group-scoped instrument**
  (Segment 13C) renders one row per boundary-defined group; one
  reviewer answer covers the whole group, counted once across
  reviewer state, monitoring, and the response extract. Each table
  row carries the reviewee identity columns + the instrument's
  visible `InstrumentDisplayField` rows + the instrument's visible
  response-field inputs (`InstrumentResponseField.visible`,
  Segment 18K) + a row-level submitted-status indicator.
- **Per-page status pills**: `not_started` / `in_progress` /
  `complete` / `submitted` (Segment 11D follow-on).
- **Save draft**: filtered to the current page's dirty inputs;
  upserts `Response` rows. Empty value deletes the row. Cell-level
  autosave landed in Segment 17B.
- **Submit**: page-aware on Submit; missing-required-on-current-page
  is a hard gate (the acknowledge-and-submit-anyway escape retired
  in Segment 11D follow-on); the missing-required card is its own
  full-width card below the bottom-grid.
- **Numeric input validation**: `<input type="number">` with
  `min` / `max` / `step="any"` + JS `setCustomValidity` for step-grid
  violations (`1e-6` tolerance) + server-side `validate_value`
  backstop in `app/services/responses/`.
- **Lifecycle gating**: reviewer save / submit / clear return
  **HTTP 403** unless the session is `ready`, the assigned
  instrument is `accepting_responses`, and `now() <
  session.deadline`. When the gate is closed, the surface renders
  read-only; saved values are hidden unless the operator turns on
  `responses_visible_when_closed` on the per-instrument sub-page.
  Deadline closure is observed lazily on every reviewer GET/POST
  and on the per-instrument operator page; the first observer flips
  `accepting_responses=false`, stamps `deadline_closed_at`, and
  emits one `instrument.closed reason=deadline` audit event.

#### Reviewee at `/me/sessions/{id}/results`

- **Identity gate**: `require_reviewee_with_current_grant` — matched to
  `Reviewee.email_or_identifier` case-insensitively, guarded by
  `participants.is_email_identified(reviewee)` (anonymous, non-email
  identifiers can't reach the page — Setup's
  `reviewees.unreachable_for_results` warning calls this out), **and**
  since 19F gated on a currently-resolving visibility grant. Every
  refusal is a **404**, so "no grant" and "not a reviewee here" are one
  outcome rather than two a caller could tell apart.
- **Body**: per-instrument sections, each rendered in one of three
  modes per the operator's per-instrument Band 3 visibility policy
  (`raw` / `anonymized` / `summarized`).
- **Summarized mode aggregates** per data type:
  Integer / Decimal → Average / Median / Min / Max;
  List → frequency + percentage;
  String → total count + average length.
- **Acknowledge card** at the foot stamps
  `reviewees.results_acknowledged_at` (idempotent; collapses to a
  `"✓ Acknowledged on {date}"` strip after the first click).

#### Observer at `/me/sessions/{id}/collation`

- **Identity gate**: matched to `Observer.email` case-insensitively;
  only `Observer.status == "active"` rows count; refusal is a **404**
  since 19F. Opted-in per-session via `sessions.observers_enabled`. An
  **archived** session renders no sections: the archive override closes
  the grant for every non-operator audience, which 19F made true of the
  observer view as well as the reviewee one.
- **Body**: per-instrument 3-row tables. Row 1 is distinct-reviewer
  headcount + shared aggregate over the observer's in-cohort
  assignment pool (partitioned by the observer's
  `cohort_rule`); Row 2 is distinct-reviewee headcount + same
  aggregate; Row 3 is a conditional `Download CSV` button.
- **Identification mode** follows Band 3: Raw rows, Anonymized
  rows, or Anonymized summaries. Anonymized downloads swap
  reviewer / reviewee names for per-session opaque tokens
  (`R-a3f8b2c1` / `E-9d4e7f10` via
  `app/services/participant_tokens.py`).
- **Cohort match rule** (multi-rule + AND/OR; e.g.
  `reviewer.tag1 IS THE SAME AS observer.tag1`) authored per-observer
  on the Observers Setup page partitions the session's assignment
  rows into the pool the observer can see.

### Audit log

Every mutating service writes an `audit_events` row with
`event_type`, `summary`, JSON `detail`, and a per-request
`correlation_id`. The `detail` JSON follows the canonical
envelope schema documented in
[`spec/architecture.md`](../spec/architecture.md) "Audit-event
detail schema" — pick one payload envelope
(`changes` / `snapshot` / `counts` / `set_changes`), pass
top-level identity slots (`session_id` / `session_code`), and
use the orthogonal slots (`reason` / `refs` / `context`) for
descriptive scalars. A per-event-type allowlist in
`EVENT_SCHEMAS` (in `app/services/audit.py`) validates each
emit on write — strict mode in tests fails any drift,
production mode logs and writes through. The 11K migration
window closed 2026-05-30; all rows written after that date
follow the canonical envelopes. Pre-cutover rows keep their
legacy shapes (the audit log is append-only).

The table below covers the most-emitted event types. The full
list lives in `EVENT_SCHEMAS`.

| event_type | When |
|---|---|
| `session.created` | new session |
| `session.updated` | edit form save (incl. `changes: {field: [old, new]}`) |
| `session.deleted` | session deletion (`session_id=None` in the row, original id in `detail`) |
| `reviewers.imported` | reviewer CSV save (incl. `cascaded_assignment_count`) |
| `reviewees.imported` | reviewee CSV save (incl. `cascaded_assignment_count`) |
| `reviewers.deleted_all` | delete-all from roster Manage view |
| `reviewees.deleted_all` | delete-all from roster Manage view |
| `assignments.generated` | FullMatrix or Manual save (incl. `mode`, `excluded_counts`) |
| `assignments.deleted_all` | delete-all from assignments hub |
| `responses.saved` | reviewer saves a draft (incl. `count`, `reviewer_id`) |
| `responses.submitted` | reviewer submits (incl. `count`, `missing_required_count`, `acknowledged_missing`) |
| `responses.cleared` | reviewer clears all their responses in a session |
| `responses.deleted_all` | operator-driven Delete Data on session detail (`detail.deleted_count`); allowed in any session status, including `ready` |
| `session.activated` | operator flips session draft→ready (`detail.override_warnings`) |
| `session.reverted_to_draft` | operator flips session ready→draft (`detail.closed_instrument_ids`, `response_count_at_revert`) |
| `session.invalidated` | operator flips session validated→draft (`detail.reason ∈ {"operator_revert", …}`); also auto-emitted by setup-mutating service code when the session was previously `validated` |
| `session.validated` | session marked validated on `?validated=1` when no blocking errors |
| `instrument.opened` | operator manually re-opens a closed instrument |
| `instrument.closed` | manual or lazy-deadline close (`detail.reason ∈ {manual, deadline}`) |
| `instrument.described` | operator edits the friendly description (`detail.description: [old, new]`) |
| `instrument.field_added` | operator adds a response field (`detail.field_key`, `label`, `response_type`, `required`, `validation`, `help_text`, `help_text_visible`) |
| `instrument.field_updated` | operator edits a response field (`detail.changes: {key: [old, new]}` for each changed key only) |
| `instrument.field_deleted` | operator deletes a response field (`detail.snapshot`, `cascaded_response_count`) |
| `instrument.fields_reordered` | up/down move OR bulk fields-save when response order changes (`detail.old_order`, `detail.new_order` as `field_key` lists; scoped to response fields) |
| `instrument.display_field_added` | operator adds a display field (`detail.source_type`, `source_field`, `label`, `order`, `visible`) |
| `instrument.display_field_updated` | operator edits a display field (`detail.changes: {key: [old, new]}` for each changed key only — `(source_type, source_field)` are immutable) |
| `instrument.display_field_deleted` | operator deletes a display field (`detail.snapshot`); no cascade since display fields have no per-row dependents |
| `instrument.display_fields_saved` | bulk fields-save when display rows' label / visibility / order changed (`detail.added` / `removed` always `[]` in 10B-2; `detail.updated` carries `[{source_type, source_field, changes: {key: [old, new]}}, …]`) |
| ~~`instruments.bulk_accepting_responses`~~ / ~~`instruments.bulk_visibility_when_closed`~~ | **Retired 18R Item 3** with their emitters (`bulk_set_accepting` / `bulk_set_visibility`). |
| `instrument.created` | operator creates a new instrument via the Instruments page **+Instrument** button (`detail.instrument_id`, `detail.session_id`, `detail.order`, `detail.after_instrument_id`). Multi-instrument is live. |
| `instrument.deleted` | operator deletes an instrument (`detail.instrument_id`, `detail.session_id`, `detail.name`, `detail.order`); cascade to response fields / display fields / assignments / responses runs via FK delete-orphan |
| `instrument.replicated` | operator clones a card's content into a new instrument after the source (Segment 13C **Replicate** button) |
| `invitations.generated` | bulk-create invitations on a ready session (`detail.count`, `detail.invitation_ids`, `detail.reviewer_ids`) |
| `invitation.sent` | outbox row written + invitation flipped to `sent` |
| `invitation.opened` | first valid token follow with matching email |
| `invitation.regenerated` | per-row token rotation + reset to `pending` |
| `reminders.sent` | batch reminder send — manual path (`detail.count`, `detail.invitation_ids`, `detail.reviewer_ids`, `detail.fell_back_count`) |
| `session.scheduled_activation_skipped` / `_retry` / `_failed_persistent` | Segment 18G Part 1 — lazy-observer outcomes for the scheduled `validated → ready` flip (`detail.reason`) |
| `session.scheduled_invites_skipped` / `_fired` | Segment 18G Part 2 — auto-send invitations on `invite_offsets` (`context.{anchor_at, offset_index, scheduled_at, actual_fired_at}`) |
| `session.scheduled_reminders_skipped` / `_fired` | Segment 18G Part 3 — auto-send reminders on `reminder_offsets`; precondition guard skips with `reason ∈ {not_ready, no_invitations, outside_response_window}` |
| `session.invite_schedule_updated` / `reminder_schedule_updated` / `activation_scheduled` | editor diffs / scheduling (Segment 18G Parts 1 + 2 + 3) |
| `session.responses_released` / `responses_release_stopped` | Workflow card Row 3 manual buttons (PR #1810) |
| `session.archived` / `unarchived` | manual / row-expander archive (Segment 18A) |
| `session.purged` | "Purge and archive" — selective hard-delete of responses / rosters / audit log (Segment 18C); `detail.counts` carries per-category drop counts |
| `session.settings_extracted` / `reviewers_extracted` / `reviewees_extracted` / `relationships_extracted` / `assignments_extracted` / `responses_extracted` / `observers_extracted` / `participant_tokens_extracted` / `data_shape_extracted` / `audit_log_extracted` | per-Extract Setup / Extract data tile downloads (Segments 12A / 12B / 18D + the participant model token-keys + Extract data tab) |
| `session.owner_added` / `owner_removed` | per-session Owners card on the Edit Session page (Segment 16B PR 2) |
| `observer.created` / `updated` / `deleted` / `bulk_inactivated` / `bulk_reactivated` | per-row + bulk observer service (`app/services/observers.py`) |
| `observers.imported` / `deleted_all` | Observers CSV save / delete-all |
| `observer.cohort_rule_assigned` | per-observer cohort-rule editor (Observers Setup page) |
| `reviewee.results_acknowledged` | reviewee clicks Acknowledge on `/me/sessions/{id}/results` |
| `data_shape.created` / `updated` / `deleted` | Extract data tab Data shaper authoring |
| `assignments.reconciled` | `replace_assignments` diff-and-reconcile (insert new / drop orphan + responses / keep matched) — replaces `assignments.generated` post-Reconciling-assignment-regeneration |
| `session.session_config_imported` | Quick Setup Settings slot apply (Segment 12A-3 PR 3) |

`excluded_counts` is a generic map (`{"self_review": N,
"inactive_reviewer": M, ...}`) so RuleBased exclusions in Segment 13 can
plug in additional reasons without a schema change. Today's keys are
`self_review`, `inactive_reviewer`, `inactive_reviewee`.

---

## What's deliberately not yet there

| Capability | Lands in |
|---|---|
| *(removed)* Vanilla-JS autosave on top of the reviewer `/save` endpoint — **shipped as Segment 17B on 2026-05-16 → 2026-05-20.** Cell-level autosave (debounced `fetch` to `/save`) layered as targeted progressive enhancement; no JS framework. | Done. |
| Activating SMTP / Graph / ACS / third-party transactional sends out of the dev outbox (today: outbox stamps `status="queued"` and never sends); institutional Microsoft 365 tenants typically block basic SMTP AUTH, so the realistic production path is one of Options B–D in `spec/email_infra_options.md`. **Deliberately gated on institutional Azure provisioning (decision 2026-09-05)** — and operationally optional meanwhile: since the participant model, access is roster + sign-in, so an operator's own generic email pointing at the app URL covers invitations (the in-app Guide's "Give reviewers access"); the real gap is per-reviewer reminders, chased by hand from the Responses page until then (rationale: `guide/segment_14B_email_infrastructure.md` Status) | **Segment 14B Parts A → H** (Part A consumes the 11E `EmailTransport` interface against the audit-log columns Segment 11C Part 2 scaffolds, with subsequent Parts F → H landing each non-SMTP backend driven by deployment / IT demand) |
| Audit-events export | **Segment 12B** (export shipped 2026-05-10; folded out of the original Segment 12 plan when Extract Data moved into 12A) |
| Scheduled / policy-driven retention — a per-deployment (or per-session) retention policy that purges aged responses / audit rows / archived sessions on a schedule. The operator-triggered half shipped as Segment 18C 2026-05-17. Deferred 2026-05-21 — the scheduled / policy-driven variant adds ops burden (cron, monitoring) for a need pilot data hasn't yet motivated. | Deferred — `guide/deferred_consolidated.md` ("18G Part 5 — Scheduled / policy-driven purge") |
| Scheduled auto-archive — a timed `draft → archived` flip on a deadline-anchored offset. Deferred 2026-05-21 — manual archive (Segment 18A row expander) + bulk archive on the lobby already handle the use case; the editor + observer wiring is marginal gain. | Deferred — `guide/deferred_consolidated.md` ("18G Part 4 — Auto-archive") |
| Spec-coverage hygiene — periodic sweep cadence + remaining Tier-1 specs (Email Template editor, Permissions) from `guide/archive/spec_sweep_11may.md` | **Segment 19A** (stub plan at `guide/archive/segment_19A_spec_documentation.md`) |
| *(removed)* Multi-instrument sessions: FullMatrix per-instrument target picker, reviewer dashboard per-instrument grouping — **shipped as Segment 15B (PRs #930 → #937 + #938 → #942) on 2026-05-13.** Each Instrument carries its own pinned rule + materialised pair set; the reviewer surface respects `Assignment.instrument_id`. Manual CSV `Instrument` column item retired post-15D. | Done. |
| *(partial)* In-app production hardening — **shipped as Segment 14A on 2026-05-18.** Structured logging + error handling + security headers + accessibility audit + runbooks + deploy-workflow concurrency. Remaining infra-side hardening (Key Vault, VNet, native Postgres types) deferred to `guide/deferred_consolidated.md`. | In-app done; infra deferred. |
| Local Postgres docker-compose for dev (SQLite + the `ci-postgres` job + migration-on-deploy is the parity story today) | **Segment 14A** (production hardening) per `guide/todo_master.md` — likely settles "won't fix" via the developer setup guide work in **Segment 20**. |
| *(removed)* Sort by reviewee on the reviewer surface — **shipped as Segment 13B on 2026-05-12.** Operator picks up to 3 default sort columns from the Display Fields table via the tri-state Sort control; reviewer overrides at view time via clickable column headers; per-table cookie persistence. | Done. |
| *(removed)* Pervasive friendly labels for `ReviewerTag1-3` / `RevieweeTag1-3` / `PairContext1-3` / `RevieweeName` / `RevieweeEmail` / `PhotoLink` — **shipped as Segment 15A (PRs #887 → #891) on 2026-05-12.** Display labels flow through every operator preview surface, the Assignments column-toggle widget, the Instrument editor's Friendly Label column, and the reviewer surface; underlying logic everywhere keeps the canonical machine name. | Done. |
| *(removed)* Operator RTD / RuleSet libraries — symmetric two-tier model (operator master library + per-session copy with explicit Save-to-library / Add-from-library actions) — **shipped as Segment 15C (PRs #908 → #913) on 2026-05-12.** Workspace seeds materialise into every newly-created session's `session_rule_sets`; operator libraries auto-copy on session create; RTD + Rule Builder cards on the session surfaces carry paired Save-to-library / Add-from-library actions; the operator Settings page lists library contents with per-row delete and session-copy counts. | Done. |
| *(removed)* Per-instrument assignments — each Instrument carries its own assignment set — **shipped as Segment 15B (PRs #930 → #937 + #938 → #942) on 2026-05-13.** `Instrument.rule_set_id` now points at a per-session RuleSet copy; per-instrument Rule picker on the Instruments page; per-instrument status + Self review include + Show / Filter / Included columns on the Assignments page. | Done. |
| *(removed)* Group-scoped instruments + Replicate instrument action-row button — **shipped as Segment 13C on 2026-05-18 → 2026-05-19.** Group-scoped flavour authorable via **Add group instrument**; one reviewer answer covers a whole boundary-defined group; **Replicate** clones a card's content into a new instrument after the source. | Done. |
| *(removed)* Operations Workflow Card — **shipped as Segment 15E on 2026-05-14.** Single persistent **Workflow** card at the top of Session Home + every Operations-row page; ten-state cascade; uniform seven-stage stepper; **Activate session** super-button collapses Generate + Validate + Activate with per-step rollback and a warnings detour to Validate. Bulk-action buttons on Invitations + Responses retire into the card; info cards reshape to inline coverage / lifecycle counters; yellow `.card.lock` notices on the three post-Operations pages retire. Spec at `spec/workflow_card.md`; plan archived at `guide/archive/segment_15E_operations_workflow_card.md`. | Done. |
| Sys admin page consolidating operator-internal / dev-only surfaces that exist today but lack a dedicated chrome surface | **Segment 16A** — shipped 2026-05-10 → 2026-05-11; plan archived at `guide/archive/segment_16A_sys_admin_page.md` |
| Role delegation among multiple operators (today: `SessionOperator` table exists with no UI for add / remove) | **Segment 16B** — shipped 2026-05-11; plan archived at `guide/archive/segment_16B_role_delegation.md` |
| Richer in-app audit views (today: 8-column CSV download only; no filter / search / drill-in / timeline) | **Segment 16C** — MVP shipped 2026-05-11; plan archived at `guide/archive/segment_16C_richer_audit_views.md`. PRs 4 + 5 (entity drill-in, cross-session search) carved out to `guide/deferred_consolidated.md`. |
| *(removed)* Sort-affordance refinements on the reviewer surface — **shipped as Segment 13B on 2026-05-12.** Sortable columns on reviewer + operator tables; operator default + reviewer live override. | Done. |
| AG Grid replacement of the reviewer-surface table (today: plain HTML `<input>` / `<textarea>` / `<select>` per cell, form-based save). Second half of workplan §11 that never landed. | **Off the roadmap** (2026-05-16) — a JS data-grid is judged overkill; recorded as an aspirational item in `guide/deferred_consolidated.md`. The reviewer-surface ergonomics it would have delivered (autosave, sticky headers, visible progress) fold into **Segment 17B** as progressive enhancement. |
| *(partial)* Reviewee + Observer participant surfaces — **shipped 2026-05-30 → 2026-06-03** under the participant-model upgrade umbrella. Reviewee `/me/sessions/{id}/results` body across all three visibility modes (Raw / Anonymized / Summarized) + Acknowledge; Observer `/me/sessions/{id}/collation` MVP with per-instrument 3-row stats + tokenized Anonymized downloads + per-observer cohort-rule editor; Workflow card Row 3 release / archive buttons; explicit per-instrument × per-audience × per-window visibility policy in `instrument_view_policies`. Remaining magic-link participation rides with Segment 14B email-side. Spec at `spec/participant_model.md`. | Mostly done; magic-link landing rides with **Segment 14B**. |
| *(removed)* Reviewer surface refinements — **shipped as Segment 17B on 2026-05-16 → 2026-05-20.** v2 polish + buttons order + status card location + row height tweaks; cell-level autosave; sticky bottom-grid action row. | Done. |
| Queue-based batch invitation sending (today: synchronous in-request loop over eligible reviewers; fine with the dev outbox, doesn't survive real SMTP latency + provider rate limits). Picks up workplan §12 work item #7. | **Segment 14B Part C** (bulk-send queue + background worker; plan at `guide/segment_14B_email_infrastructure.md`) |
| Technical-support contact (global env var, surfaces on app chrome footer + error pages + invalid-link landing). Distinct from the operational help contact on `ReviewSession` (which lives in #24). **Moved out of Segment 20 on 2026-09-05**: an unset env var renders nothing, so the mechanism needs no deployed host — only the address does. | **Unhomed** — a stub in `guide/todo_master.md` (Upcoming → Stubs) for the mechanism, + **Segment 20** (the address itself, at deploy). *(Corrected 2026-09-08: this cell read "Segment 19C Item 8", which was never true — Item 8 is the input-boundaries work, and the support contact sat unnumbered in 19C's Future items, unbuilt. It moved to `todo_master` when 19C closed.)* |
| Operator-facing **documentation of the deployed service** — a single administrator guide for the institutional host, the institutional half of `docs/troubleshooting.md`, and a currency pass over the in-app Guide (canonical since 19E rung 2, formerly `docs/quickstart.md`) + `docs/known_limitations.md`. The other six workplan §18 items are either already shipped (validation explanations, operator guide, developer setup guide, known limitations page) or moved out — three to **19E**, and the technical-support contact to what this file then called "19C Item 8". *(Corrected 2026-09-08: that was never true — 19C Item 8 is the input-boundaries work, and the contact sat unnumbered in 19C's Future items, unbuilt. It is now a stub in `guide/todo_master.md`.)* See the audit table in the plan. | **Segment 20** — **reserved 2026-09-05, gated on the institutional Azure deployment concluding** (`docs/deployment_nus.md`); plan at `guide/segment_20_operator_polish_and_documentation.md` <!-- path-ref-ok --> |
| *(removed)* **Reviewee participation disclosure** — **shipped as Segment 19F on 2026-09-07.** An active reviewee had been getting a `/me` row — name, status, a live link to `/results` — from the moment the roster was uploaded, in every lifecycle state, whether or not any instrument granted them a view, and on an archived session after that decision had been withdrawn. The gate is now a currently-resolving visibility grant; `/results` refuses without one. Observers and reviewers are unaffected by author decision, and sign-in stays open to the whole tenant. The observer archive-override gap (`spec/role_landing_and_visibility.md` §6) closed with it, and the wide rung — all four session-scoped gates answering 404 — closed the session-enumeration vector alongside. Plan archived at `guide/archive/segment_19F_reviewee_participation_disclosure.md`. | Done. |

---

## Architectural notes worth preserving

### FullMatrix is a (future) RuleBased preset

FullMatrix and Manual currently have parallel implementations, but the
storage model treats them uniformly: every assignment is a row in
`assignments` with `created_by_mode` as a string discriminator and
`Assignment.context` as JSON. Segment 13 RuleBased is expected to
introduce a generic generation framework; FullMatrix becomes the
simplest preset of that framework. The audit-detail shape
(`excluded_counts: {...}`) is already generic; Manual rows ship with
`excluded_counts: {}`. The only friction is one specific service
function name (`generate_full_matrix`) and one preview template.

### Replace-all everywhere — except assignment regeneration

CSV imports follow a replace-all shape: explicit confirm checkbox
when rows already exist; audit event records old count, new count,
and any cascaded downstream deletions.

Assignment **regeneration** is the deliberate exception (2026-05-16,
`spec/reconciling_regeneration.md`). `replace_assignments`
reconciles instead of wholesale-replacing: it diffs the rule
engine's pair set against the existing `Assignment` rows, inserts
newly eligible pairs, deletes pairs the rule no longer produces
(with their responses), and leaves matched pairs and their saved
responses untouched. The `assignments.generated` audit event
carries `new` / `deleted` / `kept` / `responses_deleted` counts.
The Activate super-button dry-runs the reconcile
(`assignments.reconcile_impact`) and only prompts for confirmation
when a run would actually delete responses.

### Multi-instrument support

The data layer and the operator + reviewer surfaces are
multi-instrument-aware. Every session seeds one Instrument at
creation time via `ensure_default_instrument` (system handle
`Default`, operator-editable `description`, two seed response
fields, three seed `pair_context_1/2/3` display fields). The
schema columns (`Instrument.session_id`, `Instrument.order`,
`Assignment.instrument_id`) and the FK delete-orphan cascades are
in place; `create_instrument(after_instrument_id=…)` and
`delete_instrument(...)` exist as service helpers and emit the
`instrument.created` / `instrument.deleted` audit events; the
reviewer surface and the operator's `/instruments` page loop over
instruments; and the `Add an instrument` / `Delete this instrument`
operator buttons are wired (10D Slice 5, 2026-05-02) with mutual-
exclusion + single-instrument-floor gates. See
`spec/architecture.md` "Conceptual hierarchy."

The original Segment 13 plan (multi-instrument sessions) is
archived at
`guide/archive/segment_13_multi_instrument_sessions_superseded.md`
since most of its scope shipped early in Segments 10A → 10D. Three
items did not ship and live in `guide/archive/unfinished_business.md` as
#27 (FullMatrix per-instrument target picker), #28 (Manual CSV
`Instrument` column), and #29 (reviewer dashboard per-instrument
grouping).

### Pair-level context post-15D

Pair-level context (the three `tag_N` slots used by the rule
engine via `pair_context.tag_N` predicates) lives on the new
`relationships` table — created inert in **13E PR 2** and
populated by the per-entity Relationships Setup page +
Quick Setup slot shipped in 15D. The legacy
`Assignment.context` JSON column was dropped in **15D PR 6b**;
the original `assignment_context_*` family retired with it.
The rule engine reads pair-context via an eager
`relationships.pair_context_lookup(db, session_id) -> dict`
pre-built once per `engine.evaluate` call (15D PR 4) so
predicate evaluation stays single-pass. See
`spec/architecture.md` and `guide/archive/segment_15D_assignments_revamp.md`.
